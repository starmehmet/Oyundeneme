# İLERLEME TAKİBİ

> Durum işaretleri: ⬜ Başlamadı · 🟨 Devam ediyor · ✅ Bitti (testler yeşil) · ⛔ Bloke
> Bir sistem ancak **birim testleri + entegrasyon testleri geçince** ✅ olur.
> Detaylı mimari: [MIMARI.md](MIMARI.md)

## Faz 1 — Dikey Dilim (Hafta 1–4)

| # | Sistem | Durum | Sahip Dosyalar | Bitti Sayılma Şartı (DoD) |
|---|--------|-------|----------------|---------------------------|
| 1 | Oyun Saati / Zaman | ✅ | `Source/.../Time/TimeKeeper.h`, `DayNightCycle.h`, `GameClock.h`, `TimeMath.h`, `TimeSettings.h` | Zaman akıyor, 10x ölçek, gündoğumu/batımı delegate'leri ateşleniyor, ışık dönüyor — **derleme başarılı (UE 5.8, MSVC 14.50.35737), 3/3 otomasyon testi PASSED** (`SurvivalGame.Time.TimeMath.{Temeller,GunesPitch,IsikFaktoru}`) |
| 2 | Oyuncu Kontrolü | ✅ | `Core/SurvivalPlayerController.h`, `Player/PlayerCharacter.h`, `Player/CameraManager.h`, `Player/CameraMath.h` + editör varlıkları (`Input/IA_*`, `IMC_Default`, `BP_PlayerController`, `BP_MainGameMode`, `Maps/Tests/TestPlayer`) | Derleme + 4/4 birim testi + **PIE'de canlı doğrulama: WASD yürüyüş + fare kamera (operatör elle test etti, pawn transform kaydı objektif kanıt)**. Ayrı `UInputHandler` katmanı YOK (bilinçli, bkz. ADR). Editör varlıkları MCP köprüsüyle (ModelContextProtocol + EditorToolset) programatik oluşturuldu |
| 3 | Etkileşim Çerçevesi | ✅ | `Interaction/InteractionMath.h`, `InteractableInterface.h`, `InteractionComponent.h`, `InteractableBase.h` + editör varlıkları (`IA_Interact`, IMC'de E eşlemesi, BP CDO, `TestPlayer` haritasında görünür test küpü) | E ile etkileşim, prompt delegate'i, 4-frame trace throttle, `ECC_Interaction` kanalı — derleme temiz + 7/7 test PASSED + **PIE'de canlı doğrulama: `InteractionCount` 0→94 (6 ayrı E-basışı, operatör elle test etti, MCP üzerinden objektif okuma)** |
| 4 | Envanter | ✅ | `Inventory/InventoryComponent.h`, `InventoryMath.h`, `ContainerActor.h` | Ekle/çıkar/yığ/transfer, ağırlık limiti, OnInventoryChanged broadcast — derleme temiz + 10/10 test PASSED (tüm sistemler dahil regresyonsuz) + **PIE canlı doğrulama: konteyner 7→0, oyuncu 0→7 tek E-basışında.** `UContainerComponent` yerine `AContainerActor` (kendi envanteri + IInteractableInterface, "hepsini topla" + `StartingItems`) — UI yok, minimal ama gerçek test içeriği |
| 5 | Öğe Veritabanı | ✅ | `Items/ItemDatabase.h`, `ItemDefinition.h`, `ItemDatabaseSettings.h`, `DT_Items` (33 satır: orijinal Odun/Balta/Tas + içerik hacmi genişletmesinden +30, *2026-07-26*) | DataTable'dan yükleme, TMap cache (kopya, ham işaretçi değil), tag/kategori filtreleme — derleme temiz + testler PASSED + **PIE'de canlı doğrulama: "ItemDatabase hazir: 3 ogem yuklendi" log'u + ağırlık hesabı doğru (Odun×5 + Balta×2 = 7).** Sıralama MIMARI.md'den DÜZELTİLDİ: Envanter #5'e bağımlı olduğundan önce yazıldı |
| 6 | Zanaat (Crafting) | ✅ | `Crafting/CraftingComponent.h`, `CraftingMath.h`, `RecipeDatabase.h`, `RecipeDefinition.h`, `DT_Recipes` (Balta_Tarifi: Odun×3+Taş×2→Balta) | Tarif doğrulama, malzeme tüketimi, kuyruk (max 16), iptalde iade — derleme temiz + 13/13 test PASSED + **PIE canlı doğrulama: `craft_start Balta_Tarifi` → "Zanaat basladi" → 2sn sonra "Zanaat tamamlandi: basarili", ağırlık 7.5→3.5→5.5 (tam beklenen).** `ACraftingStation` (istasyon-yakınlık) YAZILMADI, DoD'de yok; `craft_start <RecipeID>` konsol komutu eklendi (dev-tool + PIE testi için) |
| 7 | İnşaat | ✅ | `Construction/FoundationGrid.h`, `ConstructionGhost.h`, `BuildingBase.h`, `ConstructionComponent.h`, `BuildingDefinition.h`, `BuildingDatabase.h`, `DT_Buildings` (Depo: Odun×5) | Izgara yapışma (400 UU), ghost önizleme, çarpışma doğrulama (ızgara hücre işgali), yıkım — derleme temiz + 15/15 test PASSED (2 yeni İnşaat grubu, regresyonsuz) + **PIE canlı doğrulama: `build_place Depo` → hücre (1,0) başarılı (ağırlık 12.5→7.5), `build_demolish` → hücre boşaldı (sahnede yalnızca 1 `BuildingBase`, yetim aktör yok), aynı hücrede `build_place Depo` tekrar başarılı (ağırlık 7.5→2.5).** `AConstructionManager`(GameState)+`AFoundationGrid`(Actor) ikilisi YAZILMADI — tek bir `UFoundationGrid` (`UWorldSubsystem`), CLAUDE.md'nin "Singleton=Subsystem" kuralı; inşaat ANLIK tamamlanır (iskele/zamanla-inşa yok); ghost kırmızı/yeşil renk-geri bildirimi yok (içerik yok henüz); `RequiredMaterials` MIMARI'deki `FInventorySlot` yerine Crafting'in `FRecipeIngredient`'ı kullanır |
| 8 | Lojistik Ağı | ✅ | `Logistics/LogisticsNetwork.h`, `StorageNode.h`, `LogisticsMath.h`, `LogisticsSettings.h`, `StorageNodeType.h` | Node kayıt, rota önbelleği, taşıma isteği kuyruk, teslimat — derleme temiz + 18/18 test PASSED (3 yeni Lojistik grubu, regresyonsuz) + **PIE canlı doğrulama: 2 `AStorageNode` (Source: Odun×20, Sink: boş, 2000 UU arayla), `logistics_request Odun 5` → log "2000 UU, 4.0sn" (mesafe/hız doğru hesaplandı) → Source 20→15 (anında tüketim), ~4sn sonra Sink 0→5 (zamanlı teslimat).** İnceleme 2 gerçek çalışma-zamanı hatası buldu, ikisi de düzeltildi: (1) `AStorageNode`'un kök bileşeni YOKTU — `GetActorLocation()` her zaman (0,0,0) dönüyordu (tüm mesafe/rota mantığını kırardı); (2) `FindBestDestination` Source'u kendisi hariç tutmuyordu — Source da bir alıcı-tipiyse (varsayılan Container) mesafesi 0 olduğundan HER ZAMAN kendini seçip rotayı tamamen kırıyordu. PIE testindeki "2000 UU, 4.0sn" log satırı bu iki düzeltmenin GERÇEKTEN çalıştığının doğrudan kanıtı (düzeltilmeselerdi mesafe 0 çıkardı). `AConveyorBelt`/`ATransportDrone` (fiziksel taşıma) YAZILMADI — teslimat `ULogisticsSettings::TransportSpeed` ile mesafe/hız zamanlayıcısı kullanıyor |
| 9 | Üretim Makineleri | ✅ | `Production/ProductionMachine.h`, `ProductionManager.h`, `ProductionMath.h`, `ProductionRecipe.h`, `ProductionRecipeDatabase.h`, `DT_ProductionRecipes` (OdunIsleme: Odun×2→Taş×1, 3sn, 2 enerji/sn) | Giriş→çıktı döngüsü, Blocked durumları (Idle/Running/Blocked_NoInput/Blocked_NoOutput/Blocked_NoFuel), yakıt tüketimi, kare bölümleme (`UProductionManager`, `AProductionMachine`'in KENDİ tick'i yok) — derleme temiz + 21/21 test PASSED (3 yeni Üretim grubu, regresyonsuz) + **PIE canlı doğrulama (30+ saniye sürdürülen çalışma): `production_set_recipe OdunIsleme` → 10 üretim döngüsü, her biri TAM 3.0sn arayla ("Uretim tamamlandi" log'ları 06:18:24→27→30→...→51) → InputBuffer 20→0 (10×2 Odun), OutputBuffer 0→5 (10×1 Taş, ağırlık doğru), enerji 100→40 (30sn×2/sn) → girdi tükenince `State: Blocked_NoInput`.** İnceleme 3 gerçek çalışma-zamanı hatası buldu, üçü de düzeltildi: (1) `GetProgress()` ham saniyeyi (normalize edilmemiş) dönüyordu — `SurvivalProduction::ComputeProductionProgress` hiç çağrılmıyordu; (2) `Tick_Production`, kare-bölümleme yüzünden bir güncellemenin BİRDEN FAZLA üretim döngüsünü kapsadığı durumda fazla süreyi ATIYORDU (`Progress=0`'a sıfırlayıp döngüye devam etmiyordu) — PIE'deki 10 ardışık, tam-3sn-aralıklı döngü artık bu düzeltmenin gerçekten çalıştığının kanıtı; (3) `HasSufficientOutputRoom` her çıktı satırını AYNI (henüz değişmemiş) `CurrentWeight`'e karşı bağımsız kontrol ediyordu — çok satırlı bir çıktıda toplam ağırlık aşılsa bile her satır tek başına "sığar" görünebilirdi; `InventoryComponent::HasRoomForBatch` (yeni, toplu/tek-seferlik ağırlık kontrolü) + `FProductionRecipe::GetAggregatedOutputs()` ile düzeltildi. `AProductionMachine`, `ABuildingBase`'den türüyor (MIMARI hiyerarşisi) ama BU PASODA `UConstructionComponent`/`build_place` akışına BAĞLANMADI — test spawn'ı doğrudan `SpawnActor` ile yapıldı |
| 10 | Kaynak Simülasyonu | ✅ | `Production/ResourceSimulation.h`, `ResourceSimulationMath.h`, `ResourceTypes.h`, `ScarcityReason.h`, `ResourceSimulationSettings.h` | Enerji bütçesi (üretim/tüketim/denge), frekans (50Hz, `IsFrequencyStable`/`IsBrownout`), kıtlık alarmı (edge-triggered `OnScarcityStateChanged`), termal bütçe (birikimli sıcaklık, ayrık atalet modeli) — derleme temiz + 24/24 test PASSED (3 yeni Kaynak grubu, regresyonsuz) + **PIE canlı doğrulama (operatör elle test etti): `resource_add_fuel Motorin 10 10 3` → ~3sn sonra "Kitlik alarmi: basladi (sebep: 2=FuelCritical)"; `resource_register_consumer 100` (üretici yok) → frekans 50→0Hz, alarm nedeni 2→1 (EnergyBrownout) DEĞİŞTİ (bInScarcity aynı kalırken sebep değişimi doğru broadcast edildi — inceleme bulgusunun özellikle sorguladığı kenar durum); `resource_register_producer 150` → enerji düzeldi (frekans 50Hz istikrarlı) ama sebep 1→2'ye GERİ döndü (yakıt hâlâ kritikti) — aynı mantığın ters yönde kanıtı.** İnceleme 1 gerçek çalışma-zamanı hatası buldu: `AddFuel`, `MaxAmount` belirtilmeden yapılan İKİNCİ ve sonraki her çağrıda tavanı büyütemiyordu — rezerv sessizce ilk yatırılan miktara kilitleniyordu, sonraki eklemeler kayboluyordu; düzeltme: `AddFuel` artık `AddItem`/`RequestTransport` ile aynı desende GERÇEKTEN kabul edilen miktarı `float` olarak döner, `resource_add_fuel` konsol komutu "TAVANA ULASILDI" ile görünür kıldı. Ayrıca derleme sırasında yeni bir UHT hatası (`BlueprintReadOnly` private üyede) ve Windows Smart App Control'ün derlenmiş DLL'i engellemesi (kod hatası DEĞİL, işletim sistemi güvenlik ilkesi) atlatıldı. Bu, **Faz 1'in son sistemi** — "Dikey Dilim" tamamlandı |

**Faz 1 çıkış kriteri:** Oyuncu ağaç kesip → zanaatla malzeme üretip → fırın inşa edip → konveyörle depoya bağlayıp → otomatik üretim döngüsü kurabiliyor. 60 FPS sabit.

> **Durum notu (2026-07-24, güncellendi 2026-07-25):** 10 sistemin TÜMÜ ✅ ama aralarındaki ÜÇ entegrasyon eksiği kasıtlı olarak bu fazın kapsamı dışında bırakılmıştı (her biri kendi ADR'sinde gerekçelendirildi). Faz 2 tamamlandıktan sonra bu borcun (1) ve (2) numaralı kısımları KAPATILDI:
>
> **(1) `AProductionMachine` artık `UConstructionComponent`/`build_place` akışına bağlı** — `FBuildingDefinition`'a `BuildingClass` (`TSubclassOf<ABuildingBase>`, boşsa geriye-uyumlu `ABuildingBase`) + `ProductionRecipeIDs` eklendi; `ConfirmPlacement` artık `Def.BuildingClass`'a göre spawn ediyor, `AProductionMachine::BeginConstruction` (yeni override) tarifleri `AvailableRecipeIDs`'e kopyalıyor. **(2) Giriş/çıkış arabellekleri `ULogisticsNetwork`'e KAYITLI** — `AProductionMachine::BeginPlay` artık `InputBuffer`/`OutputBuffer`'ı iki `AStorageNode` proxy'si (`BindExternalStorage` ile GERÇEK arabelleğe bağlı, `MachineInput`/`MachineOutput` — Sistem #8'de baştan tanımlı ama hiç kullanılmamış enum değerleri) ile ağa kaydediyor. Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE 3 bulgu verdi (ikisi aynı kök neden: `SpawnActor`'ın SENKRON `BeginPlay`'i `SetNodeType`'tan ÖNCE `AcceptorCache`'i yanlış tiple kurardı — motor kaynağından doğrulandı), `SpawnActorDeferred`+`FinishSpawning` ile düzeltildi. **Derleme temiz + 51/51 test PASSED, regresyon yok.**
>
> **PIE doğrulaması TAMAMLANDI (2026-07-25, ikinci oturum):** DT_Buildings'e yeni bir satır (`OdunFirini`, `BuildingClass=AProductionMachine`, `ProductionRecipeIDs=[OdunIsleme]`) eklendi. **(2) canlı PIE'de doğrudan kanıtlandı** — sahnede Sistem #9'dan kalma önceden var olan bir `AProductionMachine` bile artık `production_dump`'ta "lojistik-giris-dugumu=kayitli lojistik-cikis-dugumu=kayitli" gösteriyor (üretim de sorunsuz devam etti: Odun tüketildi, Taş üretildi — regresyon yok). İlk `build_place OdunFirini` denemesi "malzeme yetersiz" ile başarısız oldu (oyuncu konteynerden Odun'u henüz toplamamıştı) — operatör konteyneri ÖNCE toplayıp TEKRAR denedi ve **(1) de canlı PIE'de doğrudan kanıtlandı**: `LogSurvivalProduction: Makine kaydedildi (toplam 3)`, `LogSurvivalLogistics: Dugum kaydedildi (toplam 5)`→`(toplam 6)` (yeni makinenin giriş+çıkış proxy'lerinin İKİSİ de kaydoldu), `LogSurvival: Insaat: 'OdunFirini' hucre (0,-1) - basarili`. MCP ile sahne taraması `ProductionMachine_1` adında YENİ bir aktörün var olduğunu da yapısal olarak doğruladı. **Faz 1 entegrasyon borcunun (1) ve (2) numaralı kısımları artık TAM anlamıyla kod+derleme+test+PIE ile kapatıldı.** **(3) `AConveyorBelt`** hâlâ bilerek YAZILMADI — gerçek görsel/fiziksel taşıma içerik gerektirir, dürüst kapsam dışı kalmaya devam ediyor (teslimat zaten mesafe/hız zamanlayıcısıyla fonksiyonel).

## Faz 2 — Simülasyon Derinliği (Hafta 5–8)

| # | Sistem | Durum | Sahip Dosyalar | Bitti Sayılma Şartı (DoD) |
|---|--------|-------|----------------|---------------------------|
| 11 | Hava Durumu | ✅ | `Weather/WeatherSimulation.h`, `WeatherMath.h`, `WeatherTypes.h`, `WeatherCondition.h`, `WeatherProfile.h`, `WeatherProfileDatabase.h`, `DT_WeatherProfiles` (9 satır) | 9 hava durumu, olasılıklı geçişler (ağırlıklı-rastgele + "atalet" çarpanı), 30 sn'de bir değerlendirme, broadcast — derleme temiz + 27/27 test PASSED (3 yeni Hava grubu, regresyonsuz) + **PIE canlı doğrulama (35+ dakika kesintisiz çalıştırıldı): `WeatherProfileDatabase hazir: 9 profil yuklendi`; onlarca geçiş TAM 30sn periyotla (bazı aralıklar 60/90sn — aynı durum yeniden seçilip geçiş atlanınca, düzeltmenin kanıtı); 7/9 durum gözlemlendi (düşük ağırlıklı Blizzard/Heatwave daha seyrek, beklenen); `weather_dump` çıktısı DT_WeatherProfiles'taki Clear satırıyla birebir eşleşti.** İnceleme 2 gerçek çalışma-zamanı hatası buldu, ikisi de düzeltildi: (1) `EvaluateWeather`'in "aynı durum seçildi, geçişe gerek yok" kısayolu, veritabanından HİÇ doldurulmamış struct-varsayılanlarını (`TransitionProgress` varsayılan 1.0f) "zaten oraya geçiş tamamlanmış" sanıp ilk değerlendirmede `BeginTransitionTo`'yu hiç çağırmayabiliyordu (rastgele seçim varsayılan Clear'ı tekrar seçerse) — `CurrentState` kalıcı olarak sahte varsayılanlarda kalırdı; `bHasInitializedState` bayrağıyla düzeltildi. (2) `LerpWeatherState`, `WindDirection`'ı (her zaman BİRİM vektör) düz `FMath::Lerp` ile enterpolasyon yapıyordu — neredeyse-ters-yönlü çiftlerde Alpha≈0.5'te vektör SIFIRA yaklaşıyordu (yön tanımsızlaşırdı); `FVector::SlerpNormals` (birim vektörler için küresel enterpolasyon) ile düzeltildi. 1 küçük bulgu da uygulandı: geçiş SÜRERKEN yeni bir değerlendirme tetiklenirse (varsayılan ayarlarda erişilemez ama yanlış yapılandırmada gerçek) devam eden geçişi tersine çevirebiliyordu — artık yalnızca `TransitionProgress>=1.0` iken değerlendirme yapılıyor. MIMARI'nin `AWeatherManager`(`AGameStateBase`)+`UWeatherSimulation`(subsystem) ikilisi TEK bir `UGameInstanceSubsystem`'e birleştirildi (Sistem #7/#9/#10 ile aynı "Singleton=Subsystem" kararı). **Bu, Faz 2'nin (Simülasyon Derinliği) İLK sistemi.** |
| 12 | Sıcaklık | ✅ | `Player/HealthComponent.h`, `HealthMath.h`, `Weather/TemperatureSimulation.h`, `TemperatureMath.h`, `ThermalZone.h`, `TemperatureSimulationSettings.h` | Ortam (hava+gün/gece) + bina içi (yalıtım+ısı kaynağı) sıcaklığı, oyuncu vücut sıcaklığı sürüklenmesi, güvenli aralık dışında hipotermi/sıcak-çarpması hasarı — derleme temiz + 33/33 test PASSED (5 yeni: 1 Health + 4 TemperatureMath grubu, regresyonsuz) + **PIE canlı doğrulama: normal havada (`Clear`=22°C) vücut sıcaklığı TAM 37°C'de sabit + can 100 sabit (30+ saniye izlendi); vücut sıcaklığı MCP ile 10°C'ye manuel düşürüldüğünde gerçek hipotermi hasarı uygulanıp can hızla 0'a düştü VE sıcaklık konfor hedefine (37°C) doğru geri sürüklenmeye başladı — hem "sıradan hava güvenli" hem "gerçek soğuk tehlikeli" hem "iyileşme" davranışları ayrı ayrı kanıtlandı.** İlk oyuncu-can sistemi: MIMARI'nin 28 sisteminde hiçbir yerde `Health`/hasar bileşeni yoktu (Sıcaklık DoD'si oyuncu hasarı gerektirdiği için gerçek bir mimari boşluk) — operatöre soruldu, **"Minimal UHealthComponent ekle"** seçildi ve genel-amaçlı (yeniden kullanılabilir, `TakeDamage`/`Heal` gerçek-uygulanan-miktarı döner) olarak yazıldı. **En ciddi bulgu, küçük-mod inceleme SONRASI canlı PIE testinde bulundu (birim testleri ve inceleme her ikisi de kaçırdı — ikisi de yalnızca izole pure-fonksiyonları doğruladı, uçtan-uca DENGEyi değil):** `ComputeBodyTemperatureDelta`'ya ham ortam/hava sıcaklığı doğrudan hedef olarak verilince, `DT_WeatherProfiles`'daki TÜM profiller (Heatwave hariç, en yaygın `Clear`=22°C dahil) `SafeMinBodyTemp`=35°C'nin altında olduğundan oyuncu **HER normal havada** (hiçbir aşırı koşul olmadan) hipotermiden ölüyordu — PIE'de doğrudan gözlemlendi (CurrentHealth ~1 dakika içinde 0'a düştü). Kök neden: insan çekirdek vücut sıcaklığı gerçekte dış hava sıcaklığına eşitlenmez (metabolizma/giysi normal havada ~37°C'de tutar), yalnızca gerçekten aşırı/uzun maruziyette sapar. Düzeltme: yeni `SurvivalTemperature::ComputeTargetBodyTemperature` — `[ComfortMinTemp=10, ComfortMaxTemp=30]` aralığındaki ortam sıcaklığı hedefi DEĞİŞTİRMEZ (37°C'de sabit kalır), yalnızca aralık DIŞINDAKİ fazlalık/eksiklik hedefe yansır; böylece sıradan hava güvenli, yalnızca `Snowing`(-2°C)/`Blizzard`(-10°C)/`Heatwave`(38°C) gerçek risk oluşturur — Sistem #13-14'ün "İlk kar fırtınası" (Faz 2 çıkış kriteri) milestone'uyla doğrudan uyumlu. Küçük-mod inceleme (2 mercek: ue-api, logic-lifecycle) ayrıca `ue-api` merceğinin bir ajanı bağlantı hatasıyla başarısız olup yeniden çalıştırıldı (workflow script'inde ayrıca bir index-hizalama bug'ı bulunup düzeltildi — filtrelenmiş bulgu dizisi yanlış mercek etiketiyle işaretleniyordu); 2 küçük (doğrulanmamış-ama-mantıklı) bulgu çıktı: (1) `ComputeBodyTemperatureDelta` önceki doğrusal (Euler) yaklaşım büyük `DeltaTime`'larda TAM kare-hızından-bağımsız değildi — `1-exp(-rate*dt)` TAM üstel formülüyle düzeltildi (artık kompozisyon altında matematiksel olarak kare-hızından tam bağımsız); (2) `GetAmbientTemperature()`/`GetEnvironmentTemperatureAt()` ilk Tick öncesi 20°C struct-varsayılanı döner — Sistem #10/#11'in aynı örüntüsüyle (Enerji/Hava durumu da ilk Tick'e kadar varsayılan) TUTARLI kabul edildi, düzeltme gerekmedi. `UTemperatureSimulation` (`UGameInstanceSubsystem`+`FTickableGameObject`, MIMARI'nin `GetInteriorTemperature(Building, DeltaTime)` imzası yerine anlık `GetEnvironmentTemperatureAt(Position)` — ayrı kalıcı termal-kütle modeli YOK), bölge kaydı Sistem #10'un self-pruning `TWeakObjectPtr` desenini tekrarlıyor; oyuncu tek-oyunculu varsayımla (`GetFirstPlayerController`) doğrudan bu sınıf tarafından her Tick'te işleniyor, ayrı bir oyuncu-kaydı YOK |
| 13 | Rüzgar | ✅ | `Weather/WindMath.h`, `WindSimulationSettings.h`, `WindSimulation.h`, `WindTurbine.h` | Konumsal rüzgar vektörü (küresel yön/hız + deterministik yerel hamle), türbin üretimi, yapı rüzgar-yükü (veri, henüz tüketen yok) — derleme temiz + 36/36 test PASSED (3 yeni WindMath grubu, regresyonsuz) + **PIE canlı doğrulama: `AWindTurbine` spawn edilip `UProductionManager`'ın kare-bölümlemeli zamanlayıcısı üzerinden `Running` durumuna geçti, `CurrentOutput=27.75` (0-50 aralığında, gerçek rüzgar hızıyla tutarlı kübik rampa).** `UWindSimulation` (`UGameInstanceSubsystem`) BİLEREK `FTickableGameObject` DEĞİL — küresel yön/hız her sorguda doğrudan `UWeatherSimulation::GetCurrentState()`'ten okunur (Sistem #11'in zaten sahip olduğu tek doğruluk kaynağı), yerel hamle konum+oyun-zamanına göre TAMAMEN durumsuz/deterministik hesaplanır (RNG yok, kayıt/temizleme yaşam döngüsü yok). **En ciddi bulgu, küçük-mod inceleme SIRASINDA (canlıya geçmeden ÖNCE) yakalandı:** `AWindTurbine::AdvanceProduction`/`GetProgress`'i override edecek şekilde yazıldı ama taban sınıf `AProductionMachine::AdvanceProduction` `virtual` İŞARETLENMEMİŞTİ (yalnızca `GetProgress` işaretlenmişti) — bu ya derleme hatası verir ya da (düzeltilmeseydi) `UProductionManager::Tick`'in HER ZAMAN taban-sınıf işaretçisi üzerinden çağırdığı `AdvanceProduction` sessizce taban sınıfın boş recipe-mantığına giderdi: türbin asla enerji üretmezdi, hiçbir hata/log olmadan. İki bağımsız mercek (ue-api, logic-lifecycle) de bunu BAĞIMSIZ olarak buldu — `virtual` eklenip derleme+PIE ile gerçekten override edildiği kanıtlandı. `AWindTurbine`, taban sınıfın recipe/girdi/çıktı makinesini KULLANMAZ ama `UProductionManager`'ın kayıt/kare-bölümleme altyapısını OLDUĞU GİBİ kullanır ve ürettiği enerjiyi `UResourceSimulation::RegisterEnergyProducer`'a kaydeder (Sistem #9/#10'un ikisini de yeniden kullanan, yeni bir paralel tick/muhasebe sistemi YAZILMADI) |
| 14 | Kar Birikimi | ✅ | `Weather/SnowMath.h`, `SnowSimulationSettings.h`, `SnowAccumulation.h` | Derinlik artış/azalış (hava+sıcaklıktan), hareket cezası, aşırı karda inşaat engeli, çığ riski (yamaç eğimi) — derleme temiz + 39/39 test PASSED (3 yeni SnowMath grubu, regresyonsuz) + **PIE canlı doğrulama (operatör elle test etti): `snow_set_depth 60` → birkaç saniye sonra `snow_dump`: `derinlik=35.2` (erime doğru işledi — donma noktası üstü ortam sıcaklığında beklenen), `hareket-carpani=0.58` (matematikle BİREBİR eşleşiyor: Lerp(1.0,0.4,35.2/50)), `insaat-engeli=EVET` (35.2>30 eşiği); ardından `build_place Depo` beklenen şekilde başarısız oldu.** Tam izole doğrulama YOK (aynı hücre önceki sistemlerin test aktörlerinden dolayı zaten dolu olabilirdi, ikinci bir "derinlik=0 → başarılı" karşı-testi operatörden istendi ama yanıt gelmeden devam edildi) — yine de `snow_dump`'ın `insaat-engeli` okuması `IsConstructionBlocked()`'ın GERÇEK, canlı bir derinlik değerinden doğru hesaplandığını DOĞRUDAN kanıtlıyor (Sistem #7'nin aynı-hücre-izolasyonu eksik kalan PIE testiyle aynı dürüst kabul standardı). MIMARI'nin `FSnowAccumulationGrid` (`TMap<FIntPoint,float>` per-hücre ızgara) taslağı BİLİNÇLİ olarak TEK bir global `CurrentSnowDepth`'e (Sistem #10'un `CurrentTemperature`'ıyla AYNI desen) sadeleştirildi — bu projede hava/sıcaklık zaten TAMAMEN global olduğundan bir ızgara hiçbir simülasyon değeri katmadan yalnızca aynı float'ın kopyalarını tutardı. Çığ riski (TEK konumsal unsur) oyuncunun bulunduğu noktada bir yer-normali line-trace'iyle sorgulanır, periyodik (her Tick değil) ve edge-triggered yayınlanır. Oyuncu hareket cezası `UCharacterMovementComponent::MaxWalkSpeed`'e HER Tick uygulanır; inşaat engeli `UConstructionComponent::EvaluatePlacementValidity`'e (Sistem #7) tek-nokta additive bir kontrolle bağlandı. **İnceleme bulgusu (canlıya geçmeden ÖNCE yakalandı):** oyuncunun ilk gözlemlenen `MaxWalkSpeed`'ini önbelleğe alan mekanizma yalnızca bir bool bayrakla korunuyordu — piyon değişirse (henüz erişilemez bir gelecek özellik: respawn/OpenLevel) YENİ piyonun kendi hızı hiç yakalanmaz, ESKİ piyonun değeriyle sessizce ezilirdi (bu projenin "yok edilen/değişen aktöre bağlı önbellek bayatlar" hata sınıfının bir başka görünümü); `TWeakObjectPtr<ACharacter>` ile HANGİ piyon için önbelleklendiği de takip edilerek düzeltildi — henüz tetiklenmeyen ama proaktif olarak kapatılan bir hata. Bu, **Faz 2'nin "Hava Simülasyonu" bloğunun (Sistem #11-14) son sistemi** — her biri bir öncekinin çıktısını (Weather→Temperature/Wind/Snow, Temperature→Snow) tüketiyor, gerçek entegrasyon var; ancak TEK bir canlı PIE oturumunda gerçek bir kar fırtınasının hipotermi+kar birikimi+hareket cezasını EŞ ZAMANLI göstermesi HENÜZ İZLENMEDİ (Blizzard düşük ağırlıklı, doğal oluşması uzun sürer) — "İlk kar fırtınası" kilometre taşı kod-yoluyla doğrulandı, canlı-gözlemle değil (bkz. Faz 2 durum notu) |
| 15 | NPC Yapay Zeka | ✅ | `NPC/NPCState.h`, `NPCMath.h`, `NPCTaskData.h`, `NPCBrainSettings.h`, `NPCBrain.h`, `NPCManager.h`, `NPCCharacter.h` | Durum makinesi (6 durum, sabit öncelik: Hurt>Sleeping>Eating>Working/Walking>Idle), görev yürütme (tek-görev, minimal), moral/yorgunluk, yol bulma (motor NavMesh) — derleme temiz + 42/42 test PASSED (3 yeni NPCMath grubu, regresyonsuz) + **PIE canlı doğrulama: görev atanınca Idle→Walking geçti (MCP property okuma); can 100→10 düşürülünce (görev HÂLÂ aktifken) öncelik doğru şekilde Walking'i ezip Hurt'a geçti; can geri 100'e çıkarılınca görevi hatırlayıp otomatik Walking'e DÖNDÜ — öncelik sırası ve durum-hatırlama canlı kanıtlandı.** İlk oyuncu-can bileşenini (Sistem #12) NPC için de YENİDEN KULLANDI — NPC'ye özel ikinci bir can alanı YAZILMADI. İnceleme (2 mercek) canlıya geçmeden ÖNCE **iki gerçek runtime-bug** buldu (ikisi aynı kök nedenin iki görünümü), ikisi de düzeltildi: (1) Zaten Walking durumundayken görev yeniden atanırsa (`AssignTask`) `EvaluateState` bunu Walking→Walking "değişiklik yok" sayıp `MoveToLocation`'ı asla yeniden tetiklemiyordu — NPC eski, artık geçersiz hedefe gitmeye sessizce devam ederdi; `AssignTask` artık zaten-Walking durumundaysa `MoveToLocation`'ı ELLE yeniden tetikliyor. (2) Walking'den (varmadan) başka bir duruma çıkışta (görev iptali/yaralanma/uyku) `AAIController::StopMovement()` HİÇBİR ZAMAN çağrılmıyordu — piyon mantıksal durumu değişmiş olsa bile eski hedefe fiziksel olarak kaymaya devam ederdi; yeni `OnStateExited` bunu kapattı. 1 küçük bulgu (`BeginPlay`/`EndPlay`'in `AProductionMachine`/`UHealthComponent`'in aksine `public` olması — dışarıdan yanlışlıkla çağrılıp kaydı bozabilirdi) `protected`'e taşınarak düzeltildi; 1 küçük bulgu (kare-bölümlemeli seyrek güncellemede bir durum geçişiyle çakışan yorgunluk/moral payının ESKİ değil YENİ duruma atfedilmesi — `AProductionMachine::Tick_Production`'ın döngü-bölme çözümünün aksine) karmaşıklık/etki dengesi gerekçesiyle kabul edilen bir basitleştirme olarak dokümante edildi, düzeltilmedi. **PIE'de fiziksel yürüme hareketinin kendisi bu test oturumunda DOĞRULANAMADI** — `AAIController` doğru possess etti ve `MoveToLocation` doğru hedefle GERÇEKTEN çağrıldı (nav log'da `InitPathfinding` denemesi görüldü) ama test haritasındaki `NavMeshBoundsVolume` için nav mesh hiç BAKE edilemedi (`UNavigationSystemV1` build kilidi tüm PIE oturumu boyunca takılı kaldı — MCP-güdümlü PIE'ye özgü bir çevresel kısıt olduğu değerlendirildi, kod hatası değil; skill'e kalıcı not düşüldü). MIMARI'nin TAM polymorphic `UNPCTask:UObject` hiyerarşisi + `UTaskScheduler` (Sistem #16, ayrı sistem, henüz YAZILMADI) YERİNE minimal `FNPCTaskData` struct'ı kullanıldı; `Eating` durumu MIMARI ile tutmak için enum'da duruyor ama açlık sistemi olmadığından hiç tetiklenmiyor; `UNPCManager` Sistem #9'un kare-bölümleme desenini (`SurvivalProduction::ComputeBatchSize` DOĞRUDAN yeniden kullanılarak) birebir tekrarlıyor |
| 16 | Görev Planlayıcı | ✅ | `NPC/TaskDefinition.h`, `TaskSchedulerMath.h`, `TaskSchedulerSettings.h`, `TaskScheduler.h` | Görev kuyruğu + önceliklendirme + beceri eşleşmesi (tek saf fonksiyonda birleşik), tamamlama takibi, başarısızlıkta backoff'lu yeniden kuyruk — derleme temiz + 44/44 test PASSED (2 yeni TaskSchedulerMath grubu, regresyonsuz) + **PIE canlı doğrulama (operatör elle test etti): `task_enqueue` → "Gorev kuyruga eklendi" → "Gorev atandi" (boş NPC'ye otomatik) → "Gorev tamamlandi" (NPC hedefe GERÇEKTEN yürüdü, Working'de 5sn kalıp otomatik tamamladı) → `npc_dump`: durum=Idle, gorev=hayir, yorgunluk=10.2 (5.1sn çalışmayla matematiksel olarak tutarlı) — kuyruk→atama→yürüme→çalışma→tamamlama→yeniden-boşalma zincirinin TAMAMI tek bir canlı akışta kanıtlandı.** Bu ayrıca Sistem #15'te "navmesh bake edilemedi, fiziksel yürüme doğrulanamadı" diye not düşülen sorunun bu oturumda YAŞANMADIĞINI gösterdi — NPC gerçekten oyuncunun konumuna yürüdü (harita önceki oturumdan zaten-bake-edilmiş nav verisiyle kaydedilmişti, MCP-güdümlü PIE'nin canlı-rebuild kısıtı bake edilmiş VERİYİ etkilemiyor, yalnızca YENİ ekleneni). MIMARI'nin polymorphic görev nesnesi yerine `FTaskDefinition` (Sistem #15'in `FNPCTaskData` kararıyla tutarlı) kullanıldı; MIMARI'nin "heap" imasının YERİNE düz `TArray` + doğrusal tarama (`FindBestEligibleTaskIndex`) — beceri+backoff FİLTRELEMESİ bir heap'le temiz bileşmiyor, beklenen ölçekte doğrusal tarama zaten ucuz (bkz. ADR). Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE **3 gerçek bug** buldu, üçü de düzeltildi: (1) `TaskScheduler.cpp`, `AProductionMachine::GetActorLocation()` çağırırken `Production/ProductionMachine.h`'ı hiç include etmiyordu (eksik tam-tip, Unity-dışı derlemede/Live Coding'de patlardı) — include eklendi; (2) bir görev Walking'de BAŞARISIZ olunca (`FailCurrentTask`) `ClearTask()` `CurrentState`/`AIController`'ı HEMEN senkronize etmiyordu — piyon bir sonraki `AdvanceBrain`'e kadar (kare-bölümlemede bir tam döngü) artık-geçersiz hedefe yürümeye devam ederdi; `ClearTask()` artık kendi içinde `EvaluateState()`'i HEMEN yeniden çağırıp `AIController::StopMovement()`'ı aynı anda tetikliyor (bu ayrıca dışarıdan doğrudan `ClearTask()` çağrılan küçük bir bulguyu da örtük olarak kapattı); (3) `UnregisterBrain` (NPC yok edildiğinde), aktif bir görevi `HandleTaskFailed`'ın AKSİNE backoff'la yeniden kuyruğa almadan SESSİZCE düşürüyordu — iş kalıcı olarak kaybolurdu; artık aynı requeue mantığını paylaşıyor. `UNPCBrain` artık İKİ ayrı subsystem'e kayıtlı (`UNPCManager`: kare-bölümlemeli tikleme, `UTaskScheduler`: görev tamamlama/başarısızlık delegate aboneliği) — iki farklı sorumluluk, iki ayrı kayıt (Tek Sorumluluk ilkesi) |
| 17 | Kaydet/Yükle | ✅ | `Save/SaveDataTypes.h`, `SaveDataSerializer.h`, `SaveGameManagerSettings.h`, `SaveGameManager.h` | Sıkıştırma (zlib), sürüm-geçiş noktası, bozuk-kayıt koruması, birden fazla yuva, otomatik kaydetme — derleme temiz + 46/46 test PASSED (2 yeni SaveDataSerializer grubu: round-trip sadakati + bozuk-veri koruması, regresyonsuz) + **PIE canlı doğrulama: konum (321,654)'e kaydedildi → (-999,-888)'e değiştirildi → `load_game` → GERÇEKTEN (321,654)'e geri döndü; can da kaydedilen değerle tutarlı kaldı — tam disk-round-trip (sıkıştır→yaz→oku→aç→uygula) kanıtlandı.** **En ciddi bulgu, küçük-mod inceleme SIRASINDA (canlıya geçmeden ÖNCE, motor kaynağından doğrulanarak) yakalandı:** `FArchiveLoadCompressedProxy`'nin bozuk/rastgele baytlarda `GetError()` ile "hata" DÖNMEDİĞİ, bunun yerine `FArchive::SerializeCompressedNew`'in başlık-etiketi eşleşmezse `UE_LOGF(...,Fatal,...)` ile DOĞRUDAN ÇÖKTÜĞÜ (`LowLevelFatalError`) — yazdığım "bozuk veriye karşı korumalı, çökmez" sözleşmesinin TAM TERSİYDİ; hatta kendi bozuk-veri testim (`Garbage.Init(0xFF,64)`) bu haliyle otomasyon sürecini ÇÖKERTİRDİ. Düzeltme: motorun sıkıştırma-çözme koduna hiç ulaşmadan ÖNCE kendi zarfımı (imza+CRC32+uzunluk) doğrulayan bir katman eklendi — CRC32 eşleşmesi baytların KENDİ ürettiğim baytlarla AYNI olduğunu garanti ettiğinden, bu noktadan sonra motor çağrısı güvenli. 2 diğer gerçek bug daha bulunup düzeltildi: `TotalPlayTimeSeconds` kaydediliyordu ama yüklemede canlı sisteme HİÇ geri yazılmıyordu (bir sonraki otomatik-kaydetme eski değeri sessizce kaybederdi); `InventoryComponent::RestoreSlots` yalnızca DOLU slotlar için `OnInventoryChanged` yayınlıyordu — bir önceki oturumda dolu, kaydedilen durumda boş bir slot için UI hiç haberdar edilmezdi. **SaveVersion 2 genişletmesi (2026-08-03)**: İnşaat/Üretim/Lojistik/Hava/Kar/Kaynak/Hasat/NPC/Görev/Ses durumu artık kaydediliyor — `FGameSaveData`'ya 8 yeni USTRUCT + `TMap<ESoundCategory,float>` eklendi; her alt sisteme `RestoreStateForLoad()` metodu eklendi; `BuildSaveDataFromLiveSystems`/`ApplySaveDataToLiveSystems` genişletildi; binalar yıkılıp `BuildingDatabase`'den yeniden spawn ediliyor; hasat düğümleri ve NPC'ler `GetFName()` ile eşleştiriliyor; lojistik transit eşyalar kayıt öncesi tamamlanıyor (`CompleteAllTransportsForSave`); `MigrateSaveData` v1→v2 geçişi ekleme-tabanlı (eski kayıtlar yeni alanlar için boş/varsayılan değerlerle açılır). V2 round-trip testi eklendi. **Full UBT build başarılı** (34sn, 4 action — Live Coding patch'i değil, disk-üstü DLL'ler güncellendi). **3/3 Save otomasyon testi PASSED** (`SikistirmaRoundTrip`, `V2RoundTrip`, `BozukVeriKorumasi` — V2 testi Buildings/HarvestNodes/NPCs/PendingTasks/AudioVolumes round-trip sadakatini doğruladı). **PIE canlı doğrulama**: `save_game` başarılı (2225 bayt sıkıştırılmış), `load_game` başarılı (UE'nin "kullanım dışı komut" dialog'u çıktı ama kayıt gerçekten yüklendi). Async serileştirme büyüyen payload için ERTELENDİ; "bulut senkronizasyonu" Sistem #28 Steamworks'ü bekliyor |
| 18 | World Partition | ✅ | `World/WorldPartitionMath.h`, `SurvivalWorldPartitionSettings.h`, `WorldPartitionHelper.h` | Hücrelere bölme, histerezisli uzaklık-tabanlı yükle/boşalt, hücre durumu korunur — derleme temiz + 48/48 test PASSED (2 yeni WorldPartitionMath grubu, regresyonsuz) + **PIE canlı doğrulama: oyuncu UnloadRadius dışına taşınınca "hücre (0,0): boşaltıldı" logu + `WindTurbine_0.bHidden=true`; oyuncu geri dönünce "hücre (0,0): yüklendi" logu + `bHidden=false` — tam round-trip kanıtlandı.** Motorun GERÇEK native World Partition özelliği (harita-dönüştürme, HLOD) YERİNE MIMARI'nin taslağına sadık hafif bir özel sistem (ölçek/risk dengesizliği: native dönüşüm bu projenin düz haritasını kökten değiştirirdi, bkz. ADR). **İki dosya adı/sınıf adı çakışması ampirik olarak keşfedildi:** UE 5.8'in kendi `Engine/Public/WorldPartition/WorldPartitionSettings.h`'ı VE `UWorldPartitionSettings` sınıfı ZATEN var — dosya `SurvivalWorldPartitionSettings.h`, sınıf `USurvivalWorldPartitionSettings` olarak yeniden adlandırılarak çözüldü (UHT hem dosya hem sınıf adlarının motor+proje genelinde benzersiz olmasını istiyor). **En ciddi inceleme bulgusu:** gizle/collision-kapat YALNIZCA hücre-seviyesi GEÇİŞTE uygulanıyordu — zaten-boşaltılmış bir hücreye yeni bir aktör eklenirse (inşa edildi/doğdu) VEYA bir aktör başka bir hücreye taşınırsa hiçbir geçiş tetiklenmediği için o aktörün görünürlüğü asla senkronize olmazdı; düzeltme: her tarama artık kendi hücresinin GÜNCEL durumunu HER aktöre koşulsuz uygular. `UProductionManager`/`UNPCManager`'a (Sistem #9/#15) küçük, additive bir entegrasyon eklendi: boşaltılmış hücrelerdeki makine/NPC'ler ilerletilmiyor — YENİ bir "duraklat" semantiği icat edilmedi, her nesnenin zaten sahip olduğu "gerçek geçen süre kaybolmaz" garantisi hücre yeniden yüklenince otomatik telafi sağlıyor. DÜRÜST kapsam: "boşaltma" gerçek spawn/destroy DEĞİL (yalnızca gizle+collision-kapat+ilerletme-atla) — bellek ayrımı aynı kalır, yalnızca render/collision/simülasyon maliyeti azalır; gerçek bellek-azaltan akış per-sistem spawn/destroy uç noktalarını gerektirir (Sistem #17 artık per-sistem durumu kaydediyor ama World Partition'ın runtime spawn/destroy'u ayrı bir entegrasyon) |
| 19 | Ses Yöneticisi | ✅ | `Audio/SoundCategory.h`, `AudioMath.h`, `SoundInstance.h`, `AudioManagerSettings.h`, `AudioManager.h`, `MusicManager.h` | Kategori mikseri (Master×Kategori çarpımsal), 3D ses mesafe budaması, crossfade müzik — derleme temiz + 50/50 test PASSED (2 yeni AudioMath grubu, regresyonsuz) + **PIE canlı doğrulama (operatör elle test etti): `audio_dump` varsayılanları BİREBİR gösterdi (master=1.00 music=0.70 ambient=0.80 sfx=1.00 ui=1.00 voice=1.00); `audio_set_volume Music 0.3`→`audio_dump` yalnızca Music'i değiştirdi (0.70→0.30, diğerleri sabit); `audio_test_play`/`music_test_play` (Sound/Music==null) crash etmeden tamamlandı; `music_dump`→"(yok)".** Projede HİÇ ses varlığı (`USoundWave`/`USoundCue`) olmadığından (`Content/` tamamen sessiz) tasarım baştan motorun gerçek API'lerine (`PlaySoundAtLocation`/`PlaySound2D`/`CreateSound2D`+`FadeIn`/`FadeOut`) ince bir sarmalayıcı olarak kuruldu; `Sound==nullptr` güvenli-işlenen, test edilebilir bir yol olarak baştan tasarlandı. Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE **4 doğrulanmış bulgu** verdi, dördü de düzeltildi: (1) **majör** — `SpawnSound2D`/`CreateSound2D` varsayılan `bAutoDestroy=true` ile çağrılıyordu; motor kaynağından doğrulandı (`AudioComponent.cpp::PlaybackCompleted`) ki döngüsüz bir parça bitince motor bileşeni KENDİLİĞİNDEN yok eder, `UMusicManager`'a hiçbir bildirim gelmez (`OnAudioFinished` hiç bağlı değildi) — `CurrentMusicComponent` sessizce çöp bir işaretçiye dönerdi ve bir sonraki çağrı bunu bare non-null kontrolüyle (bu projenin `IsValid()` kuralına aykırı) kullanmaya çalışırdı; düzeltme: `bAutoDestroy=false` (yaşam döngüsü tamamen elle yönetiliyor) + tüm kontrol noktaları `IsValid()`'e çevrildi. (2) **majör** — `SetCategoryVolume` yalnızca haritayı güncelliyordu, ÇALMAKTA olan müziğe hiç yayılmıyordu (bir sonraki Play/Crossfade'e kadar eski sesle çalmaya devam ederdi) — CLAUDE.md'nin "sistemler arası iletişim = delegate broadcast" kuralına uygun yeni `FOnCategoryVolumeChanged` (dynamic multicast) eklenip `UMusicManager` abone edildi, artık Music/Master değişince çalan parçanın sesi ANINDA `SetVolumeMultiplier` ile güncelleniyor. (3) **minör** — `Spawn`/`CreateSound2D` başarısız olup null dönerse (`CurrentMusicComponent`=null) `CurrentMusic` yine de yeni parçaya ayarlanıyordu — `GetCurrentMusic()` çalmayan bir parçayı "çalıyor" gösterirdi; düzeltme: `CurrentMusic` yalnızca bileşen GERÇEKTEN oluşunca ayarlanıyor. (4) **minör** — test paketi `MaxAudibleDistance<=0` sözleşmesinin yalnızca `==0` dalını kapsıyordu, negatif değer hiç sınanmamıştı; test eklendi. `Sound` alanı MIMARI'nin `USoundWave*`'i YERİNE bilerek `USoundBase*` (hem `PlaySoundAtLocation` hem `PlaySound2D` zaten bunu bekliyor, hem `USoundWave` hem `USoundCue` bundan türüyor). DÜRÜST kapsam: "ortam döngüleri (rüzgar/yağmur/makine)" hiç YAZILMADI (gerçek döngü sesi içeriği gerektirir, `BuildingBase`'in boş mesh alanıyla aynı dürüst-atlama gerekçesi); "max 128 eşzamanlı" için ayrı bir sayaç/limit YAZILMADI (motorun kendi `USoundConcurrency` sistemi zaten bunu üstlenebilir, içerik olmadan test edilemez bir sınır eklemek anlamsız) |
| 20 | UI Mimarisi | ✅ | `UI/UIMath.h`, `ScreenBase.h`, `ScreenManager.h`, `HUDController.h`, `InventoryWidget.h` | Modal ekran yığını + giriş yönlendirmesi (UI vs oyun), kalıcı HUD widget yönetimi, envanter veri-bağlama — derleme temiz + 51/51 test PASSED (1 yeni UIMath grubu, regresyonsuz) + **PIE canlı doğrulama (operatör elle test etti): `ui_push_screen`→derinlik 0→1, `fare-imleci` gizliden görünüre geçti; `ui_pop_screen`→derinlik 1→0, imleç geri gizlendi; `hud_show_widget`/`hud_hide_widget`→aktif-widget 0→1→0 — tam round-trip, hiç hata yok.** Proje HENÜZ hiçbir Widget Blueprint (WBP_*) içeriğine sahip değil (`Content/` UI'siz) — tasarım baştan `UScreenBase`'i (MIMARI'nin taslağı) BİLEREK soyut yapmadı: içerik olmadan bile PIE'de gerçek, örneklenebilir bir widget olarak test edilebiliyor (`ABuildingBase`'in içeriksiz test edilebilirliğiyle aynı gerekçe). Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE **6 doğrulanmış bulgu** verdi (biri kritik), altısı da düzeltildi: (1) **kritik** — `ScreenStack`/`ActiveWidgets` yalnızca `IsValid()` ile takip ediliyordu; bu, bir ekranın `PopScreen`/`HideWidget` DIŞINDA (ör. kendi kapat düğmesinden doğrudan `RemoveFromParent`) kapanmasını HİÇ yakalayamaz (widget hâlâ geçerli bir UObject, yalnızca Slate'ten kopuk) — oyuncu görünmeyen bir menüde `FInputModeUIOnly`'ye kilitli kalırdı. Motor kaynağından doğrulandı: `RemoveFromParent` her `UUserWidget`'ın sahip olduğu `OnNativeDestruct` olayını SENKRON tetikliyor — her ikisi de (`ScreenManager`, `HUDController`) artık widget oluşturulunca buna abone olup etkilenen kaydı ANINDA temizliyor (motorun `GameViewportSubsystem`'ine bağımlılık GEREKMEDİ). (2) **majör** — `UInventoryWidget` widget kapanınca `OnInventoryChanged` aboneliğini HİÇ bırakmıyordu (yalnızca `SetBoundInventory` tekrar çağrılırsa temizleniyordu) — `NativeDestruct` override'ıyla düzeltildi. (3) **majör** — `BoundInventory` güçlü `TObjectPtr` idi (`AContainerActor` gibi widget'tan bağımsız yaşam döngülü bir aktöre ait olabilir) — `ULogisticsNetwork`'ün `Source`/`Destination`'ıyla AYNI `TWeakObjectPtr` desenine çevrildi. (4-5) minör: `HUDController::GetOwningPlayerController`'ın Possess-öncesi sessizce nullptr dönmesi (zaten bir Warning logluyor, tetikleyen bir çağrı yok — dokümante edildi, değiştirilmedi); `SetWidgetToFocus` eksikliği eklenirken PIE canlı testinde YENİ bir hata ortaya çıktı ("Attempting to focus Non-Focusable widget" — içerik-boş `UScreenBase`'in odaklanacak alt widget'ı yok) — `SWidget::SupportsKeyboardFocus()` kontrolüyle düzeltilip PIE'de hatasız olduğu YENİDEN doğrulandı. DÜRÜST kapsam: "ortam döngüleri" (Sistem #19 emsaliyle aynı) ve "batch güncelleme/virtual scroll" (gerçek içerik/genişlik olmadan test edilemez bir mekanizma icat etmek yerine bilerek ERTELENDİ) yazılmadı; `UInventoryGridPanel` (MIMARI'nin kendi uydurma taslağı, motorda böyle bir sınıf yok) hiç yazılmadı — gerçek ızgara/ikon düzeni Widget Blueprint içerik fazına bırakıldı. **Faz 2'nin (Simülasyon Derinliği) 10 sisteminin TAMAMI bitti.**

**Faz 2 çıkış kriteri:** Kar fırtınasında NPC işçiler içeri kaçıyor, ısıtma yakıt tüketiyor, üretim yavaşlıyor; kaydet/yükle her şeyi geri getiriyor.

> **Durum notu (2026-07-24):** "Hava Simülasyonu" bloğunun (Sistem #11-14) TÜMÜ ✅ ve — Faz 1'in aksine — gerçek bir entegrasyon zinciri var: Sıcaklık, Rüzgar ve Kar'ın ÜÇÜ de doğrudan Hava Durumu'nun çıktısını (Condition/Temperature/WindSpeed/Precipitation) tüketiyor, Kar ayrıca Sıcaklık'ın ortam hesabını yeniden kullanıyor — hiçbiri paralel/kopya bir "hava" kavramı icat etmedi. Kod-yolu analizi doğruluyor ki bir Blizzard (Condition=Blizzard, Temp=-10°C, Wind=50, Precip=0.8) OTOMATİK olarak üç etkiyi eş zamanlı tetikler: gerçek hipotermi hasarı (Sıcaklık), sınırsız kar birikimi → hareket cezası + inşaat engeli + çığ riski (Kar), ve türbinlerin tam kapasite üretimi (Rüzgar) — hiçbir ek kod/manuel bağlama GEREKMEDEN. **Güncelleme (2026-07-25): "İlk kar fırtınası" kilometre taşı KANITLANDI.** `weather_force 7` ile iki turda test edildi: `temperature_dump` hipotermiyi gerçek ve ölümcül gösterdi (can 67.8→0.0, `HealthComponent::TakeDamage` gerçekten çağrıldı), `wind_dump` yüksek rüzgar gösterdi (küresel-hız 41-50), `snow_dump` sıfırdan büyüyen pozitif kar derinliği gösterdi (0.8-1.0 — kısa zorlanmış-test penceresi yüzünden mütevazı ama gerçek, entegrasyonun kendisi çalışıyor). Üçü AYNI `UWeatherSimulation::GetCurrentState()` çağrısından besleniyor. İlginç bir davranış notu (hata değil): `weather_force`'un `TimeSinceLastEvaluation=0` sıfırlaması arka arkaya İKİNCİ bir rastgele geçişi engelliyor ama 4 komutu elle yazıp okumak 30sn'lik doğal pencereyi doldurunca bir sonraki doğal reroll yine de devreye girdi (`ikinci turda 7→1'e geçiş başladı` log'u) — tasarım gereği, düzeltme gerekmedi. **Yan etki, sonraki çalışmaya devrediliyor**: oyuncu bu testte can=0 ile öldü; kod tabanında `OnDeath`'e SIFIR abonelik olduğu doğrulandı (respawn/ölüm-ekranı hiç yazılmamış, dürüst kapsam dışı bırakılmış bir alan) — `Heal()` de ölü aktöre işlem yapmayı reddediyor, tek geri dönüş yolu `load_game`→`SetCurrentHealthForLoad` (Sistem #17'nin gerçek kullanımı, zaten "Tam döngü" kilometre taşının bir parçası).

## Faz 3 — Üretim Kalitesi (Hafta 9–12)

| # | Sistem | Durum | Not |
|---|--------|-------|-----|
| 21 | Multiplayer temeli | ⬜ | Release sonrasına ertelenebilir; replikasyon-uyumlu tasarım şart |
| 22 | Dev araçları | ✅ | `DevTools/ProfilingCommands.cpp` — `profile_spawn_stress [Bina] [Makine]` (kare-bölümlenmiş, `FMath::Clamp` korumalı), PIE'de 100 bina+50 makine ile 59.93 FPS/16.69ms kanıtlandı. `DevTools/LogisticsVisualizer.cpp` — `logistics_visualize [0\|1]` (düğümler renk-kodlu küre+etiket, aktif taşımalar çizgi+lerp'lenmiş kargo küresi, `ULogisticsNetwork::DrawDebugVisualization` — Sistem #8'in kendi dosyasında, DevTools yalnızca konsol-komut yüzeyini topluyor), PIE'de renkli küreler+doğru etiketler+temiz log kanıtlandı. `weather_set`/`time_scale` AYRI YAZILMADI — Sistem #1 (`time_sethour`/`time_settimescale`) ve Sistem #11 (`weather_force`) zaten karşılıyor, aynı işi tekrar etmek "paralel/kopya sistem" yasağına girerdi *(2026-07-25)*. **`give_item <ItemID> [Adet]` eklendi** (`InventoryComponent.cpp`, 2026-08-03) — oyuncunun envanterine dev-cheat ile öğe ekler (`AddItem` dönüş değerini loglar), Sistem #29 sonrası 13 yeni ham maddenin (henüz hepsi hasat düğümüne bağlı değil) test edilebilmesi için; PIE'de canlı doğrulandı: `give_item DemirCevheri 5` → ağırlık 14.5→29.5→44.5 (iki çağrı, her biri tam +15, `AddItem` matematiği birebir tutarlı) |
| 23 | Hata işleme & log | ✅ | MIMARI'nin `FGameLogger`/`FErrorHandler`/`FStructuredLogger`/`FCrashReporter::SendToDeveloper` taslağı BİLİNÇLİ olarak yazılmadı (sırasıyla: `UE_LOG`+`LogSurvival*` kategorileriyle çakışırdı; olmayan Sistem #21 Multiplayer'a hizmet ederdi; tüketicisiz JSON altyapısı olurdu; gerçek bir backend gerektirir). `HandleSaveCorruption` zaten Sistem #17'de var (CRC32+imza doğrulaması). UE'nin kendi `CrashReportClient`'i motor varsayılanı olarak aktif (devre dışı bırakan bir override yok, doğrulandı). Onun yerine: whole-codebase çökme-güvenliği denetimi (Workflow, 5 alt-sistem grubu, 80 araç çağrısı) — SIFIR gerçek bulgu, mevcut per-sistem adversarial-review sürecinin işe yaradığını doğruladı *(2026-07-25)* |
| 24 | Content pipeline | ✅ | `DevTools/ContentValidator.cpp` — `content_validate` (yalnız editor) konsol komutu, `/Game` altındaki tüm varlıkları CLAUDE.md isimlendirme kurallarına (BP_/WBP_/DT_/DA_/SM_/SK_/M_/MI_/T_/A_ + `IA_`/`IMC_`) göre denetler — `GetDerivedClassNames` ile DataAsset soyağacı da kapsanıyor. FBX/PSD import-otomasyonu BİLİNÇLİ yazılmadı (gerçek 3D içerik yok). PIE'de 14/14 varlık doğrulandı (0 ihlal) *(2026-07-25)* |
| 25 | Performans profiling | ✅ | `DevTools/ScopedTimer.h` — `SURVIVAL_SCOPED_TIMER`/`SURVIVAL_SCOPED_TIMER_WARN` makroları (yalnızca Development/Editor — Shipping'de `UE_LOG` derleme-zamanında elenir, dokümante edildi), `WorldPartitionHelper::RebuildCellRegistry`'ye takılıp PIE'de gerçek ölçüm alındı (0.03-0.06ms, darboğaz değil). `Docs/PROFILING.md` — haftalık checklist + regresyon karşılaştırma tablosu *(2026-07-25)* |
| 26 | CI/CD | 🟨 | `.github/workflows/ci.yml` sağlamlaştırıldı (2026-07-26): **inceleme bulgusu (motor kaynağından doğrulandı)** — `-TestExit="Automation Test Queue Empty"` `UnrealEditor-Cmd.exe` doğrudan çağrıldığında hiç parse edilmiyor (yalnızca RunUAT.exe'nin C# katmanının konvansiyonu), kaldırıldı. `Quit` komutunun kendi exit-code'u (`RequestExitWithStatus`, testler başarısızsa gerçek hata koduyla çıkar) GEÇERLİ bulundu ama tek başına yeterli değil (çökme/sıfır-test-eşleşmesi durumlarını kapsamaz) — CLAUDE.md kuralına uyarak ayrı bir "`TestResults\index.json` doğrula" adımı eklendi (succeeded/failed/notRun sayılarını parse eder, `failed>0` VEYA toplam `0` ise job'ı FAIL eder), gerçek bir index.json + simüle edilmiş başarısız/sıfır-test senaryolarıyla mantık doğrulandı. Ayrıca stale `TestResults/` klasörü test öncesi temizleniyor (eski sonuçların yeni çalışmayı maskelemesini önler). **Operatör kararı**: self-hosted runner KURULMADI (bu makineyi kalıcı bir CI çalıştırıcısına çevirmek ayrı, güvenlik açısından önemli bir karar — operatöre soruldu, "sadece workflow dosyasını sağlamlaştır" seçildi) — CI hâlâ yalnızca `workflow_dispatch` (elle tetikleme) ile çalışabilir, push/PR tetikleyicileri kapalı |
| 27 | Oyun dengesi | 🟨 | **Denge geçişi #1 tamamlandı** (2026-07-25): operatör kararları — hipotermi mevcut haliyle kaldı (doğru bulundu), üretim/zanaat yavaş/metodik (`OdunIsleme.productionTime` 3→15sn, `Balta_Tarifi.craftingTime` 2→8sn, PIE'de doğrulandı), ham madde bol (`RequiredMaterials`/ingredients zaten ucuz, DEĞİŞTİRİLMEDİ). `constructionTime` bilinçli değiştirilmedi (kodda hiç tüketilmiyor, Sistem #7'nin önceki kararı). `DA_BalanceConfig` YAZILMADI — MIMARI'nin taslağı, zaten çalışan per-sistem DataTable/Settings deseniyle (DT_Recipes, DT_ProductionRecipes, vb.) çakışan merkezi bir "paralel sistem" olurdu. Haftalık denge döngüsü henüz kurulmadı (tek geçiş yapıldı). **İçerik hacmi genişletmesi tamamlandı** (2026-07-26): `DT_Items` +30 (33 toplam: 13 RawMaterial, 8 Component, 4 Tool, 5 Consumable), `DT_Recipes` +19 zanaat tarifi, `DT_ProductionRecipes` +5 üretim tarifi (Demir/Bakır Eritme, Tuğla Pişirme, Cam Eritme, Kemik Öğütme), `DT_Buildings` +10 (5 `ProductionMachine` + 5 sade bina) — tümü eklendi/`set_rows` ile yazıldı/`save_assets([])` iş-arounduyla diske kaydedildi (dosya mtime ile doğrulandı), referans bütünlüğü (her ItemID'nin `DT_Items`'ta var olduğu) elle çapraz kontrol edildi. **Test kapsamı o an dürüstçe sınırlıydı**: dünyada hiçbir ham madde için toplama/hasat mekanizması bulunmadığından (yeni bulgu, bkz. YOL_HARITASI ADR 2026-07-26 — mevcut Odun/Taş bile toplanamıyordu) uçtan uca "topla→zanaat et→üret" PIE testi yapılamamıştı; doğrulama DataTable-round-trip (`get_rows`) + referans bütünlüğü ile sınırlı kalmıştı. **Güncelleme (2026-07-26, Sistem #29 sonrası)**: bu temel boşluk artık kapandı — `Agac`→Odun ve `Kaya`→Tas için gerçek hasat düğümleri var ve orijinal Odun/Taş ekonomisi uçtan uca ("topla→zanaat et→üret") PIE'de canlı doğrulandı (bkz. Sistem #29 satırı). İçerik-hacmi genişletmesinin getirdiği 13 yeni ham madde (DemirCevheri, Kil, Kum, Lif, vb.) için ise HENÜZ kendi hasat düğümü yok — bunların doğrulaması hâlâ yalnızca DataTable-seviyesinde (referans bütünlüğü), gerçek oynanışta toplanamıyorlar |
| 28 | Steam deployment | ⬜ | Steamworks SDK, başarımlar, cloud save, yayın checklist'i |
| 29 | Hasat Düğümleri (Harvest Nodes) | ✅ | `AHarvestNode`(`AInteractableBase`'in ilk somut alt sınıfı) + `UHarvestNodeDatabase`(`UGameInstanceSubsystem`) + `UHarvestNodeManager`(`UWorldSubsystem`+`FTickableGameObject`, kare-bölümlemeli, yalnızca tükenmiş düğümleri işler) + `HarvestMath.h`(`SurvivalHarvest` namespace) + `DT_HarvestNodes` (v1: `Agac`→Odun 2-4 adet/3 hasat/60sn yeniden-doğma, `Kaya`→Tas 1-3 adet/3 hasat/90sn). 4 ajanlı tasarım paneliyle tasarlandı (2026-07-26), sonra kodlandı — derleme temiz + 54/54 test PASSED (53 önceki + 1 yeni `DeltaTimeKelepcesi`). **2-mercek adversarial inceleme 4 gerçek bulgu verdi, dördü de düzeltildi**: (1) majör — `AddItem`'in KISMİ kabulünde (0<Eklenen<İstenen) düğüm tükenmezken oyuncu gerçek/kalıcı eşya kazanıyordu (sınırsız-çiftlik riski) → `HasRoomFor` ön-kontrolü eklendi (ProductionMachine deseni); (2) majör — geçersiz `NodeID`'de düğüm görünür+çarpışmalı ama etkileşilemez kalıp Manager'a hiç kaydolmadığından kalıcı bozuk kalıyordu → `BeginPlay`'de bu dalda da gizle+collision-kapat eklendi; (3) minör — `YieldCountMin=0` durumunda düğüm hiçbir şey vermeden normal tükeniyordu → `NormalizeYieldRange` artık Min'i 1'e kelepçeliyor; (4) minör — MIMARI.md'nin vaat ettiği `DT_HarvestNodes` referans-bütünlüğü kontrolü `content_validate`'e hiç eklenmemişti → eklendi (`DT_HarvestNodes.YieldItemID`'nin `DT_Items`'ta var olduğunu doğruluyor). **PIE'de CANLI uçtan-uca doğrulandı** (operatör E ile topladı, MCP ile çapraz kontrol edildi): Agac 3→2→1→0 sarj (her hasatta doğru miktar Odun envantere eklendi, `AddItem` dönüş değeri kontrolü doğrulandı), tükenince gizlendi+çarpışması kapandı (`trace_world` ile teyit), **tam 60 saniye sonra** (`RespawnSeconds=60` ile birebir eşleşerek) `UHarvestNodeManager` onu otomatik yeniden doğurdu ("yeniden dogdu (sarj: 3)") — kare-bölümlemeli yeniden-doğma zamanlayıcısının canlı, doğru çalıştığının kanıtı. `content_validate`'in yeni referans-bütünlüğü kontrolü de PIE'de "2 satır tarandı, 0 ihlal" ile doğrulandı. **Yan bulgu (ayrı, önemli hata)**: PIE testi sırasında `UTimeKeeper::Tick`'in `DeltaTime`'ı hiç kelepçelemediği keşfedildi — editörün MCP çağrılarına yanıt verirken bloke olması bir sonraki karede dev bir `DeltaTime` üretip oyun saatini tek karede saatlerce ileri sıçratıyordu (x10 ölçekte ~97dk gerçek-zaman donması → ~16 saatlik oyun-içi sıçrama, canlı gözlemlendi — siyah ekran/gece yarısı sanılan ilk bulgu buydu). `SurvivalTime::ClampFrameDeltaTime` (yeni, `TimeMath.h`) eklenip `TimeKeeper::Tick`'te kullanıldı, birim testiyle doğrulandı. Test haritasına (`TestPlayer.umap`) `HarvestNode_Agac`/`HarvestNode_Kaya` (Engine Cube mesh, PlayerStart'a ~500-700 UU) kalıcı olarak yerleştirildi, `DT_HarvestNodes` + harita diske kaydedildi. **İçerik genişletmesi (2026-08-03)**: `DT_HarvestNodes`'a 9 yeni düğüm eklendi (DemirDamari→DemirCevheri, BakirDamari→BakirCevheri, KomurDamari→Komur, KilYatagi→Kil, KumYigini→Kum, TuzYatagi→TuzKristali, LifBitkisi→Lif, MeyveAgaci→Meyve, BalikNoktasi→Balik) — içerik-hacmi genişletmesinin 13 yeni ham maddesinden 9'u artık dünyada toplanabilir; kalan 4'ü (Deri/Et/Kemik/Yag) BİLİNÇLİ dışarıda bırakıldı (hayvan ürünü, gerçek avlanma ayrı bir yaratık-AI sistemi gerektirir, v1 statik-düğüm modeline uymaz). 9 aktör test haritasına yerleştirildi (MCP `ProgrammaticToolset` ile toplu, Engine Cube mesh). **PIE'de canlı doğrulandı**: `HarvestNodeDatabase hazir: 11 dugum tanimi yuklendi` + DemirDamari uçtan uca test edildi (E ile 3 hasat: 2+1+2 DemirCevheri, sarj 3→0 tükendi) — 9'u da aynı `AHarvestNode` kodunu kullandığından DemirDamari örneği hepsini doğrular |

---

## HAFTALIK GÜNLÜK

> Her çalışma seansı sonunda buraya bir satır ekle. Format: tarih — yapılan — sonraki adım.

| Tarih | Yapılan | Sonraki Adım |
|-------|---------|--------------|
| 2026-08-03 | **Kaydet/Yükle genişletmesi (SaveVersion 1→2)**: İnşaat/Üretim/Lojistik/Hava/Kar/Kaynak/Hasat/NPC/Görev/Ses durumu artık kaydediliyor. 8 yeni USTRUCT (`FBuildingSaveData`, `FWeatherSaveData`, `FResourceSaveData`, `FHarvestNodeSaveData`, `FNPCSaveData`, `FTaskSaveData`) + `FGameSaveData` genişletildi; 7 alt sisteme `RestoreStateForLoad()` eklendi; `BuildSaveDataFromLiveSystems`/`ApplySaveDataToLiveSystems` tüm sistemleri kapsayacak şekilde genişletildi; `LogisticsNetwork::CompleteAllTransportsForSave()` eklendi; `MigrateSaveData` v1→v2 geçişi (ekleme-tabanlı); V2 round-trip testi eklendi. Editör açık olduğundan (Live Coding) komut-satırı derlemesi yapılamadı — kod inceleme ajanı TÜM imzaları/include'ları doğruladı, derleme hatası bulamadı. İkinci geçiş incelemesinde bir **runtime-bug** bulunup düzeltildi: `NPCBrain::RestoreStateForLoad` durumu `Walking` olarak ayarlıyordu ama `MoveToLocation` çağırmıyordu — durum geçişi olmadığından `OnStateEntered` hiç tetiklenmez, NPC yüklemeden sonra yerinde kalırdı; `AssignTask`'taki aynı kalıpla (satır 76-82) düzeltildi. Daha önce aynı gün: `give_item` dev-cheat + 9 yeni hasat düğümü eklenmişti | Full UBT build başarılı (34sn). 3/3 Save otomasyon testi PASSED (V2RoundTrip dahil). PIE'de `save_game`/`load_game` manuel doğrulandı. `give_item Stone 50` DataTable'da "Stone" ID'si olmadığından başarısız — doğru ID'ler Türkçe (Odun, Tas, DemirCevheri vb.) |
| 2026-08-03 | `give_item <ItemID> [Adet]` dev-cheat komutu (`InventoryComponent.cpp`) + `DT_HarvestNodes`'a 9 yeni düğüm (13 yeni ham maddeden 9'u; Deri/Et/Kemik/Yag bilinçli dışarıda — hayvan ürünü) eklendi. Derleme temiz + 54/54 test. **İkisi de PIE'de canlı doğrulandı**: `give_item` ağırlık matematiği birebir tuttu; DemirDamari düğümü E ile uçtan uca toplandı (2+1+2 DemirCevheri, sarj 3→0 tükendi). Yan-doğrulama: `RevertToLastSave` envanteri de doğru geri yüklüyor (ölümde give_item eşyaları silindi). 9 aktör MCP `ProgrammaticToolset` ile toplu yerleştirildi | Kaydet/Yükle genişletmesi |
| 2026-07-26 | Sistem #29 (Hasat Düğümleri) ✅ TAMAMLANDI — tasarım paneli → kodlama → 2-mercek inceleme (4 bulgu düzeltildi) → derleme+test (54/54) → PIE'de canlı uçtan-uca doğrulama (topla→tüket→gizle/collision-kapat→60sn'de otomatik yeniden-doğma). Yan bulgu: `TimeKeeper::Tick` DeltaTime kelepçesi eksikti (donma sonrası saat sıçraması, canlı gözlemlendi), düzeltildi + test edildi. Faz 1'in "her ham madde toplanabilir olmalı" borcu kapandı | Operatör onayıyla commit/push; sonrasında Faz 3'ün kalan kalemleri (Sistem #21/#26/#28) veya haftalık denge döngüsü |
| 2026-07-26 | İçerik hacmi genişletmesi (YOL_HARITASI "İçerik: 30+ öğe, 20+ tarif, 10+ bina, 5+ makine" kalemi) tamamlandı: `DT_Items` +30, `DT_Recipes` +19, `DT_ProductionRecipes` +5, `DT_ProductionRecipes`'e bağlı `DT_Buildings` +10 (5 `ProductionMachine` + 5 sade bina). Hepsi MCP `DataTableTools` (`add_rows`→`set_rows`→`get_rows` doğrulama) ile eklendi, `AssetTools.save_assets([])` iş-arounduyla (isimli-yol formu "Asset does not exist" hatası veriyor, boş-liste "tümünü kaydet" çalışıyor) diske yazıldı, dosya mtime'larıyla doğrulandı. Referans bütünlüğü (her tarif/bina girdisinin `DT_Items`'ta karşılığı olduğu) elle çapraz kontrol edildi, hepsi geçerli. **Keşfedilen boşluk**: dünyada hiçbir ham madde (yeni ya da eski) için toplama/hasat mekanizması yok, bu yüzden uçtan uca "topla→üret" PIE testi yapılamadı — bkz. YOL_HARITASI ADR 2026-07-26. `content_validate` operatör tarafından PIE'de henüz çalıştırılmadı | Operatör `content_validate`'i çalıştırıp sonucu paylaşabilir; sonrasında commit/push yalnızca açıkça istenirse |
| 2026-07-25 | **Sistem #27: Denge geçişi #1** — operatör 3 karar verdi: (1) hipotermi mevcut haliyle doğru (rahat bölge 10-30°C dışında hedef vücut sıcaklığı kayıyor ama hasar ancak vücut 35°C altına inince başlıyor — sınırın hemen dışı neredeyse zararsız, gerçek tehlike yalnızca ciddi/sürekli aşırı havada; operatöre bu mekanik açıklanınca "bu haliyle" onaylandı, kod değişikliği yok). (2) üretim/inşaat yavaş/metodik — MCP DataTableTools ile gerçek satırlar okunup değiştirildi: `DT_ProductionRecipes.OdunIsleme.productionTime` 3→15sn, `DT_Recipes.Balta_Tarifi.craftingTime` 2→8sn. (3) ham madde bol — `RequiredMaterials`/ingredients (Odun x5 binalar, Odun x3+Taş x2 Balta, Odun x2 OdunIşleme) zaten ucuzdu, DEĞİŞTİRİLMEDİ. **Bulgu**: `constructionTime` alanı DataTable'da var ama `BuildingBase.cpp`'nin kendi yorumunda "TÜKETİLMİYOR" diye önceden belgelenmiş (Sistem #7'nin kararı) — DataTable sayısını artırmak sessizce hiçbir şey değiştirmezdi; operatöre bunu gerçek yapmak için yeni kod mu yazılsın diye soruldu, yanıt gelmeyince en az müdahaleci seçenek (inşaat anlık kalsın) uygulandı. **PIE canlı doğrulama**: `production_set_recipe` sonrası ~5sn'de ilerleme=%33+çıktı boş (eski 3sn olsaydı çoktan birkaç döngü tamamlanmış olurdu), ~15sn sonra `logistics_request Tas 1` başarıyla taşıdı (döngü gerçekten tamamlandı) — üretim süresi değişikliği doğrulandı. Zanaat süresi sahne malzeme kısıtı yüzünden uçtan uca canlı test edilemedi ama disk-okuma doğrulandı, aynı mekanizma (üretimde) çalıştığı kanıtlandı. Kod değişikliği yok (yalnızca DataTable içeriği), build/test gerekmedi | Sistem #21 (Multiplayer, ertelendi)/#26 (CI/CD, runner gerekli)/#28 (Steam, hesap gerekli) veya operatörün seçeceği başka bir öncelik — Sistem #27 tek geçişle 🟨, haftalık denge döngüsü henüz kurulmadı |
| 2026-07-25 | **Sistem #24 (Content pipeline) TAMAMLANDI** — operatör Faz 3'ten seçti. Bu konuşma başlamadan ÖNCE kesintiye uğramış bir oturumdan kalma, hiç review/build/test edilmemiş bir `ContentValidator.cpp` bulundu (temel alındı, üzerine yazılmadı) — `content_validate` konsol komutu, Asset Registry ile `/Game` altındaki tüm varlıkları CLAUDE.md'nin isimlendirme kurallarına göre denetliyor. Küçük-mod inceleme (2 mercek, Workflow) 6 bulgu verdi, 4'ü doğrulandı: **majör** — CLAUDE.md'nin zorunlu `DA_` öneki kural tablosunda yoktu (unutulmuş) — `IAssetRegistry::GetDerivedClassNames` ile `UDataAsset` soyağacındaki TÜM sınıflar toplanıp ayrıca kontrol edildi (DataAsset'in kendisi genelde alt-sınıflanır, tam sınıf-adı eşleşmesi yetmezdi). **majör** — dosyanın WITH_EDITOR sarmalamasını atlama gerekçesi ("AssetRegistry modülü runtime'da da linklenir") DOĞRUYDU ama YANLIŞ soruyu cevaplıyordu: asıl sorun, cooklenmiş bir pakette Blueprint/WidgetBlueprint varlıklarının sınıf-adının (`BlueprintGeneratedClass`/`WidgetBlueprintGeneratedClass`'a) DEĞİŞMESİ — bu, projenin en çok kullandığı iki kuralı (BP_/WBP_) cooklenmiş bir çalıştırmada sessizce geçersiz kılardı; `WITH_EDITOR` eklendi. **minör** — paketli derlemede AssetRegistry serileştirme varsayımına sessiz bağlılık (WITH_EDITOR eklenince zaten devre dışı kaldı, ayrıca düzeltme gerekmedi). **minör** — kullanılmayan `Args` parametresi, zararsız. **PIE'de CANLI bulunan bir bulgu daha** (statik incelemenin kaçırdığı): `InputAction`/`InputMappingContext` motor kaynağından doğrulandığı üzere GERÇEKTEN `UDataAsset`'ten türüyor — DataAsset-soyağacı kontrolü tablodaki daha özel `IA_`/`IMC_` kuralından ÖNCE çalışınca bu iki türü yanlışlıkla `DA_` bekler hale getirdi (`IMC_Default`, 5× `IA_*` sahte-ihlal olarak loglandı). Düzeltme: tam-eşleşme tablosu HER ZAMAN önce, DataAsset-soyağacı kontrolü yalnızca tabloda özel bir kural yoksa devreye giriyor. **Derleme temiz + 51/51 test PASSED** (3 build+test turu). **PIE canlı doğrulama**: `content_validate` → 14 varlık, 13/13 kural-tanımlı uygun, 0 ihlal, 1 varlık (harita, kendi kuralı var) hariç | Sistem #21 (Multiplayer, ertelendi)/#26 (CI/CD, runner gerekli)/#27 (Denge)/#28 (Steam, hesap gerekli) veya operatörün seçeceği başka bir öncelik |
| 2026-07-25 | **Sistem #23 (Hata işleme & log) TAMAMLANDI** — operatör Faz 3'ten seçti. MIMARI'nin taslağı (`FGameLogger`, `FErrorHandler`, `FStructuredLogger`, `FCrashReporter`) BİLİNÇLİ olarak yazılmadı: `FGameLogger`'ın kendi `ELogLevel`/`ELogCategory` enum'ları `UE_LOG`+`LogSurvival*` kategorileriyle (zaten 25 sistem boyunca kullanılan) birebir çakışırdı — paralel/kopya sistem. `FErrorHandler::HandleNetworkDisconnect` hiç var olmayan bir sisteme (Sistem #21 Multiplayer, ertelendi) hizmet ederdi. `FErrorHandler::HandleSaveCorruption` zaten Sistem #17'de var (`SaveGameManager::LoadGame`'in CRC32+imza doğrulaması, motor kaynağından doğrulanmış gerçek bir çökme riskini kapatıyordu) — yeniden sarmalamak değer katmazdı. `FStructuredLogger` (JSON) hiçbir tüketicisi olmayan spekülatif altyapı olurdu. `FCrashReporter::SendToDeveloper` gerçek bir backend gerektirir (yok, Sistem #28 Steamworks'ü bekliyor) — UE'nin kendi `CrashReportClient`'i motor varsayılanı olarak zaten aktif (`DefaultEngine.ini`'de devre dışı bırakan hiçbir override yok, doğrulandı). MIMARI'nin kendi notu bu sistemin "tüm sistemlerin sonunda" çalışmasını öngörmüştü — tam da şimdi (Faz 1+2 bitti) doğru zaman. Onun yerine: **whole-codebase çökme-güvenliği denetimi** (Workflow, 5 alt-sistem grubu paralel — Construction/Production/Logistics/DevTools, Weather/Temperature/Wind/Snow, NPC/TaskScheduler/Save, UI/Audio/WorldPartition/Player/Core, Inventory/Crafting/Interaction/Items/Time — 80 araç çağrısı, null-deref/kontrolsüz `Cast<>`/dizi sınır aşımı/DataTable-bulunamadı yayılımı/`GetWorld()` zinciri kategorilerinde arandı) — **SIFIR gerçek bulgu**, her ikisi de bağımsız doğrulama denemesi yaptı. Bu, kod değişikliği gerektirmedi; mevcut per-sistem 2-lens adversarial-review sürecinin (bu oturum boyunca her sistemde uygulanan) gerçekten işe yaradığını doğruladı | Faz 3'ün geri kalanı (#24 Content pipeline, #21/#26/#28 dış altyapı gerektirdiği için beklemede, #27 vague/süreç işi) veya operatörün seçeceği başka bir öncelik |
| 2026-07-25 | **Sistem #25 (Performans profiling) TAMAMLANDI** — operatör Faz 3'ten seçti. `DevTools/ScopedTimer.h`: `FSurvivalScopedTimer` (RAII, `FPlatformTime::Seconds()` ile ölçer) + `SURVIVAL_SCOPED_TIMER`/`SURVIVAL_SCOPED_TIMER_WARN` makroları — Unreal'in kendi `SCOPE_CYCLE_COUNTER`/Insights altyapısını TEKRARLAMAZ, onu açmadan hızlı/geçici "bu blok kaç ms sürdü" cevabı verir. İlk gerçek kullanım: `WorldPartitionHelper::RebuildCellRegistry` (bu oturumda daha önce "kare-bölümlemesiz periyodik tarama, ölçülmedi" diye dokümante edilmiş açık nokta). Küçük-mod inceleme (2 mercek, Workflow) 4 bulgu verdi, 3'ü doğrulandı: **majör** — sınıfın kendi yorumu "eşik aşılınca Warning HER ZAMAN basar" diyordu ama motor kaynağından doğrulandı ki proje `Target.cs`'lerinde `bUseLoggingInShipping` açık olmadığından (varsayılan) Shipping build'de `UE_LOG` (Warning dahil) derleme zamanında tamamen eleniyor — yorum düzeltildi, araç açıkça "yalnızca Development/Editor" olarak işaretlendi (projedeki TÜM diğer dev-konsol komutlarıyla aynı durum, ayrıca `bUseLoggingInShipping` açmak kapsam dışı, ayrı ve büyük bir proje kararı). **minör** — `FPlatformTime` maliyeti Shipping'de boşuna çalışıyor (kabul edilebilir, mevcut çağrı noktası zaten 1sn'de bir). **minör** — aynı fiziksel satırda iki çağrı `UE_JOIN`/`__LINE__` çakışması (motorun kendi `TRACE_CPUPROFILER_EVENT_SCOPE`'uyla aynı bilinen kısıt) — tek satırlık uyarı yorumu eklendi. `Docs/PROFILING.md` oluşturuldu (araçlar, haftalık checklist, regresyon tablosu). **Derleme temiz + 51/51 test PASSED**. **PIE canlı doğrulama**: `Log LogSurvival Verbose` sonrası `RebuildCellRegistry` gerçekten ölçüldü — **0.03-0.06 ms/çağrı**, 5ms uyarı eşiğinin çok altında, hiç Warning tetiklenmedi — bu sahne ölçeğinde kesinlikle darboğaz değil, açık nokta gerçek veriyle kapandı | Faz 3'ün geri kalanı (#23-24, #21/#26/#28 dış altyapı gerektirdiği için beklemede, #27 vague/süreç işi) veya operatörün seçeceği başka bir öncelik |
| 2026-07-25 | **Sistem #22 (Dev araçları) TAMAMLANDI** — operatör Faz 3'ten (#21-28) bunu seçti (diğerleri gerçek dış hesap/altyapı gerektirdiği için — Steam geliştirici hesabı, self-hosted CI runner — şu an prematür). `logistics_visualize [0\|1]` eklendi: `ULogisticsNetwork::DrawDebugVisualization` (Sistem #8'in kendi dosyasında, düğüm/taşıma verisine private erişim gerektirdiği için), `DevTools/LogisticsVisualizer.cpp`'deki konsol komutuyla açılıp kapanıyor. Küçük-mod inceleme (2 mercek, Workflow) 6 bulgu verdi, 5'i doğrulandı: **kritik/majör** — koddaki yorum `LifeTime=0.0f`'in "tek kare, bir sonraki tick zaten üzerine çizecek" anlamına geldiğini iddia ediyordu ama motor kaynağı (`DrawDebugHelpers.cpp::GetDebugLineLifeTime`) tam tersini gösterdi: `LifeTime<=0` olduğunda çizim `ULineBatchComponent::DefaultLifeTime`'a (SABİT 1.0 saniye) düşüyor, her çağrı ÖNCEKİni silmeden yeni segment ekliyor — 60 FPS'te ~60 çakışan kopya birikiyordu (hareketli kargo küresi bir "iz" gibi görünürdü). Düzeltme: `LifeTime = DeltaTime` (Tick'in kendi parametresi). **majör** — `DrawDebugString`'e `TestBaseActor=nullptr` verilince TÜM etiketler aynı aktöre (`WorldSettings`) bağlanıyor, motorun aktör-başına 128-etiket sınırı (`r.DebugSafeZone.MaxDebugTextStringsPerActor`) aşılınca fazlası SESSİZCE çizilmiyordu — her etikete kendi düğüm/kaynak aktörü verilerek düzeltildi. **minör** — her tick koşulsuz `FString::Printf` tahsisleri, dev-tool için kabul edilebilir, düzeltilmedi. **Ayrıca**: build sırasında TAMAMEN İLGİSİZ, önceden var olan bir isim çakışması ortaya çıktı — `ResourceSimulation.cpp` ve `WindSimulation.cpp`'nin ikisi de anonim ad alanında aynı isimde (`FindPlayerPawn`) ama farklı dönüş tipinde bir yardımcı fonksiyon tanımlamış; Unity Build gruplaması bu turda ikisini aynı derleme birimine düşürünce çakışma patladı (daha önce hiç aynı gruba düşmemişlerdi) — `FindResourcePlayerPawn`/`FindWindPlayerPawn` olarak yeniden adlandırılıp düzeltildi, projenin geri kalanına etkisi yok. **Derleme temiz + 51/51 test PASSED** (2 tur, regresyon yok). **PIE canlı doğrulama**: `logistics_visualize 1` + `logistics_request Odun 3` — ekran görüntüsünde renkli küreler (Sink=kırmızı, Container=sarı, MachineOutput=macenta) + doğru stok etiketleri + temiz log, smear/artık yok. Kozmetik not (düzeltilmedi): birbirine çok yakın düğümlerin (ör. bir makinenin giriş/çıkış proxy'leri + yanındaki konteyner) etiketleri ekranda üst üste binebiliyor | Faz 3'ün geri kalanı (#23-27, #21/#26/#28 dış altyapı gerektirdiği için beklemede) veya operatörün seçeceği başka bir öncelik |
| 2026-07-25 | **YENİ ÖZELLİK: Ölünce son kayıt noktasına dönme** — operatör kararı ("ölünce ne olacak" sorusuna yanıt: "bir önceki kayıt noktasına dönecek"). `USaveGameManager::RevertToLastSave()` (yeni, en son başarıyla yazılan yuvayı — otomatik veya elle, hangisi son ise — yükler) + `APlayerCharacter::HandleDeath()` (`HealthComponent::OnDeath`'e abone, tek tüketici — bileşen genel-amaçlı kalmaya devam ediyor). Küçük-mod inceleme (2 mercek, Workflow) canlıya geçmeden ÖNCE 5 bulgu verdi, 4'ü doğrulandı: (1) **majör** — `SetActorLocation` motor kaynağından doğrulanan `TeleportSucceeded`/`OnTeleported` zincirini tetiklemiyordu, oyuncu ışınlandıktan sonra eski hız/düşme durumuyla hareket etmeye devam ederdi; `TeleportTo`'ya geçilerek düzeltildi (bu ayrıca (2) numaralı bulguyu da çözdü). (2) **minör** — aynı çağrı geometriye-gömülme kontrolü yapmıyordu (`TeleportTo`'nun `FindTeleportSpot`'u bunu üstleniyor). (3) **kritik** — hiç kayıt yokken ölüm kalıcı/iyileşemez 0-HP soft-lock'a yol açıyordu (`TakeDamage`/`Heal` `IsDead()` iken kalıcı no-op) — `HandleDeath` artık kayıt yoksa tam canla devam ettiriyor. (4) **kritik** — otomatik kaydetmenin bu "ölü" anlık görüntüyü meşru kontrol noktası sanıp zehirleyebileceği bulgusu (3) düzeltilince yapısal olarak imkansız hale geldi. **PIE'de CANLI beşinci bir bulgu daha çıktı** (statik incelemenin kaçırdığı): yalnızca canı geri getirmek yetmiyordu — ölüm sebebi (hipotermi) hâlâ sürüyorsa `TemperatureSimulation` bir sonraki karede tekrar hasar verip `OnDeath`'i YENİDEN tetikliyor, sonsuz ölüm-diriliş döngüsüne giriyordu (canlı testte onlarca tekrarla doğrulandı). Düzeltme: `HandleDeath` artık HER dönüş türünde (başarılı veya fallback) `VücutSıcaklığı`'nı 37°C'ye sıfırlıyor (kod tabanındaki TEK hasar kaynağı hipotermi/aşırı-sıcak olduğundan güvenli bir varsayım — yeni bir hasar kaynağı eklenirse yeniden değerlendirilmeli). **Derleme temiz + 51/51 test PASSED** (regresyon yok, üç ayrı build+test turu). **PIE canlı doğrulama** (3 tur gerekti — ilk ikisi doğal hava rerollu yüzünden ya eksik ya sonuçsuzdu): `weather_force 7` sabit tutularak gerçek bir ölüm elde edildi, TEK bir "Oyuncu oldu"/`RevertToLastSave`/`LoadGame` satırı (öncekinin aksine onlarca DEĞİL) ve sonrasında can'ın gerçek kayıt değerinde (61.9) sabit kalıp YENİ bir ölüm tetiklenmediği kanıtlandı | Faz 3 (Sistem #21-28) veya operatörün seçeceği başka bir öncelik |
| 2026-07-25 | **KİLOMETRE TAŞI: Tam döngü** (Faz 2) KANITLANDI — editör kapatılıp yeniden açıldıktan sonra AYNI PIE oturumunda: `task_enqueue`→NPC atandı→yürüdü/çalıştı (yorgunluk 0→7.3)→görevi tamamladı; araya `save_game`(2221 bayt)+`load_game` round-trip'i girdi, `npc_dump` NPC durumunun bundan ETKİLENMEDİĞİNİ (Sistem #17'nin "yalnız Zaman+Oyuncu" sözleşmesiyle tutarlı) kanıtladı; ardından `audio_dump`+`ui_push_screen`+`hud_show_widget` çalıştı, son `npc_dump` yine NPC'nin bozulmadığını gösterdi — NPC+kaydet/yükle+ses+arayüz GERÇEKTEN eş zamanlı bir arada çalıştığı kanıtlandı, kod değişikliği gerekmedi. Yan gözlem (motor kaynaklı, düşük öncelik, kod hatası değil): `load_game`/`audio_dump` komutlarında zararsız bir "Kullanım dışı komut" modal diyaloğu çıkıyor ama komutun kendi çıktısı hemen ardından başarıyla geliyor — projede sıfır `DeprecatedFunction` kullanımı olduğu grep ile doğrulandı, kovalanmadı. Faz 2'nin daha geniş "NPC kar fırtınasında içeri kaçar" çıkış kriteri bu kapsamda DEĞİL, test edilmedi | Operatörün 3 maddelik "not done" listesindeki HEPSİ artık kanıtlandı (profiling, kar fırtınası, tam döngü) — sıradaki: Faz 3 (Sistem #21-28) veya operatörün seçeceği başka bir öncelik |
| 2026-07-25 | **KİLOMETRE TAŞI: İlk kar fırtınası** KANITLANDI — `weather_force 7` ile PIE'de (operatör elle komut yazdı) hipotermi (can 67.8→0.0, ölümcül), rüzgar (küresel-hız 41-50) ve kar birikimi (derinlik 0→0.8-1.0) AYNI Blizzard anında eş zamanlı gözlemlendi, üçü de `UWeatherSimulation::GetCurrentState()`'i paylaşıyor. Kod değişikliği GEREKMEDİ (yalnızca gözlem/doğrulama). İlginç davranış notu: `weather_force`'un 30sn'lik reroll-koruması, birden çok konsol komutunu elle yazıp okumanın süresiyle aşılabiliyor (tasarım gereği, hata değil). Yan etki: oyuncu can=0 ile öldü, `OnDeath`'e kod tabanında hiç abonelik yok (respawn dürüst kapsam dışı) — canlandırma bir sonraki adımda `load_game` ile yapılacak | "Tam döngü" kilometre taşı (Faz 2) — önce `load_game` ile oyuncuyu canlandırma |
| 2026-07-25 | Sistem #22'nin bir parçası: `profile_spawn_stress` konsol komutu (Sistem #22, `DevTools/ProfilingCommands.cpp` — projenin İLK dosyası bu klasörde) eklendi, küçük-mod inceleme (2 mercek) 5 bulgu verdi (kelepçelenmemiş `Atoi` girdisi + PIE'yi dondurma riski dahil), 4'ü düzeltildi, 1'i (mevcut `ULogisticsNetwork::RegisterNode`'un O(M²) kayıt-anı toplu maliyeti) bilinçli olarak DÜZELTİLMEDİ (dev-tool'un tek seferlik kullanımı için orantısız bir Sistem #8 çekirdek refactoru gerektirirdi). **Derleme temiz + 51/51 test PASSED** (regresyon yok). PIE'de "Profiling ilk ölçüm: 60 FPS'te 100 bina + 50 makine" kilometre taşı KANITLANDI — `profile_spawn_stress 100 50` sonrası **59.93 FPS/16.69ms** (Game=5.08ms, Draw=3.37ms, GPU=5.09ms), hedef karşılandı. **Yöntemsel bir yanlış-alarm SIRASINDA yakalanıp düzeltildi:** ilk ölçüm PIE penceresi (operatör sohbete geçtiği için) OS-odaksızken alınmış, 8-20 FPS gibi yanıltıcı düşük değerler vermişti; kod-tarafı kök-neden araması (arka planda bir workflow ile) `WorldPartitionHelper::RebuildCellRegistry`'nin 1sn'de bir kare-bölümlemesiz 3× `TActorIterator` taraması yaptığını (CLAUDE.md'nin "Tick disiplini" kuralına aday bir ihlal) buldu, ama motor kaynağından (`Actor.cpp::SetActorHiddenInGame/SetActorEnableCollision`) doğrulanınca bu çağrıların ZATEN dahili eşitlik korumalı olduğu (`if (IsHidden()!=bNewHidden)`) ortaya çıktı — asıl ipucu `stat unit`teydi: Game+Draw+GPU toplamı (~14-21ms) ölçülen Frame süresinden (118ms) tamamen kopuktu, bu da kod değil arka-plan throttle'ı (editörün "Use Less CPU when in Background" ayarı) işaret ediyordu; pencere odaklanınca sorun kayboldu, HİÇBİR kod değişikliği gerekmedi. WorldPartitionHelper'ın kare-bölümlemesiz taraması yine de gelecekte (çok daha büyük aktör sayısında) gözden geçirilmesi gereken dokümante bir gözlem olarak bırakıldı (düzeltilmedi — şu ölçekte gerçek darboğaz değil) | "İlk kar fırtınası" ve "Tam döngü" kilometre taşları (ikisi de tek PIE oturumunda eş zamanlı gözlem bekliyor) |
| 2026-07-25 | Faz 1 entegrasyon borcu (Construction↔Logistics↔Production) ✅ TAMAMLANDI — operatör Faz 2 sonrası bu çalışmayı seçti. `FBuildingDefinition::BuildingClass`+`ProductionRecipeIDs` eklendi, `build_place` artık üretim makinesi spawn edebiliyor; `AProductionMachine::BeginPlay` `InputBuffer`/`OutputBuffer`'ı iki `AStorageNode` proxy'siyle (`BindExternalStorage`, `MachineInput`/`MachineOutput`) lojistik ağına kaydediyor. Küçük-mod inceleme (2 mercek) `SpawnActor`'ın senkron `BeginPlay`'i yüzünden `AcceptorCache`'in yanlış tiple kurulduğu kritik bir bulgu verdi (motor kaynağından doğrulandı), `SpawnActorDeferred`+`FinishSpawning` ile düzeltildi. **Derleme temiz + 51/51 test PASSED** (regresyon yok, saf wiring). **PIE'de İKİ AYRI oturumda TAM doğrulandı**: ilk oturumda (2) kanıtlandı (önceden var olan bir makine bile artık ağa kayıtlı) ama `build_place OdunFirini` malzeme yetersizliğinden başarısız oldu ve operatör editörü kapattı; ikinci oturumda (operatör "aç yapalım" dedi) konteyner önce toplanıp `build_place OdunFirini` TEKRAR denendi — bu kez "Makine kaydedildi (toplam 3)" + "Dugum kaydedildi (toplam 5)→(toplam 6)" + "Insaat: basarili" ile (1) de tam kanıtlandı, MCP sahne taraması yeni `ProductionMachine_1` aktörünü yapısal olarak doğruladı | Faz 1 entegrasyon borcu TAMAMEN kapandı. Sıradaki: Faz 3 (Üretim Kalitesi, Sistem #21-28) veya operatörün seçeceği başka bir öncelik |
| 2026-07-24 | Sistem #20 ✅ TAMAMLANDI — UI Mimarisi, **Faz 2'nin (Simülasyon Derinliği) 10 sisteminin SONUNCUSU.** Proje hiç Widget Blueprint içeriğine sahip olmadığından `UScreenBase` bilerek soyut yapılmadı — içeriksiz haliyle bile PIE'de gerçek bir widget olarak test edilebiliyor. Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE 6 bulgu verdi (biri kritik), altısı da düzeltildi. **En ciddi bulgu:** `ScreenStack`/`ActiveWidgets` yalnızca `IsValid()` ile takip ediliyordu — bir ekran kendi kapat düğmesinden `PopScreen` DIŞINDA kapanırsa (motorun `RemoveFromParent`'i widget'ı hâlâ geçerli bir UObject bırakır, yalnızca Slate'ten koparır) bu ASLA yakalanmazdı, oyuncu görünmeyen bir menüde girdi kilitli kalırdı. Motor kaynağından doğrulanan `OnNativeDestruct` olayına (RemoveFromParent SENKRON tetikler) abone olunarak düzeltildi. Ayrıca `UInventoryWidget`'ın delegate aboneliğini widget kapanınca hiç bırakmaması (NativeDestruct eklendi) ve `BoundInventory`'nin güçlü referans olması (ULogisticsNetwork emsaliyle TWeakObjectPtr'a çevrildi) düzeltildi. PIE canlı testinde SetWidgetToFocus eklenirken YENİ bir hata ortaya çıktı ("Non-Focusable widget") — SupportsKeyboardFocus() kontrolüyle kapatılıp yeniden doğrulandı. **Derleme temiz + 51/51 test PASSED** (50 önceki + 1 yeni UIMath grubu). PIE canlı doğrulama: `ui_push_screen`/`ui_pop_screen` derinlik+giriş-modu round-trip'i, `hud_show_widget`/`hud_hide_widget` aktif-widget round-trip'i, ikisi de hatasız. DÜRÜST kapsam: ortam döngüleri ve batch güncelleme/virtual scroll (içerik/genişlik olmadan test edilemez) bilerek YAZILMADI | **Faz 2 TAMAMLANDI.** Sıradaki: Faz 3 (Üretim Kalitesi, Sistem #21-28) — önce Sistem #22 (Dev araçları, zaten kısmen mevcut) veya #23 (Hata işleme/log) değerlendirilebilir, operatöre soruldu |
| 2026-07-24 | Sistem #19 ✅ TAMAMLANDI — Ses Yöneticisi. Projede hiç ses varlığı olmadığından (`Content/` sessiz) tasarım motorun gerçek API'lerine (`PlaySoundAtLocation`/`PlaySound2D`/`CreateSound2D`+`FadeIn`/`FadeOut`, `Archive`/`AudioComponent.cpp` kaynağından doğrulandı) ince bir sarmalayıcı olarak kuruldu, `Sound==null` baştan güvenli-test-edilebilir bir yol olarak tasarlandı. Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE **4 doğrulanmış bulgu** verdi, dördü de düzeltildi: (1) **majör** — motor `bAutoDestroy=true` varsayılanıyla döngüsüz bir parça bitince `UAudioComponent`'i kendiliğinden yok ediyordu (`OnAudioFinished` hiç bağlı değildi), `CurrentMusicComponent` bare non-null kontrolüyle (bu projenin `IsValid()` kuralına aykırı) çöp işaretçi olarak kullanılabilirdi — `bAutoDestroy=false` + tüm kontroller `IsValid()`'e çevrildi. (2) **majör** — `SetCategoryVolume` çalmakta olan müziğe hiç yayılmıyordu (bir sonraki Play/Crossfade'e kadar eski sesle çalardı) — CLAUDE.md'nin delegate-broadcast kuralına uygun yeni `FOnCategoryVolumeChanged` eklenip `UMusicManager` abone edildi, artık anlık güncelleniyor. (3-4) minör: Spawn/Create null dönerse `CurrentMusic` yine de yeni parçaya ayarlanıyordu (düzeltildi, yalnızca gerçekten oluşunca ayarlanıyor); negatif `MaxAudibleDistance` hiç test edilmemişti (test eklendi). **Derleme temiz + 50/50 test PASSED** (48 önceki + 2 yeni AudioMath grubu). PIE canlı doğrulama (operatör elle test etti): `audio_dump` varsayılanları birebir gösterdi, `audio_set_volume Music 0.3` sonrası yalnızca Music değişti, `audio_test_play`/`music_test_play` (null) crash etmeden tamamlandı, `music_dump`→"(yok)". DÜRÜST kapsam: "ortam döngüleri" ve "max 128 eşzamanlı" sayacı içerik/test-edilebilirlik olmadan anlamsız olduğundan bilerek YAZILMADI. **Faz 2'nin (Simülasyon Derinliği) 10 sisteminden 9'u bitti** | Sistem #20 (UI Mimarisi) — Faz 2'nin son sistemi |
| 2026-07-24 | Sistem #18 ✅ TAMAMLANDI — Dünya Bölümlendirme. Motorun native World Partition'ı YERİNE MIMARI'nin taslağına sadık hafif bir özel sistem (ölçek/risk dengesizliği çok büyüktü). **Derleme sırasında iki gerçek isim çakışması keşfedildi:** UE 5.8'in kendi `WorldPartitionSettings.h`/`UWorldPartitionSettings`'i ile hem dosya hem sınıf adı çakıştı — `SurvivalWorldPartitionSettings` olarak yeniden adlandırılarak çözüldü (UHT'nin dosya+sınıf adı benzersizliği motor+proje genelinde geçerli). Küçük-mod inceleme canlıya geçmeden ÖNCE en ciddi bulguyu buldu: gizle/collision-kapat yalnızca hücre-seviyesi GEÇİŞTE uygulanıyordu, yeni eklenen veya hücre değiştiren aktörler asla senkronize olmazdı — düzeltme: her tarama artık kendi hücresinin güncel durumunu her aktöre koşulsuz uygular. `UProductionManager`/`UNPCManager`'a (Sistem #9/#15) boşaltılmış hücrelerde ilerletmeyi atlayan küçük bir entegrasyon eklendi — yeni bir "duraklat" semantiği icat edilmeden, mevcut "gerçek geçen süre kaybolmaz" garantisi otomatik telafi sağlıyor. **Derleme temiz + 48/48 test PASSED** (46 önceki + 2 yeni WorldPartitionMath grubu). PIE canlı doğrulama: oyuncu UnloadRadius dışına taşınınca hücre boşaltıldı + türbin gizlendi, geri dönünce hücre yüklendi + türbin tekrar göründü — tam round-trip. DÜRÜST kapsam: "boşaltma" spawn/destroy değil (yalnızca gizle+collision-kapat+ilerletme-atla) — bellek değil CPU/render maliyeti azalıyor | Sistem #19 (Ses Yöneticisi) |
| 2026-07-24 | Sistem #17 ✅ TAMAMLANDI — Kaydet/Yükle. `UGameplayStatics::SaveGameToMemory`/`LoadGameFromMemory` (motorun kendi `USaveGame` serileştirmesi) + `FArchiveSaveCompressedProxy`/`LoadCompressedProxy` (zlib) ile gerçek sıkıştırma. **En ciddi bulgu, küçük-mod inceleme SIRASINDA (motor kaynağından — `Archive.cpp::SerializeCompressedNew` — doğrulanarak) yakalandı:** motorun sıkıştırma-çözme kodu bozuk/rastgele baytlarda `GetError()` ile hata DÖNMÜYOR, `Fatal`-seviyeli log ile DOĞRUDAN ÇÖKÜYOR — "bozuk veriye karşı korumalı" sözleşmem başta YANLIŞTI (kendi bozuk-veri testim bile bu haliyle süreci çökertirdi). Düzeltme: motora ulaşmadan ÖNCE kendi imza+CRC32+uzunluk zarfımı doğrulayan bir katman eklendi. 2 diğer gerçek bug daha bulunup düzeltildi: `TotalPlayTimeSeconds` yüklemede geri yazılmıyordu; `InventoryComponent::RestoreSlots` boşalan slotlar için UI'ı bilgilendirmiyordu. **Derleme temiz + 46/46 test PASSED** (44 önceki + 2 yeni SaveDataSerializer grubu — bozuk-veri testi düzeltme SONRASI süreci çökertmeden geçti). PIE canlı doğrulama: konum kaydedilip başka bir yere taşındı, `load_game` ile GERÇEKTEN kaydedilen konuma geri döndü — tam disk-round-trip kanıtlandı. Dürüst kapsam: yalnızca Zaman+Oyuncu kaydediliyor (İnşaat/Üretim/Lojistik/NPC/Hava henüz değil), async serileştirme ve bulut senkronizasyonu bilinçli olarak ERTELENDİ (gerekçe ADR'de) | Sistem #18 (Dünya Bölümlendirme) |
| 2026-07-24 | Sistem #16 ✅ TAMAMLANDI — Görev Planlayıcı, Sistem #15'in üzerine kuruldu (`UNPCBrain`'e `SkillLevel` + görev-tamamlama/başarısızlık zamanlayıcıları eklendi, kesintide SIFIRLANMAZ — "görev durumunu devam ettir" DoD'si). Küçük-mod inceleme (2 mercek) canlıya geçmeden ÖNCE 3 gerçek bug buldu, üçü de düzeltildi: eksik `ProductionMachine.h` include'u (derleme riski), görev başarısız olunca `AIController`'ın hemen durdurulmaması (piyon artık-geçersiz hedefe yürümeye devam ederdi — `ClearTask()` artık kendi içinde anında `EvaluateState()` tetikliyor), NPC yok edildiğinde aktif görevin sessizce kaybolması (artık `HandleTaskFailed` ile aynı backoff'lu requeue mantığını paylaşıyor). **Derleme temiz + 44/44 test PASSED** (42 önceki + 2 yeni TaskSchedulerMath grubu). **PIE canlı doğrulama tam zinciri kanıtladı** (operatör elle test etti): `task_enqueue`→kuyruğa eklendi→boş NPC'ye otomatik atandı→NPC GERÇEKTEN yürüdü→Working'de 5sn çalışıp otomatik tamamladı→`npc_dump` Idle/görevsiz/yorgunluk-matematiksel-tutarlı gösterdi. Bu ayrıca Sistem #15'te "navmesh doğrulanamadı" notunu düzeltti — bu oturumda fiziksel yürüme GERÇEKTEN çalıştı (nav verisi önceki oturumdan zaten bake edilmiş kaydedilmişti) | Sistem #17 (Kaydet/Yükle) |
| 2026-07-24 | Sistem #15 ✅ TAMAMLANDI — NPC Yapay Zeka, **Faz 2'nin "Yerleşim Hayatı" bloğunun (Sistem #15-20) ilk sistemi.** `UHealthComponent`'i (Sistem #12) NPC için YENİDEN KULLANDI, MIMARI'nin tam `UNPCTask`/`UTaskScheduler` hiyerarşisi YERİNE minimal `FNPCTaskData` (Sistem #16 gerçek kuyruk/beceri-eşleşmesini üstlenecek), yol bulma motorun NavMesh+`AAIController::MoveToLocation`'ı ile (özel pathfinding YAZILMADI), `UNPCManager` Sistem #9'un kare-bölümleme desenini (`ComputeBatchSize` doğrudan yeniden kullanılarak) tekrarlıyor. İnceleme (2 mercek) canlıya geçmeden ÖNCE aynı kök nedenin iki görünümü olan 2 gerçek bug buldu: Walking durumundayken görev yeniden atanınca `MoveToLocation` hiç yeniden tetiklenmiyordu (Walking→Walking "değişiklik yok" sayılıyordu) VE Walking'den çıkışta `StopMovement()` hiç çağrılmıyordu — ikisi de düzeltildi (`AssignTask` zaten-Walking'de elle yeniden tetikliyor, yeni `OnStateExited` durduruyor). **Derleme temiz + 42/42 test PASSED** (39 önceki + 3 yeni NPCMath grubu). PIE canlı doğrulama: görev atanınca Idle→Walking, can düşürülünce (görev aktifken) Hurt'un Walking'i doğru ezdiği, iyileşince görevi hatırlayıp Walking'e geri döndüğü MCP property okumalarıyla kanıtlandı — ama fiziksel yürüme hareketinin kendisi bu oturumda doğrulanamadı (`NavMeshBoundsVolume` için nav mesh hiç bake edilemedi, `MoveToLocation` doğru çağrıldığı nav log'unda görüldü ama motor tarafında bir build-kilidi PIE boyunca takılı kaldı — MCP-güdümlü PIE'ye özgü çevresel bir kısıt, kod hatası değil, skill'e not düşüldü) | Sistem #16 (Görev Planlayıcı) |
| 2026-07-24 | Sistem #14 ✅ TAMAMLANDI — Kar Birikimi, **Faz 2'nin "Hava Simülasyonu" bloğunun (Sistem #11-14) son sistemi.** MIMARI'nin per-hücre `TMap<FIntPoint,float>` ızgarası bilinçli olarak TEK global `CurrentSnowDepth`'e sadeleştirildi (hava/sıcaklık zaten global, ızgara hiçbir değer katmazdı — Sistem #10'un `CurrentTemperature` asimetrisiyle aynı gerekçe). İnceleme (2 mercek) canlıya geçmeden ÖNCE bir bulgu verdi: oyuncunun `MaxWalkSpeed`'ini önbelleğe alan mekanizma yalnızca bir bool bayrakla korunuyordu — piyon değişse (henüz erişilemez bir gelecek özellik) YENİ piyonun kendi hızını hiç yakalamaz, ESKİ piyonun değeriyle sessizce ezerdi; `TWeakObjectPtr<ACharacter>` ile hangi piyon için önbelleklendiği de takip edilerek proaktif olarak kapatıldı (henüz tetiklenmemiş ama bu projenin tekrarlayan "bayat aktör-bağlı önbellek/kayıt" hata sınıfının bir başka görünümü). **Derleme temiz + 39/39 test PASSED** (36 önceki + 3 yeni SnowMath grubu). PIE canlı doğrulama (operatör elle test etti): `snow_set_depth 60` → `snow_dump` gerçek erimeyi (derinlik 60→35.2) ve doğru hesaplanmış hareket-çarpanını (0.58, matematikle birebir) ve inşaat-engelini (EVET, 35.2>30) gösterdi; `build_place Depo` beklenen şekilde başarısız oldu — ama aynı hücrenin önceki sistemlerin test aktörlerinden dolayı zaten dolu olma ihtimali nedeniyle tam izole değil (ikinci bir "derinlik=0→başarılı" karşı-testi operatörden istendi, yanıt gelmeden devam edildi; `snow_dump`'ın doğrudan `insaat-engeli` okuması yine de yeterli kanıt kabul edildi, Sistem #7'deki aynı dürüst standart). Sıcaklık/Rüzgar/Kar'ın ÜÇÜ de Hava Durumu'nun çıktısını tüketiyor (gerçek entegrasyon, Faz 1'in aksine) ama TEK bir canlı Blizzard'ın üç etkiyi eş zamanlı gösterdiği gözlemlenmedi — bkz. Faz 2 durum notu | Sistem #15 (NPC Yapay Zeka) — Faz 2'nin "Yerleşim Hayatı" bloğu başlıyor |
| 2026-07-24 | Sistem #13 ✅ TAMAMLANDI — Rüzgar Simülasyonu. `UWindSimulation` bilerek non-tickable (küresel rüzgar Sistem #11'den okunur, yerel hamle durumsuz/deterministik hesaplanır); `AWindTurbine` Sistem #9/#10'un altyapısını (kare-bölümlemeli zamanlayıcı + enerji-üretici kaydı) yeniden kullanıyor. Küçük-mod inceleme (2 mercek) **inşaat/build aşamasından ÖNCE, review sırasında** kritik bir bulgu verdi (ikisi de bağımsız): `AWindTurbine::AdvanceProduction`/`GetProgress` override edilecek şekilde yazıldı ama taban sınıf `AProductionMachine::AdvanceProduction` `virtual` DEĞİLDİ (yalnızca `GetProgress` işaretlenmişti — kendi hatam, ikisini birden işaretlemeyi unuttum). Düzeltilmeseydi: derleme hatası VEYA (specifier'lar kaldırılsaydı) `UProductionManager`'ın taban-sınıf işaretçisi üzerinden yaptığı çağrı sessizce taban sınıfın recipe-mantığına giderdi, türbin hiçbir hata vermeden asla enerji üretmezdi. `virtual` eklenip derleme+PIE ile override'ın gerçekten çalıştığı kanıtlandı (`CurrentOutput=27.75`, `State=Running`). **Derleme temiz + 36/36 test PASSED** (33 önceki + 3 yeni WindMath grubu). PIE canlı doğrulama: spawn edilen türbin `UProductionManager`'a kaydolup gerçek rüzgar hızından kübik-rampa çıktısı üretti | Sistem #14 (Kar Birikimi) — Faz 2'nin "Hava Simülasyonu" bloğunun son sistemi, "İlk kar fırtınası" kilometre taşını tamamlar |
| 2026-07-24 | Sistem #12 ✅ TAMAMLANDI — Sıcaklık Simülasyonu + **ilk oyuncu-can sistemi** (`UHealthComponent`, MIMARI'de hiç yoktu, operatör "Minimal UHealthComponent ekle" seçti). Küçük-mod inceleme (2 mercek) 2 küçük bulgu verdi (Euler-yaklaşım kare-hızı düzeltmesi uygulandı, ilk-Tick-öncesi-varsayılan Sistem #10/#11 ile tutarlı kabul edildi) ama **en ciddi bulguyu ne birim testleri ne de inceleme yakaladı — canlı PIE testinde bulundu:** `DT_WeatherProfiles`'daki TÜM sıradan hava profilleri (en yaygın `Clear`=22°C dahil, yalnız `Heatwave` hariç) `SafeMinBodyTemp`=35°C altında olduğundan, vücut sıcaklığı ham ortam sıcaklığına doğrudan sürüklenince oyuncu **HER normal havada** (hiçbir aşırı koşul yokken) hipotermiden ölüyordu — PIE'de `CurrentHealth`'in ~1 dakikada 0'a düşmesiyle doğrudan gözlemlendi. Kök neden: gerçek insan çekirdek sıcaklığı dış hava sıcaklığına eşitlenmez, metabolizma/giysi normal havada ~37°C'de tutar. Düzeltme: yeni `SurvivalTemperature::ComputeTargetBodyTemperature` — `[ComfortMinTemp=10, ComfortMaxTemp=30]` bandındaki ortam sıcaklığı hedefi DEĞİŞTİRMEZ (37°C sabit), yalnızca bandın DIŞI hedefe yansır; artık sıradan hava güvenli, yalnızca `Snowing`/`Blizzard`/`Heatwave` gerçek tehlike (Faz 2'nin "İlk kar fırtınası" çıkış kriteriyle doğrudan uyumlu). Bu, bu projede pure-fonksiyon birim testlerinin VE izole-mercek incelemenin ikisinin de kaçırdığı, yalnızca uçtan-uca CANLI oynanış testinin ortaya çıkarabildiği ilk denge/entegrasyon hatası — metodolojiye "PIE doğrulaması opsiyonel değil, tamamlayıcı bir bulgu katmanı" dersi olarak düşüldü. **Derleme temiz + 33/33 test PASSED** (27 önceki + 1 Health + 4 yeni TemperatureMath grubu). PIE canlı doğrulama: düzeltme sonrası normal havada vücut sıcaklığı 37°C/can 100 sabit kaldı; MCP ile vücut sıcaklığı 10°C'ye manuel düşürülünce gerçek hasar uygulanıp can hızla 0'a düştü ve sıcaklık konfora doğru geri sürüklendi — güvenli/tehlikeli/iyileşme davranışlarının üçü de kanıtlandı | Sistem #13 (Rüzgar Simülasyonu) |
| 2026-07-24 | Sistem #11 ✅ TAMAMLANDI — **Faz 2'nin (Simülasyon Derinliği) ilk sistemi.** Küçük-modda inceleme 2 doğrulanmış **runtime-bug** verdi: (1) `EvaluateWeather`'in erken-dönüş kısayolu, `TransitionProgress`'in struct-varsayılanı (1.0f, "zaten tamamlanmış" anlamına geliyor) ile veritabanından HİÇ doldurulmamış gerçek durumu ayırt edemiyordu — ilk değerlendirmede rastgele seçim varsayılan Clear'ı tekrar seçerse `BeginTransitionTo` (veritabanı profillerini + rastgele rüzgar yönünü uygulayan TEK yer) hiç çağrılmadan `CurrentState` kalıcı olarak sahte varsayılanlarda kilitlenirdi; `bHasInitializedState` bayrağı eklenerek düzeltildi. (2) `LerpWeatherState`, her zaman BİRİM vektör olan `WindDirection`'ı düz `FMath::Lerp` ile enterpolasyon yapıyordu — neredeyse-ters-yönlü iki rüzgar yönü arasında Alpha≈0.5'te vektör SIFIRA yaklaşıp yön tanımsızlaşırdı (nadir bir kenar durum DEĞİL, her geçişte rastgele 360° seçilen yön göz önüne alınca genel durum); `FVector::SlerpNormals` (motorun birim-vektör enterpolasyonu için sunduğu doğru araç) ile düzeltildi. 1 küçük bulgu da uygulandı: geçiş sürerken tetiklenen bir değerlendirme devam eden geçişi tersine çevirebiliyordu (varsayılan ayarlarda `TransitionDuration`<`EvaluationInterval` olduğu için erişilemez ama veri yanlış yapılandırılırsa gerçek) — artık değerlendirme yalnızca önceki geçiş tamamlandıktan (`TransitionProgress>=1.0`) sonra yapılıyor, gecikmiş değerlendirme kaybolmuyor. Tasarım: MIMARI'nin `AWeatherManager`(`AGameStateBase`)+`UWeatherSimulation`(subsystem) ikilisi Sistem #7/#9/#10'un "Singleton=Subsystem" kararıyla TEK sınıfa birleştirildi; hava DEĞERLERİ (kıtlık alarmının aksine) her Tick'te sürekli yayınlanır (CraftingComponent'in `OnCraftingProgress`'iyle aynı gerekçe); `FWeatherProfile` DataTable satır ADI yerine açık `Condition` alanına göre önbelleklenir (Item/Recipe/Building'den bilinçli sapma — enum zaten var, string round-trip gereksiz). **Derleme temiz + 27/27 test PASSED** (24 önceki + 3 yeni Hava grubu). PIE canlı doğrulama 35+ dakika kesintisiz çalıştırıldı: 9 profil yüklendi, onlarca geçiş TAM 30sn periyotla (bazı aralıklar 60/90sn — aynı durum tekrar seçilip atlanınca, düzeltmenin kanıtı), 7/9 durum gözlemlendi, `weather_dump` çıktısı yazılan profil verisiyle birebir eşleşti — hiçbir çökme/geriye-sıçrama/takılma olmadı | Sistem #12 (Sıcaklık Simülasyonu) |
| 2026-07-24 | Sistem #10 ✅ TAMAMLANDI — **Faz 1 (Dikey Dilim) sistemlerinin TAMAMI bitti (bkz. çıkış kriteri durum notu — entegrasyon ayrı).** Küçük-modda inceleme 1 doğrulanmış **runtime-bug** verdi: `AddFuel`, `MaxAmount` belirtilmeden yapılan İKİNCİ+ çağrılarda tavanı asla büyütemiyordu — rezerv ilk yatırılan miktara sessizce kilitleniyordu (`void` dönüyordu, çağıran tarafın kaybı fark etmesi imkansızdı). Düzeltme: `AddFuel` artık `float` döner (GERÇEKTEN kabul edilen miktar — `AddItem`/`RequestTransport` ile aynı desen), `resource_add_fuel` konsol komutu eksik kabulü "TAVANA ULASILDI" ile logluyor. 1 küçük bulgu da uygulandı: `AActor` yalnızca dolaylı (console-komut yardımcı fonksiyonlarının include'ları üzerinden) tam tipti — `#include "GameFramework/Actor.h"` + `class AActor;` ileri-bildirimi eklendi (kırılgan örtük bağımlılık, Sistem #7'nin `UStaticMesh` include dersiyle aynı sınıf). Tasarım: `UResourceSimulation` (`UGameInstanceSubsystem`+`FTickableGameObject`, Lojistik ile aynı desen) PROAKTİF olarak Sistem #7/#8/#9'da tekrar tekrar bulunan "yok edilen aktör kaydı kalıcı kalıyor" hata sınıfına karşı BAŞTAN savunmalı yazıldı (`PruneStaleRegistrations`, her Tick'te TWeakObjectPtr anahtarlı kayıtları temizler) — inceleme bu savunmayı ayrıca test etti, ek bulgu çıkmadı. Frequency ANLIK türetilir (elektrik şebekesi gerçek atalet göstermez), CurrentTemperature BİRİKİMLİDİR (gerçek termal kütle modeli) — bilinçli bir asimetri. **Derleme temiz + 24/24 test PASSED** (21 önceki + 3 yeni Kaynak grubu). PIE canlı doğrulama: yakıt-kıtlığı alarmı matematiksel beklentiyle (~3sn) tetiklendi, enerji brownout tetiklenince alarm SEBEBİ değişti (bInScarcity sabit kalırken), üretici eklenince enerji düzeldi ama sebep yakıt-kritik'e GERİ döndü — edge-triggered broadcast mantığı her iki yönde de kanıtlandı. **İki ortam sorunu** (kod hatası değil) atlatıldı: UHT `BlueprintReadOnly`-private-üye hatası (Sistem #4 `Slots` dersiyle aynı, `protected`+getter deseniyle çözüldü) ve Windows **Smart App Control**'ün derlenmiş, imzasız DLL'i "Uygulama Denetimi ilkesi" ile engellemesi (operatör ayarı kontrol edince çözüldü — skill'e `windows-toolchain.md`'ye kalıcı not düşüldü) | Faz 2 (Sistem #11: Hava Durumu) VEYA Faz 1 entegrasyon çalışması (Construction↔Production↔Logistics, gerçek konveyör) — karar operatöre bırakıldı |
| 2026-07-24 | Sistem #9 ✅ TAMAMLANDI. Küçük-modda inceleme 3 doğrulanmış **runtime-bug** verdi: (1) `AProductionMachine::GetProgress()` normalize edilmemiş ham saniyeyi dönüyordu (`SurvivalProduction::ComputeProductionProgress` — test edilmiş, kodda hiç çağrılmayan bir fonksiyon — CraftingComponent'in kendi ilerlemesini normalize etme deseninden sapma) — `GetProgress()` artık `.cpp`'ye taşınıp normalize ediyor. (2) **En ciddi bulgu**: `Tick_Production`, kare-bölümleme yüzünden TEK bir `AdvanceProduction` çağrısının BİRDEN FAZLA `ProductionTime`'ı kapsayabileceği durumda (çok makine VEYA kısa `ProductionTime`) fazla süreyi sessizce ATIYORDU — `Progress=0`'a sıfırlayıp döngüye devam etmiyordu; bu, `ProductionManager.h`'ın kendi belgelediği "gerçek süre hiç kaybolmaz" garantisiyle doğrudan çelişiyordu. Düzeltme: `Tick_Production` artık bir `for(;;)` döngüsüyle kaynaklar TÜKENENE veya süre BİTENE kadar birden fazla tamamlanmış döngü işliyor (veri hatasına karşı `MaxCyclesPerAdvance=1000` savunma tavanı, aşılırsa kalan süre GERÇEKTEN bir sonraki çağrıya bırakılıyor, atılmıyor). (3) `HasSufficientOutputRoom` çok-satırlı bir çıktıda her satırı AYNI statik `CurrentWeight`'e karşı bağımsız kontrol ediyordu (toplam ağırlık aşılsa bile her satır "sığar" görünebilirdi) — `UInventoryComponent::HasRoomForBatch` (yeni, additive — Sistem #4'ün `InventoryComponent`'ine dokunmadan eklendi) + `FProductionRecipe::GetAggregatedOutputs()` ile düzeltildi. Tasarım kararları: `FProductionRecipe` MIMARI'nin İNLİNE `TArray<FProductionRecipe>` taslağı YERİNE DataTable+Subsystem deseni (Item/Recipe/BuildingDatabase ile aynı — "tek doğruluk kaynağı"); MIMARI'nin iki-adımlı `SetActiveRecipe`+`StartProduction`'ı TEK adıma indirgendi (henüz seçim UI'ı yok); `UProductionManager` (yeni, `UWorldSubsystem`+`FTickableGameObject`) "kare bölümleme"yi (CLAUDE.md: "500 makine / 60 frame") ilk kez gerçek bir mekanizmaya döktü — her makine kendi `GetActorTick` yerine `AdvanceProduction(GameTime)` ile GERÇEK-geçen-süre muhasebesiyle güncelleniyor. **Derleme temiz + 21/21 test PASSED** (18 önceki + 3 yeni `SurvivalGame.Production`, regresyonsuz). PIE canlı doğrulama (operatör elle test etti, 30+ saniye sürdürüldü): `production_set_recipe OdunIsleme` → 10 üretim döngüsü TAM 3.0sn arayla tamamlandı (çoklu-döngü düzeltmesinin doğrudan kanıtı) → InputBuffer 20→0, OutputBuffer 0→5, enerji 100→40, girdi tükenince `State: Blocked_NoInput`. **Yan not:** MCP köprüsü oturumu bu sistemde TEKRAR (üçüncü kez) bayatladı — `mcp-call.ps1`'e eklenen "otomatik iyileştirme" bu sefer TETİKLENMEDİ (kök neden teşhis edilemedi, elle önbellek temizleme ile atlatıldı); ayrıca oturum ortasında `mcp__unreal-mcp__*` araçları bağlantısı koptu, kalan iş ham `ue-tool.ps1`/`mcp-call.ps1` script'leriyle tamamlandı, araçlar daha sonra kendiliğinden geri geldi | Sistem #10 (Kaynak Simülasyonu) |
| 2026-07-24 | Sistem #8 ✅ TAMAMLANDI. Küçük-modda inceleme 2 doğrulanmış **runtime-bug** verdi, ikisi de sistemi tamamen işlevsiz bırakacak ciddiyette: (1) `AStorageNode` hiç kök bileşen atamıyordu (`UInventoryComponent` bir `USceneComponent` DEĞİL, kök olamaz) — `AActor::GetActorLocation()` motor kaynağından doğrulandığı üzere kök yoksa HER ZAMAN `(0,0,0)` döner, bu da tüm mesafe/rota mantığını (en yakın hedef araması + taşıma süresi hesabı) sessizce kırardı; düzeltme: yeni bir `USceneComponent SceneRoot` eklenip `SetRootComponent` ile kök yapıldı. (2) `FindBestDestination`, taşımanın `Source`'unu aramadan HARİÇ TUTMUYORDU — `Source` kendisi de alıcı bir tipse (varsayılan `Container`), kendine mesafesi 0 olduğundan HER ZAMAN kendini "en yakın hedef" seçip `RequestTransport`'un `Destination==Source` korumasına takılır, otomatik rota TAMAMEN çalışmazdı (varsayılan/en yaygın yapılandırmada); düzeltme: `FindBestDestination`'a `ExcludeNode` parametresi eklendi, `RequestTransport` kendi `Source`'unu bu şekilde hariç tutuyor. 2 küçük bulgu da uygulandı: `FVector::Dist`'in (UE5 LWC'de double) örtük `float`'a daralması artık açık `static_cast`; açıkça verilen bir `Destination` artık `CanAcceptItem` ile doğrulanıyor (önceden yalnızca otomatik-çözümlenen hedefler kontrol ediliyordu). Tasarım: `ULogisticsNetwork` `UCraftingComponent`'in "hemen tüket / zamanlayıcıyla üret / atomik geri-alma" desenini birebir taşıyor (Sistem #6), `AStorageNode` bilerek `AContainerActor`'dan (Sistem #4) ayrı tutuldu (farklı sorumluluk: makine/ağ tüketimi, oyuncu değil). **Derleme temiz + 18/18 test PASSED** (15 önceki + 3 yeni Lojistik grubu). PIE canlı doğrulama: 2 `AStorageNode` (Source: Odun×20, Sink: boş, 2000 UU arayla) → `logistics_request Odun 5` → log "2000 UU, 4.0sn" (iki düzeltmenin de gerçekten çalıştığının kanıtı) → Source 20→15 anında, ~4sn sonra Sink 0→5. Ayrıca: MCP köprüsünün `mcp-call.ps1` yardımcı script'i, editör yeniden başlatıldığında arka planda çalışan bir izleyici script'in oturumunun bayatlaması sorununu (Sistem #7'de de görülmüştü) kendi kendine iyileştirecek şekilde güçlendirildi (oturum hatası tespit edilirse otomatik yeniden-el sıkışma + tek seferlik yeniden deneme) | Sistem #9 (Üretim Makineleri) |
| 2026-07-24 | Sistem #7 ✅ TAMAMLANDI. Küçük-modda inceleme 2 doğrulanmış bulgu verdi: (1) **compile-error** — `TSoftObjectPtr<UStaticMesh>::LoadSynchronous()` iki çağrı noktasında (`BuildingBase.cpp`, `ConstructionComponent.cpp`) `UStaticMesh` hiç tam tanımlanmadan çağrılıyordu (ileri-bildirim yeterli değil, `Cast<T>` tam tip ister) — iki doğrulama ajanı SharedPCH'in bunu örtük olarak kapatıp kapatmadığı konusunda birbirine ZIT sonuca vardı (biri "gerçek hata", diğeri "SharedPCH /FI ile zaten tam tip sağlıyor, false-positive" dedi); tartışmayı çözmek yerine `#include "Engine/StaticMesh.h"`'i `BuildingDefinition.h`'a EKLEYEREK belirsizliği tamamen ortadan kaldırdım (PCH-detaylarına bağımlı olmayan, tek satırlık, yan etkisiz düzeltme). (2) **runtime-bug** — `ConfirmPlacement`'ın atomik-tüketim rollback'i yalnızca "malzeme yetersiz" dalını kapsıyordu; malzemeler tüketildikten SONRA `SpawnActor`/`RegisterBuilding` başarısız olursa (motor tarafından gerçekten mümkün, gözden geçirme motor kaynağından doğruladı) oyuncu hiçbir şey almadan malzeme kaybediyordu — `CancelCrafting`'deki "iade eksikse logla" deseniyle aynı şekilde düzeltildi. Ayrıca 3 küçük bulgu (doğrulanmamış, küçük-mod politikası) uygulandı: `DB` null-kontrolü eksikliği (`ConfirmPlacement`), ve `ABuildingBase`'in `NotPlaceable` yapılması (elle yerleştirilmiş/`BeginConstruction` hiç çağrılmamış bir örnekte `Demolish()` varsayılan `GridCoord`=(0,0)'ı silip başka bir binanın kaydını bozabilirdi — kök nedeni ortadan kaldırdım, bayrakla sarmadım). Derleme kendi başına 1 hata daha yakaladı: yeni test dosyasında `TestEqual` çağrıları `FVector` bileşenlerini (`double`, UE5 LWC) `float` literalle karşılaştırıyordu → aşırı yükleme belirsizliği (C2666), literalleri `double`'a çevirerek düzeltildi. **Derleme temiz + 15/15 test PASSED** (13 önceki + 2 yeni `SurvivalGame.Construction.ConstructionMath`, regresyonsuz). Editör: `DT_Buildings` (Depo: Odun×5, Cube mesh — `TSoftObjectPtr<UStaticMesh>` DataTable sütunu MCP `set_rows` üzerinden 3 farklı refPath formatıyla da YAZILAMADI, bilinen bir MCP-köprüsü tuhaflığı olarak skill'e not düşüldü, kozmetik/DoD-dışı olduğu için engelleyici değil), `UBuildingDatabaseSettings`+`UConstructionSettings` `DefaultGame.ini`'ye bağlandı (editör yeniden başlatılıp CDO'dan doğrulandı). **PIE canlı doğrulama (operatör elle test etti):** `build_place Depo` → hücre (1,0) başarılı (ağırlık 12.5→7.5), `build_demolish` → hücre boşaldı (MCP: sahnede yalnızca 1 `BuildingBase` kaldı, yetim aktör yok), aynı hücrede `build_place Depo` tekrar başarılı (ağırlık 7.5→2.5, MCP ile doğrulandı). Aynı-hücrede-YIKMADAN-ikinci-deneme (grid-doluluk-red testi) operatör tarafından atlandı; envanterin `Slots` alanı reflection'dan okunamadığı için (private, `CurrentWeight` gibi ayrı bir `BlueprintReadOnly` aynası yok) yeniden malzeme verip izole test kurulamadı — bu DoD maddesi (1) kod incelemesinin `EvaluatePlacementValidity`'nin `IsCellOccupied`'i ÖNCE kontrol ettiğini doğrudan doğrulamasına, (2) demolish→yeniden-başarılı akışının AYNI kontrol yolunu ters yönde kanıtlamasına dayanarak kabul edildi — tam siyah-kutu izolasyonu YOK, dürüstçe not düşülüyor | Sistem #8 (Lojistik Ağı) |
| 2026-07-21 | Proje iskeleti oluşturuldu: 115 klasör, .uproject, modül, config, takip dosyaları | Sistem #1 (Zaman) — `UTimeKeeper` + `UDayNightCycle` implementasyonu |
| 2026-07-21 | Sistem #1 kodu yazıldı: TimeMath (saf), TimeSettings, TimeKeeper, DayNightCycle, GameClock, SurvivalGameMode + 4 otomasyon testi + konsol komutları (`time_sethour`, `time_settimescale`, `time_dump`). Çok-ajanlı kod incelemesi yapıldı. | UE 5.8 kur (VS 2026 / MSVC v145) → derle → `SurvivalGame.Time` testlerini çalıştır → ✅ işaretle. Sonra Sistem #2 (Oyuncu Kontrolü) |
| 2026-07-21 | Motor hedefi UE 5.8'e çekildi (operatör düzeltmesi): .uproject EngineAssociation, Target.cs `BuildSettingsVersion.V7` + `EngineIncludeOrderVersion.Unreal5_8`, README/CLAUDE.md/CI belgeleri. Gereksinim: VS 2026 (MSVC v145) | UE 5.8 kur → derle → testleri çalıştır |
| 2026-07-21 | İnceleme 9 doğrulanmış bulgu çıkardı, HEPSİ düzeltildi: (1) güneş rotasyonu oku-değiştir-yaz→mutlak yazım, (2) saat kapısı `bClockRunning`+`ResetForNewGame`, (3) `OnDayNightStateChanged` başlangıç durumu yayını, (4) UTF-8 BOM (16 dosya + `Tools/add-bom.ps1`), (5) Build.cs ölü include path silindi, (6) delegate koalesans sözleşmesi belgelendi, (7) tek-oyunculu varsayım notu, (8) MinuteOfDayFloat IEEE sınır kelepçesi + testi, (9) Sunrise/Sunset sanitizasyonu + tek-sefer uyarı | Derleme doğrulaması (UE kurulumu bekliyor) |
| 2026-07-21 | UE 5.8 kuruldu (`C:\Users\atoly\Desktop\Unreal\UE_5.8`, engine kök doğrulandı). İlk `SurvivalGameEditor Win64 Development` derlemesi başlatıldı (arka plan, `build_log.txt`) | Derleme sonucu bekleniyor — geçerse otomasyon testleri (`SurvivalGame.Time`) çalıştırılacak |
| 2026-07-21 | Sistem #2 (Oyuncu Kontrolü) C++ tarafı bitti: `APlayerCharacter` (üçüncü şahıs, SpringArm+Camera, `bOrientRotationToMovement`), `ASurvivalPlayerController` (Enhanced Input bağlama, 5 handler), `UCameraManager` (zoom, saf `CameraMath`), GameMode varsayılan sınıflar. API'ler yazmadan ÖNCE gerçek 5.8 header'larından doğrulandı → çok-ajanlı inceleme 0 compile-error/0 runtime-bug buldu (2 minor kozmetik, uygulandı). Derleyicinin yakaladığı tek hata: C4458 — yerel `Character` değişkeni `AController::Character` üyesini gölgeliyor (5 handler'da `PlayerChar` olarak değiştirildi). Derleme temiz + **4/4 test PASSED** (ZoomAdimi yeni, Time testleri regresyonsuz) | Editörde Enhanced Input varlıkları + `BP_PlayerController` → PIE doğrulaması → Sistem #2 ✅. Sonra Sistem #3 (Etkileşim) |
| 2026-07-23 | Sistem #6 ✅ TAMAMLANDI. Küçük-modda inceleme (391k token) 2 doğrulanmış runtime-bug verdi — ikisi de "iade/tüketim miktarı kontrol edilmiyor" sınıfından, Sistem #4'te `TransferItemTo`'da düzeltilen aynı hatanın `CancelCrafting`'e sızması: (1) `CancelCrafting` `AddItem`'ın dönüşünü hiç okumuyordu (envanter doluysa iade sessizce eksik kalırdı), (2) `CanCraftRecipe`/`StartCrafting` bir tarifte aynı ItemID iki ayrı satırda geçerse ({Wood,5}+{Wood,3}) satır-satır bağımsız kontrol ediyordu (toplamıyordu) — gerçek maliyetten daha az malzemeyle üretim mümkündü. Çözüm: `FRecipeDefinition::GetAggregatedIngredients()` (ItemID'ye göre toplayan saf yardımcı) + `StartCrafting`'i ATOMİK yapan rollback (herhangi bir kalem eksik çıkarsa o ana kadar tüketilenler geri eklenir) + `CancelCrafting`'in eksik iadeyi loglaması. Regresyon testi eklendi (`RecipeDefinitionTests.cpp`). PIE doğrulaması sırasında **ayrı bir operasyonel hata** da bulundu ve düzeltildi: "editörü yeniden başlat" komutu `CloseMainWindow`'un sessizce başarısız olabileceğini hesaba katmıyordu — iki editör süreci aynı anda çalışıp `StartingItems` güncellemesi eski sürece gidip kayboldu; kök neden CDO'yu doğrudan sorgulayarak (`RecipeTable` gerçekte "None" mı) teşhis edildi, çözüm: kapatma sonrası process listesini DOĞRULAMAK (yalnızca "Editor kapandi" yazdırmamak) | Sistem #7 (İnşaat) |
| 2026-07-23 | Sistem #4+#5 ✅ TAMAMLANDI (birlikte — bağımlılık sırası MIMARI'den düzeltildi). Küçük-modda inceleme (ADR kararı, 437k token — önceki tam-modlara göre belirgin ucuz) 1 doğrulanmış runtime-bug (ComputeMaxAffordableCount'ta epsilon bölme öncesi ağırlık uzayında eklendiğinden çok küçük UnitWeight'te yanlış "sığar" sonucu — düzeltildi + regresyon testi eklendi) + 2 doğrulanmamış-ama-uygulanan minor bulgu (SoftObjectPtr.h include eksikliği; FInventorySlot::Durability transferde hep 1.0f'a sıfırlanıyordu — AddItem'a SourceDurability parametresi eklenip TransferItemTo slot-bazlı hale getirildi) verdi. Derleme 3 küçük hata sonrası temiz (BlueprintReadOnly private üyede kullanılamaz — Slots ve CurrentWeight düzeltildi). Editör: `DT_Items` (Odun/Balta/Taş) + `ItemDatabaseSettings` config bağlantısı (DefaultGame.ini) + `AContainerActor` test konteyneri (`StartingItems`). PIE doğrulaması: konteyner ağırlığı 7→0, oyuncu 0→7, tek E-basışında | Sistem #6 (Zanaat) |
| 2026-07-23 | Sistem #3 ✅ TAMAMLANDI. PIE doğrulaması: haritaya görünür test küpü eklendi (`AInteractableBase` + Engine Cube mesh, `/Script/EnhancedInput` üzerinden `add_to_scene_from_class`), oyuncu tekrar tekrar E'ye bastı, `InteractionCount` 0→94 (6 ayrı sıçrama) — tam zincir (tuş→IA→component→arayüz→sayaç) uçtan uca kanıtlandı | Sistem #4 (Envanter) |
| 2026-07-23 | Sistem #3 C++ + editör tarafı bitti: `InteractionMath` (saf: trace kısma / prompt debounce / menzil), `IInteractableInterface` (BlueprintNativeEvent), `UInteractionComponent` (kamera-yönlü küre süpürmesi, odak önbelleği, debounce'lu delegate), `AInteractableBase` (+`InteractionCount` sayacı — PIE doğrulaması MCP'den okunacak), controller'a IA_Interact. Editörde: `IA_Interact` + 9 eşlemeli IMC (E dahil) + BP CDO. **Derleme temiz, 7/7 test PASSED.** İnceleme (bütçe limitinde yarım kaldı, 4/7 ajan) 3 bulgu verdi, hepsi uygulandı: (1) **runtime bug** — yok edilen odak nesnesinde prompt temizlenmiyordu (`IsStale()` eklendi), (2) kare-0 trace sözleşmesi (post-increment), (3) menzil-orijinden ölçüm kararı ADR'ye | PIE doğrulaması (test küpü + E) → Sistem #3 ✅. Sonra Sistem #4 (Envanter) |
| 2026-07-23 | **Bütçe dersi:** aylık harcama limitine çarpıldı. Sebep: 3 sistemde toplam ~2.1M ajan tokeni yakan tam-boy çok-ajanlı incelemeler. ADR'ye kural yazıldı — inceleme varsayılan olarak küçük koşulacak (2 mercek, doğrulama yalnız compile-error/runtime-bug), tam sürüm yalnız kritik sistemlerde | — |
| 2026-07-23 | Sistem #2 ✅ TAMAMLANDI. Editör tarafı MCP köprüsüyle yapıldı (`.mcp.json` → `ModelContextProtocol` plugin HTTP sunucusu + `EditorToolset` araç setleri): 4 IA + IMC_Default (8 eşleme, gerçek modifier nesneleri) + BP_PlayerController (CDO atamaları) + BP_MainGameMode + TestPlayer haritası (şablondan kopya, WorldSettings GameMode override, 'Sun' etiketli ışık). **Kritik bug bulundu+çözüldü:** UE 5.8'de `InputMappingContext.Mappings` deprecated (5.7) — runtime YALNIZCA `DefaultKeyMappings.Mappings` okuyor (`GetMappings()` kaynağından doğrulandı); eşlemeler oraya taşınınca girdi çalıştı. PIE canlı test: hareket (0,0)→(623,324), yaw 0→53.5 — operatör onayı + transform kaydı. Yan kanıt: güneş PIE'de gerçek zamanlı dönüyor (Sistem #1 canlı doğrulama) | Sistem #3 (Etkileşim Çerçevesi) |
| 2026-07-21 | Ortam sorunları çözüldü: (1) Windows SDK/VS hiç kurulu değildi → VS 2026 Community kuruldu; (2) UE 5.8, VS2026'nın varsayılan MSVC 14.51 ile derlenmiyor (`hash_map` kaldırıldı, `nvtesslib` kırılıyor, Epic'in düzeltmesi 5.8'e backport edilmedi) → MSVC **14.50.35717** bileşeni ayrıca kuruldu, `%APPDATA%\Unreal Engine\UnrealBuildTool\BuildConfiguration.xml`'de `<CompilerVersion>14.50.35717</CompilerVersion>` ile sabitlendi; (3) NetFxSDK eksikti (`SwarmInterface` modülü) → `.NET Framework 4.6.2 targeting pack` eklendi. Ardından gerçek bir kod hatası çıktı: UE 5.8'de `EAutomationTestFlags` enum class'a taşınmış, `EAutomationTestFlags::ApplicationContextMask` artık yok — `TimeMathTests.cpp`'deki 3 kullanım `EAutomationTestFlags_ApplicationContextMask` (serbest sabit) olarak düzeltildi | **Derleme başarılı, 3/3 test PASSED.** Sistem #1 ✅. Sistem #2 (Oyuncu Kontrolü — Enhanced Input + üçüncü şahıs kamera) |

---

## BLOKE / RİSK LİSTESİ

| Risk | Etki | Durum | Aksiyon |
|------|------|-------|---------|
| ~~UE sürümü seçilmedi (5.4 varsayıldı)~~ | — | ✅ | Çözüldü: operatör UE 5.8 bildirdi; .uproject + Target.cs (V7/Unreal5_8) + tüm belgeler güncellendi. VS 2026 / MSVC v145 gerekir |
| ~~VS2026 varsayılan MSVC 14.51 ile UE 5.8 derlenmiyor~~ | `hash_map` eksik → `nvtesslib` derleme hatası | ✅ | Çözüldü: MSVC 14.50.35717 bileşeni kuruldu + `BuildConfiguration.xml`'de sabitlendi. **Bu makineye özel, sistem-geneli bir ayar — yeni bir geliştirici makinesi kurulumunda tekrarlanmalı** (bkz. README) |
| Git LFS kurulmadı | .uasset'ler repo'yu şişirir | ⚠️ **ACİLLEŞTİ** | Content/ altında artık 11 gerçek binary var (4 IA + IMC + 2 BP + TestPlayer.umap + DT_Items + DT_Recipes + DT_Buildings) ve hiçbiri commit'lenmedi. Herhangi bir `git add` ÖNCESİ: `git lfs install && git lfs track "*.uasset" "*.umap"` → `.gitattributes`'ı da commit'le |
| CI için self-hosted runner yok | Otomatik build çalışmaz | ⬜ | Hafta 2'de runner kur veya CI'ı manuel tetiklemeli yap. Runner makinesinde de MSVC 14.50 pini + NetFxSDK gerekecek |
