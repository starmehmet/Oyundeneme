# YOL HARİTASI — 12 Hafta → Alpha, 16 Hafta → Steam

> İlerleme işaretleri [ILERLEME.md](ILERLEME.md) dosyasında tutulur. Bu dosya plandır, değişirse burayı güncelle.

## Hafta 1–2 · Temel (Sistemler 1–5)
- [x] UE 5.8 projesi açılıyor, C++ modülü derleniyor (VS 2026 / MSVC **14.50** pinli — bkz. ADR) *(2026-07-21)*
- [x] Git + Git LFS + ilk commit — `git lfs track "*.uasset" "*.umap"` kuruldu, 247 dosya (14 .uasset/.umap LFS üzerinden) commit edilip `https://github.com/starmehmet/Oyundeneme`'ye push edildi *(2026-07-25)*
- [x] Sistem 1: Zaman (TimeKeeper, DayNightCycle) — 3/3 test + PIE'de canlı güneş dönüşü doğrulandı *(2026-07-21)*
- [x] Sistem 2: Oyuncu kontrolü (Enhanced Input, üçüncü şahıs kamera) — 4/4 test + PIE canlı WASD/fare doğrulaması *(2026-07-23)*
- [x] Sistem 3: Etkileşim (trace kanalı, IInteractableInterface) — 7/7 test + PIE canlı doğrulama (`InteractionCount` 0→94) *(2026-07-23)*
- [x] Sistem 4: Envanter (slot, ağırlık, transfer) — 11/11 test + PIE canlı doğrulama (konteyner 7→0, oyuncu 0→7) *(2026-07-23)*
- [x] Sistem 5: Öğe veritabanı (DT_Items: Odun/Balta/Taş, cache) — sıralama Sistem 4'ten ÖNCEye alındı (bağımlılık) *(2026-07-23)*
- [x] Dev konsol komutları: `time_sethour`, `time_settimescale`, `time_dump` *(2026-07-21)*
- [x] Test haritası: `Content/Maps/Tests/TestPlayer.umap` (şablondan; zemin + PlayerStart + 'Sun' ışık + GameMode override). Sistem 3 için `TestInteraction.umap` ayrıca gelecek *(2026-07-23)*

## Hafta 3–4 · Dikey Dilim (Sistemler 6–10)
- [x] Sistem 6: Zanaat (DT_Recipes, kuyruk) — 13/13 test + PIE canlı doğrulama (`craft_start` → ağırlık 7.5→3.5→5.5) *(2026-07-23)*
- [x] Sistem 7: İnşaat (FoundationGrid, ghost, yıkım) — 15/15 test + PIE canlı doğrulama (`build_place`→başarılı→`build_demolish`→hücre boşaldı→`build_place` tekrar başarılı) *(2026-07-24)*
- [x] Sistem 8: Lojistik (node kaydı, rota önbelleği, taşıma kuyruğu, teslimat) — 18/18 test + PIE canlı doğrulama (`logistics_request` → 4sn sonra teslimat, mesafe/hız doğru) *(2026-07-24)*
- [x] Sistem 9: Üretim makineleri (giriş→çıktı döngüsü, bloke durumları, yakıt, kare bölümleme) — 21/21 test + PIE canlı doğrulama (10 döngü, tam 3.0sn arayla, `Blocked_NoInput` geçişi doğru) *(2026-07-24)*
- [x] Sistem 10: Kaynak simülasyonu (enerji bütçesi, frekans, kıtlık alarmı, termal bütçe) — 24/24 test + PIE canlı doğrulama (yakıt-kıtlığı → enerji-brownout → sebep-değişimi her iki yönde) *(2026-07-24)* — **Faz 1'in 10 sisteminin TAMAMI bitti**
- [x] **KİLOMETRE TAŞI: Oynanabilir döngü** — topla → üret → inşa et → otomatikleştir. Construction↔Logistics↔Production entegrasyonunun (1) ve (2) numaralı kısımları (2026-07-25) kod+derleme+test+PIE (iki oturumda) ile TAM doğrulandı — fırın `build_place` ile inşa edilebiliyor VE üretim arabellekleri lojistik ağına otomatik kayıtlı. (3) gerçek konveyör hâlâ bilerek kapsam dışı (yalnızca görsel, teslimat zaten fonksiyonel) — bkz. ILERLEME.md Faz 1 çıkış kriteri durum notu *(2026-07-25)*
- [x] Profiling ilk ölçüm: 60 FPS'te 100 bina + 50 makine — `profile_spawn_stress 100 50` + PIE canlı ölçüm: **59.93 FPS / 16.69ms** (Game=5.08ms, Draw=3.37ms, GPU=5.09ms), hedef karşılandı. İlk ölçümde (PIE penceresi sohbete geçilirken odaksız kalmıştı) editörün "Use Less CPU when in Background" throttle'ı 8-20 FPS gibi yanıltıcı değerler verdi — `stat unit` ile Game+Draw+GPU toplamının (~14ms) Frame süresinden (118ms) kopuk olduğu görülüp pencere odağı doğrulanarak düzeltildi; **ölçüm yöntemi notu: PIE penceresi OS-odaklı değilken `stat fps` güvenilmez**. Yan bulgu (düzeltilmedi, düşük öncelik): `WorldPartitionHelper::RebuildCellRegistry` 1sn'de bir kare-bölümlemesiz 3× `TActorIterator` yapıyor — şu ölçekte ucuz (motor içi `SetActorHiddenInGame`/`SetActorEnableCollision` zaten eşitlik korumalı) ama aktör sayısı çok büyürse gözden geçirilmeli *(2026-07-25)*

## Hafta 5–6 · Hava Simülasyonu (Sistemler 11–14)
- [x] Sistem 11: Hava durumu (9 durum, ağırlıklı-rastgele geçişler, 30sn periyot, broadcast) — 27/27 test + PIE canlı doğrulama (35+ dk, onlarca geçiş tam 30sn periyotla) *(2026-07-24)*
- [x] Sistem 12: Sıcaklık (bina içi/dışı, oyuncu hasarı) — ilk oyuncu-can sistemi (`UHealthComponent`) + 33/33 test + PIE canlı doğrulama; kritik denge hatası (sıradan havada evrensel ölüm) canlı testte bulunup düzeltildi *(2026-07-24)*
- [x] Sistem 13: Rüzgar (konumsal vektör, türbin, yapı yükü) — 36/36 test + PIE canlı doğrulama (spawn edilen türbin gerçek rüzgar hızından enerji üretti); inceleme sırasında kritik bir eksik-`virtual` bulgusu (ikisi de bağımsız mercek) yakalanıp düzeltildi *(2026-07-24)*
- [x] Sistem 14: Kar birikimi (global derinlik, erime, hareket cezası, inşaat engeli, çığ riski) — 39/39 test + PIE canlı doğrulama (`snow_set_depth 60` → erime/hareket-çarpanı/inşaat-engeli matematikle birebir tutarlı); inceleme sırasında bir bulgu (piyon-degisimine karsi bayat onbellek) proaktif olarak kapatıldı *(2026-07-24)*
- [x] **KİLOMETRE TAŞI: İlk kar fırtınası** — `weather_force 7` ile TEK bir canlı PIE oturumunda gerçek bir Blizzard'ın üç etkiyi AYNI ANDA tetiklediği KANITLANDI: hipotermi gerçek ve ölümcül (`temperature_dump`: can 67.8→0.0, `HealthComponent::TakeDamage` canlı çalıştı), rüzgar yüksek (`wind_dump`: küresel-hız 41-50 aralığı), kar birikimi sıfırdan büyüyerek pozitif (`snow_dump`: derinlik 0.8-1.0 — kısa maruziyet penceresi yüzünden küçük ama gerçek, sıfır değil). Üçü de AYNI `UWeatherSimulation::GetCurrentState()` çıkışından besleniyor, entegrasyon doğrulandı. Yan gözlem (davranış notu, hata değil): `weather_force`'un `TimeSinceLastEvaluation` sıfırlaması arka arkaya rastgele geçişi engelliyor ama 30sn'lik pencere manuel-komut-yazma süresiyle aşılınca doğal reroll yine devreye giriyor. **Yan etki**: oyuncu can=0 ile öldü, `OnDeath`'e kod tabanında SIFIR abone var (respawn/ölüm-ekranı henüz yazılmadı, dürüst kapsam dışı) — `load_game` ile canlandırma Sistem #17'nin gerçek kullanımı olarak "Tam döngü" kilometre taşına devrediliyor *(2026-07-25)*

## Hafta 7–8 · Yerleşim Hayatı (Sistemler 15–20)
- [x] Sistem 15: NPC AI (durum makinesi 6-durum, yorgunluk/moral, motor NavMesh ile yol bulma, `UHealthComponent` yeniden kullanımı) — 42/42 test + PIE canlı doğrulama (öncelik sırası + durum-hatırlama kanıtlandı; fiziksel yürüme nav-mesh-bake ortam sorunu yüzünden doğrulanamadı) *(2026-07-24)*
- [x] Sistem 16: Görev planlayıcı (öncelik+beceri+backoff filtrelemesi tek fonksiyonda, olay-güdümlü atama) — 44/44 test + PIE canlı doğrulama (kuyruk→atama→yürüme→çalışma→tamamlama zincirinin TAMAMI tek akışta kanıtlandı) *(2026-07-24)*
- [x] Sistem 17: Kaydet/Yükle (zlib sıkıştırma, sürüm-geçiş noktası, bozuk-kayıt koruması, yuva yönetimi, otomatik kaydetme) — 46/46 test + PIE canlı doğrulama (konum kaydedildi→değiştirildi→`load_game`→GERÇEKTEN geri döndü, tam disk-round-trip); inceleme motor kaynağından doğrulanan GERÇEK bir çökme riski (bozuk veride Fatal-log) buldu, kendi doğrulama zarfımla kapatıldı *(2026-07-24)*
- [x] Sistem 18: World Partition (hücrelere bölme, histerezisli uzaklık-tabanlı yükle/boşalt, gizle+collision-kapat+ilerletme-atla) — 48/48 test + PIE canlı doğrulama (hücre boşaltıldı→türbin gizlendi→geri dönünce hücre yüklendi→türbin göründü, tam round-trip); derleme sırasında motorun native `WorldPartitionSettings` dosya+sınıf adıyla çakışma keşfedilip yeniden adlandırılarak çözüldü *(2026-07-24)*
- [x] Sistem 19: Ses (kategori mikseri, 3D mesafe budaması, crossfade müzik) — 50/50 test + PIE canlı doğrulama (`audio_dump`/`audio_set_volume` canlı yayılım kanıtlandı, null-safety `audio_test_play`/`music_test_play` ile kanıtlandı); inceleme 4 bulgu buldu (motor `bAutoDestroy` yaşam-döngüsü riski dahil), dördü de düzeltildi *(2026-07-24)*
- [x] Sistem 20: UI (modal ekran yığını + giriş yönlendirmesi, kalıcı HUD widget'ları, envanter veri-bağlama) — 51/51 test + PIE canlı doğrulama (`ui_push_screen`/`ui_pop_screen` derinlik+giriş-modu round-trip, `hud_show_widget`/`hud_hide_widget` round-trip); inceleme kritik bir bulgu (kendi kapatan widget'ların IsValid() ile yakalanamaması) buldu, `OnNativeDestruct` olayına abone olunarak düzeltildi — **Faz 2'nin (Simülasyon Derinliği) 10 sisteminin TAMAMI bitti** *(2026-07-24)*
- [x] **KİLOMETRE TAŞI: Tam döngü** — TEK canlı PIE oturumunda tüm zincir EŞ ZAMANLI KANITLANDI: `task_enqueue`→NPC atandı→yürüdü/çalıştı (yorgunluk 0→7.3)→görevi tamamladı; araya `save_game`(2221 bayt sıkıştırılmış)+`load_game` round-trip'i girdi; `npc_dump` NPC durumunun save/load'dan ETKİLENMEDİĞİNİ kanıtladı (Sistem #17'nin "yalnız Zaman+Oyuncu kaydediliyor" sözleşmesiyle tutarlı); ardından `audio_dump` (canlı kategori mikseri) + `ui_push_screen`/`hud_show_widget` (viewport giriş modu doğru değişti) çalıştı, son `npc_dump` NPC durumunun bunlardan da etkilenmediğini gösterdi — gerçek eş zamanlı bir arada çalışma. Not: Faz 2'nin daha geniş "NPC kar fırtınasında içeri kaçar" çıkış kriteri BU kapsamda değil, ayrı ve test edilmedi *(2026-07-25)*

## Hafta 9–10 · Üretim Sertleştirme (Sistemler 21–28)
- [x] Sistem 22: Dev araçları (`profile_spawn_stress`, `logistics_visualize`) — `weather_set`/`time_scale` mevcut komutlarla (Sistem #1/#11) karşılandı, ayrı yazılmadı *(2026-07-25)*
- [x] Sistem 23: Hata işleme + crash reporter — MIMARI'nin `FGameLogger`/`FErrorHandler`/`FStructuredLogger`/`FCrashReporter` taslağı bilinçli yazılmadı (UE_LOG+LogSurvival* ile çakışırdı / olmayan sistemlere hizmet ederdi / backend gerektirirdi), UE'nin kendi `CrashReportClient`'i zaten aktif; onun yerine whole-codebase çökme-güvenliği denetimi yapıldı (sıfır gerçek bulgu) *(2026-07-25)*
- [x] Sistem 25: Performans regresyon testleri — `SURVIVAL_SCOPED_TIMER` + `Docs/PROFILING.md` (haftalık checklist + regresyon tablosu) *(2026-07-25)*
- [x] Sistem 24: Content pipeline — `content_validate` (yalnız editor), `/Game` altını CLAUDE.md isimlendirme kurallarına göre denetler; FBX/PSD import-otomasyonu bilinçli yazılmadı (gerçek 3D içerik yok) *(2026-07-25)*
- [ ] Sistem 26: CI/CD tam otomasyonu — workflow dosyası sağlamlaştırıldı (`index.json` doğrulaması, ölü `-TestExit` temizlendi), self-hosted runner henüz KURULMADI (operatöre soruldu, bilinçli olarak ertelendi) *(2026-07-26)*
- [x] Sistem 27: Denge geçişi #1 (ekonomi + zorluk eğrisi) — hipotermi mevcut haliyle onaylandı, üretim/zanaat süresi yavaşlatıldı (`OdunIsleme` 3→15sn, `Balta_Tarifi` 2→8sn), ham madde maliyeti değiştirilmedi (zaten bol) *(2026-07-25)*
- [x] İçerik: 30+ öğe, 20+ tarif, 10+ bina, 5+ makine — `DT_Items` +30 (33 toplam), `DT_Recipes` +19 (20 toplam), `DT_ProductionRecipes` +5 (6 toplam), `DT_Buildings` +10 (11 toplam, 6'sı `ProductionMachine`) eklendi, kaydedildi, referans bütünlüğü elle doğrulandı (hiçbir tarif/bina var olmayan bir ItemID'ye işaret etmiyor). Uçtan uca PIE oynanış testi YAPILAMADI — bkz. ADR tablosu: dünyada hiçbir ham madde için toplama/hasat mekanizması yok *(2026-07-26)*
- [ ] (Opsiyonel) Sistem 21: Multiplayer temeli
- [x] **Sistem 29: Hasat Düğümleri (Harvest Nodes) — TAMAMLANDI, Faz 1 dikey-dilim borcu kapatıldı.** Tasarım (4 ajanlı panel) → kodlama → 2-mercek inceleme (4 bulgu, hepsi düzeltildi) → derleme+test (54/54) → PIE'de canlı uçtan-uca doğrulama (topla→tüket→gizle/collision-kapat→60sn'de otomatik yeniden-doğma, hepsi kanıtlandı). Ayrıca `TimeKeeper::Tick`'in DeltaTime'ı kelepçelemediği (donma sonrası saat sıçraması) bulgusu bu testte keşfedilip düzeltildi *(2026-07-26)*

## Hafta 11–12 · Alpha
- [ ] QA turu: tüm entegrasyon testleri + 2 saatlik oynama seansı hatasız
- [ ] Performans: 500 bina/makine sahnesinde < 16 ms frame
- [ ] Bellek: < 8 GB, kayıt dosyası < 100 MB
- [ ] Denge geçişi #2
- [ ] **KİLOMETRE TAŞI: Alpha build** — arkadaş/kapalı test dağıtımı

## Hafta 13–16 · Beta → Steam
- [ ] Sistem 28: Steamworks (başarımlar, cloud save)
- [ ] Yerelleştirme: TR + EN (minimum)
- [ ] Store sayfası: görseller, fragman, açıklama
- [ ] Beta geri bildirim döngüsü (2 hafta)
- [ ] Yayın checklist'i ([MIMARI.md](MIMARI.md) bölüm 28)
- [ ] **KİLOMETRE TAŞI: Steam Early Access / Launch**

---

## KARAR KAYDI (ADR — Architecture Decision Record)

> Önemli teknik kararları buraya ekle ki "neden böyleydi?" sorusu 6 ay sonra cevaplanabilsin.

| Tarih | Karar | Gerekçe |
|-------|-------|---------|
| 2026-07-21 | ~~UE 5.4~~ → **UE 5.8** (operatör düzeltmesi), C++ modül adı `SurvivalGame` | Kullanıcının motoru 5.8; Target.cs `BuildSettingsVersion.V7` + `EngineIncludeOrderVersion.Unreal5_8`, VS 2026 / MSVC v145 toolset şart (web'den doğrulandı) |
| 2026-07-21 | Deterministik simülasyon çekirdeği (üretim/lojistik/kaynak) subsystem'lerde, görsellik Actor'larda | Test edilebilirlik + ileride multiplayer sunucu-otoritesi |
| 2026-07-21 | Tüm denge verisi DataTable/DataAsset'te, kodda sabit yok | Tasarımcı iterasyonu kod değişikliği gerektirmesin |
| 2026-07-21 | ECC_GameTraceChannel1=Interaction, ECC_GameTraceChannel2=ConstructionPlacement | Kanal çakışmalarını baştan sabitle |
| 2026-07-21 | Zaman delegate'leri (dakika/saat/gün) TimeKeeper'da; DayNightCycle yalnızca sunrise/sunset + ışık sahibi (MIMARI.md'de OnTimeChanged DayNightCycle'daydı) | Tek sahip ilkesi: zamanın kaynağı kim ise devrilme olaylarını o yayınlar; çift yayın/çift abonelik riski kalkar |
| 2026-07-21 | DayNightCycle TimeKeeper'ı delegate ile değil her tick poll ederek okur | Subsystem init sırası bağımsızlığı; world-level sistemin instance-level sisteme abonelik yaşam döngüsü riski yok |
| 2026-07-21 | Sistem ayarları UDeveloperSettings ile (UTimeSettings) — DataAsset değil | Project Settings UI + DefaultGame.ini'de diff'lenebilir config; DataAsset'ler oyun İÇERİĞİ için saklanıyor |
| 2026-07-21 | Güneş rotasyonu MUTLAK yazılır (`SetActorRotation(FRotator(Pitch, SunYaw, 0))`), asla GetActorRotation ile geri okunup değiştirilmez | `FQuat::Rotator()` pitch'i [-90,+90]'a normalize eder; oku-değiştir-yaz deseni -90 sonrası her karede aynalanıp güneşi titretir (inceleme bulgusu, doğrulandı) |
| 2026-07-21 | Saat kapısı: zaman yalnızca `StartClock()` sonrası akar; `ASurvivalGameMode` StartPlay/EndPlay'de açıp kapar, menü haritası düz AGameModeBase kullanmalı | Menüde saat ilerlemesin + "menüye dön → yeni oyun"da `ResetForNewGame()` seam'i olsun (inceleme bulgusu) |
| 2026-07-21 | Zaman delegate sözleşmesi: sıçramalarda ara devrilmeler ATLANIR; durum-güdümlü dinleyiciler `OnDayNightStateChanged`/payload kullanır, kenar saymaz | Kenar-karşılaştırmalı broadcast koalesans yapar; sözleşme header'larda belgelendi (inceleme bulgusu) |
| 2026-07-21 | Kaynak dosyalar UTF-8 with BOM (`Tools/add-bom.ps1`) | BOM'suz UTF-8'i MSVC Türkçe Windows'ta cp1254 okur → TEXT() literalleri mojibake (inceleme bulgusu) |
| 2026-07-21 | Geliştirici makinesinde MSVC **14.50** araç zinciri zorunlu (14.51 DEĞİL), `BuildConfiguration.xml`'de `CompilerVersion` ile sabitlenmiş | VS2026'nın güncel/varsayılan toolset'i 14.51+; UE 5.8 bununla derlenmiyor (`<hash_map>` kaldırılmış, `nvtesslib` kırılıyor). Epic'in düzeltmesi (`std::unordered_map`, CL54453441) 5.8 release dalına backport edilmedi. Ampirik olarak doğrulandı: derleme + 3/3 test PASSED (bkz. ILERLEME.md günlüğü, README kurulum bölümü) |
| 2026-07-21 | Otomasyon testlerinde `EAutomationTestFlags_ApplicationContextMask` (serbest sabit) kullanılır, `EAutomationTestFlags::ApplicationContextMask` (üye) DEĞİL | UE 5.8'de `EAutomationTestFlags` enum class'a taşınmış; `ApplicationContextMask` üye olmaktan çıkıp bağımsız `constexpr` sabite dönüşmüş (`AutomationTest.h`). Derleme hatasından ampirik olarak keşfedildi — gelecekteki tüm `IMPLEMENT_*_AUTOMATION_TEST` çağrılarında bu söz dizimini kullan |
| 2026-07-21 | Sistem #2'de MIMARI.md'deki `UInputHandler` (16-frame circular buffer) YAZILMADI | `APawn::AddMovementInput` → `ControlInputVector` → `ConsumeMovementInputVector` zinciri zaten frame-güvenli giriş biriktirme+tüketim yapıyor; paralel bir buffer katmanı kod tekrarı olurdu ("motorda varsa yeniden yazma" kuralı) |
| 2026-07-23 | Sistem #4/#5 sırası MIMARI.md'den DÜZELTİLDİ: önce Öğe Veritabanı, sonra Envanter yazıldı | Envanterin slot verisi (`FInventorySlot.ItemID`) zaten `UItemDatabase`'e bağımlı — tersten inşa edilemez. Aynı sınıf hata: dokümantasyon build-sırasını değil kavramsal anlatım sırasını yansıtıyordu |
| 2026-07-23 | `FInventorySlot` tam bir `UItemInstance` UObject'i DEĞİL — yalnızca `FName ItemID` + `Count` + `Durability`; tanım verisi (ağırlık, max yığın) her zaman ID ile `UItemDatabase`'den çözülür, slotta kopyalanmaz | Tek doğruluk kaynağı ilkesi + "gerekmedikçe soyutlama ekleme" — per-item UObject'in tek gerekçesi (bağımsız dayanıklılık) tek bir `float Durability` alanıyla karşılanıyor |
| 2026-07-23 | `UContainerComponent` yerine `AContainerActor` (kendi `UInventoryComponent`'i + `IInteractableInterface`, "hepsini topla" + `StartingItems`) | UI sistemi (#20) gelene kadar konteyner içeriğini seçip aktarma akışı yok; bu, `TransferItemTo`'yu PIE'de gerçek bir sahne nesnesiyle sınayan en küçük anlamlı içerik |
| 2026-07-23 | `ACraftingStation` (istasyon-yakınlık kısıtlaması) YAZILMADI — DoD'de yok; `WorkstationType` alanı DT_Recipes'te veri olarak duruyor, `CraftingComponent` bunu ZORUNLU KILMIYOR (her yerde craft edilebilir) | Sistem #2'deki `UInputHandler` kararıyla aynı sınıf: gerekmeyen içeriği şimdiden inşa etme. Gelecekteki üretim/inşaat sistemleri bu alanı tüketebilir |
| 2026-07-23 | Tarif satırlarında AYNI ItemID birden fazla kez geçebilir ({Wood,5}+{Wood,3}) — `FRecipeDefinition::GetAggregatedIngredients()` bunu TOPLAR, `CanCraftRecipe`/`StartCrafting`/`CancelCrafting` HEP bu toplu haritayı kullanır, satır-satır değil | İnceleme bulgusu: satır-satır bağımsız kontrol, tekrarlanan ItemID'de (yaygın bir DataTable-yazım hatası) gerçek maliyeti olduğundan düşük gösterip malzeme hırsızlığına yol açıyordu |
| 2026-07-23 | `StartCrafting` ATOMİK: `RemoveItem`'ın her çağrısının dönüşü kontrol edilir, herhangi bir kalem eksik çıkarsa o ana kadar tüketilenler GERİ EKLENİR, iş kuyruğa asla yarım girmez | İnceleme bulgusu: eskiden `RemoveItem`'ın dönüşü hiç okunmuyordu — tekrarlanan-ItemID hatasıyla birleşince oyuncu tarifin gerçek maliyetinden azını ödeyebiliyordu |
| 2026-07-23 | Editör "yeniden başlat" komutlarında process kapanışını `CloseMainWindow` sonrası MUTLAKA doğrula (process listesini kontrol et), yalnızca komutun "kapandı" yazdırmasına güvenme | Sessiz operasyonel hata: iki editör süreci aynı anda çalıştı, bir MCP güncellemesi eski (yanıt vermeyen) sürece gidip kayboldu — kök neden CDO'yu doğrudan sorgulayarak teşhis edildi. Kural: "editör kapandı" iddiası her zaman `Get-Process` ile doğrulanmalı |
| 2026-07-23 | Küçük-modda inceleme (2 mercek, yalnız ciddi bulgular doğrulanır) Sistem #4/#5'te ~437k token — Sistem 1-3'ün tam-modlarına (521k-1.05M) göre belirgin ucuz | ADR kararının (2026-07-23, bütçe dersi) ilk somut sonucu — karar işe yaradığı doğrulandı, bundan sonraki sistemlerde varsayılan bu kalacak |
| 2026-07-21 | Kamera sahipliği ayrımı: SpringArm/Camera bileşenleri `APlayerCharacter`'ın alt nesneleri, `UCameraManager` yalnızca DAVRANIŞ yönetir (`RegisterCameraRig` ile bağlanır) | Attachment hiyerarşisi `USceneComponent` gerektirir, `UActorComponent` sahnede konumlanamaz; davranış/sahne ayrımı test edilebilirliği korur (`CameraMath` saf) |
| 2026-07-21 | Enhanced Input varlık referansları `EditDefaultsOnly` UPROPERTY — kodda `NewObject` ile runtime üretim YOK | Varlıklar (`IA_*`, `IMC_Default`) editörde oluşturulup `BP_PlayerController`'da atanır: oyuncu tuş-atama (rebinding) desteği, tasarımcı iterasyonu ve UE konvansiyonu bunu gerektirir |
| 2026-07-23 | **IMC eşlemeleri `DefaultKeyMappings.Mappings`'e yazılır — `Mappings` dizisine ASLA** | UE 5.8: `InputMappingContext.Mappings` 5.7'de deprecated; runtime `GetMappings()` yalnızca `DefaultKeyMappings.Mappings` döndürüyor (motor kaynağından doğrulandı). Deprecated diziye yazılan eşlemeler sessizce yok sayılır — "her şey bağlı ama girdi çalışmıyor" olarak kendini gösterdi; PIE canlı testiyle çözüm doğrulandı |
| 2026-07-23 | Etkileşim menzili aktör ORİJİNİNDEN ölçülür (yüzeyden değil); büyük etkileşilebilirler `GetInteractionDistance`'ı orijin→yüzey farkını kapsayacak şekilde override ETMELİDİR | Prop ölçeğinde doğru ve ucuz; makine ölçeğinde (Sistem #9) yanlış-negatif üretir: 6 m'lik makinenin yüzeyine değen oyuncu orijine 300 UU uzakta olup 250 UU menzile takılır. Alternatif (Hit.ImpactPoint + `ActorGetDistanceToCollision`) iki çağrı yerinde farklı ölçüm yapardı; tek kural + override daha öngörülebilir. **İlk makine-ölçekli etkileşilebilirde yeniden değerlendir** (inceleme bulgusu) |
| 2026-07-23 | **Çok-ajanlı inceleme varsayılan olarak KÜÇÜK koşulur** (2 mercek, doğrulama yalnız compile-error/runtime-bug bulgularına); tam 3-mercek+tüm-bulgu sürümü yalnızca kritik/riskli sistemlerde | Sistem 1–3 incelemeleri ~2.1M ajan tokeni yaktı ve aylık harcama limitine çarpıldı (2026-07-23). Bulgu kalitesi değerliydi ama minor bulguların adversarial doğrulaması maliyetin çoğunu oluşturuyor; derleyici + otomasyon testleri zaten ilk savunma hattı |
| 2026-07-23 | Editör otomasyonu: Epic'in `ModelContextProtocol` plugin'i (proje `.mcp.json` → `http://127.0.0.1:8000/mcp`) + `EditorToolset` araç setleri; sunucu `bAutoStartServer` ile otomatik kalkar | Varlık oluşturma/özellik atama/PIE kontrolü/log okuma programatik yapılabiliyor; el ile editör adımı darboğazı kalktı. Araç çağrı deseni: `Tools/` altında değil, scratchpad `mcp-call.ps1`/`ue-tool.ps1` (oturuma özel). Kısıt: `set_properties` diziyi büyütürken mevcut elemanı değiştiremez (önce reset, sonra tek çağrıda tam liste); instanced alt-nesne = düz class-path string |
| 2026-07-24 | Sistem #7: MIMARI.md'deki `AConstructionManager`(GameStateBase)+`AFoundationGrid`(AActor) ikilisi YAZILMADI — tek bir `UFoundationGrid` (`UWorldSubsystem`) | CLAUDE.md kuralı "Singleton = UGameInstanceSubsystem, elle yönetilen static manager yazma" World-kapsamlı hâliyle uygulandı: level'a manuel bir grid/manager aktörü yerleştirmeye gerek kalmaz, dünya var olur olmaz grid de var olur (Sistem #1'deki TimeKeeper/DayNightCycle desenine paralel) |
| 2026-07-24 | Çarpışma doğrulaması ızgara hücre işgaline (TSet, O(1)) dayanır — fiziksel overlap/box sorgusu DEĞİL | MIMARI.md'nin kendi "Ölçeklenebilirlik Riski" notu bunu 500 bina için ÇÖZÜM olarak öneriyor (O(n) çarpışma kutusu yerine O(1) TSet arama); DoD'nin "çarpışma doğrulama" maddesi bu şekilde okundu |
| 2026-07-24 | `FBuildingDefinition::RequiredMaterials`, MIMARI.md'deki `TArray<FInventorySlot>` YERİNE Crafting'in `TArray<FRecipeIngredient>`'ı kullanır | `FInventorySlot` çalışma-zamanı slot durumu (Durability dahil) taşır — bir "gereksinim" için anlamsız; `FRecipeIngredient` (ItemID+Count) zaten var olan, tam olarak istenen şey. Sistem #4/#5 sıralama düzeltmesiyle aynı sınıf hata: MIMARI bazen yanlış tip/sıra kodluyor |
| 2026-07-24 | İnşaat ANLIK tamamlanır — iskele/zamanla-inşa (`AcceptConstruction`/`ConstructionProgress` tick'i) YAZILMADI; `ABuildingBase::BeginConstruction` çağrıldığı an `bIsConstructed=true` | DoD'de zaman kavramı yok; zamanla-inşa `CraftingComponent`'in `FCraftingJob`/tick desenini neredeyse birebir kopyalardı (kod tekrarı). Sistem #6'daki `ACraftingStation` kapsam-dışı kararına paralel. `FBuildingDefinition::ConstructionTime` veri olarak duruyor, gelecekte tüketilebilir |
| 2026-07-24 | Ghost (`AConstructionGhost`) kırmızı/yeşil dinamik materyal renk-geri bildirimi YAZILMADI — yalnızca `bIsValid` bool (BlueprintReadOnly, MCP/PIE doğrulaması için) | Henüz materyal/mesh içeriği yok (Sistem #24 content pipeline bekliyor); Sistem #3'teki `InteractionCount` desenine paralel — veri seviyesinde test edilebilirlik yeterli, görsel geri bildirim içerik fazına ertelendi |
| 2026-07-24 | `ABuildingBase` yıkımı E-etkileşimine BAĞLANMADI (`IInteractableInterface` uygulamıyor) — yalnızca `Demolish()` public API + `build_demolish` konsol komutu (dev-tool, `craft_start` ile aynı desen) | Kazayla bina yıkma riski (oyuncu binaya yürüyüp yanlışlıkla E'ye basar) + gerçek yıkım UX'i (özel araç/mod) henüz tasarlanmadı; DoD'nin "yıkım" maddesi mekanizma-kanıtı istiyor, UX kararı değil |
| 2026-07-24 | `ABuildingBase` `UCLASS(NotPlaceable)` — editörde Place Actors panelinden elle sürüklenemez | İnceleme bulgusu: `GridCoord` yalnızca `BeginConstruction` çağrılınca anlamlı değer alır; elle yerleştirilmiş (BeginConstruction hiç çağrılmamış) bir örnekte `Demolish()` varsayılan `GridCoord`=(0,0)'ı grid'den siler ve o hücrede GERÇEKTEN duran başka bir binanın kaydını bozar. Bir bayrakla etrafını sarmak yerine kök nedeni (elle yerleştirme yolu) ortadan kaldırdım — `AProductionMachine` (Sistem #9) bu sınıftan türeyince aynı korumayı miras alır |
| 2026-07-24 | `TSoftObjectPtr<UStaticMesh>::LoadSynchronous()` çağrılan her yerde `Engine/StaticMesh.h` include'u EXPLICIT eklenir — ileri-bildirim + SharedPCH'e güvenilmez | İnceleme: iki bağımsız doğrulama ajanı, aynı örüntüdeki (`BuildingBase.cpp` vs `ConstructionComponent.cpp`) iki çağrı noktası için birbirine ZIT sonuca vardı (biri "SharedPCH `/FI` ile zaten tam tip sağlanıyor, derleme hatası yok" dedi, diğeri "gerçek derleme hatası" dedi) — ikisi de motor/UBT kaynağından kanıt gösterdi. PCH-seçim detaylarına bağımlı, kırılgan bir doğruluk yerine `BuildingDefinition.h`'a tek satırlık explicit include eklemek belirsizliği tamamen ortadan kaldırdı (Item/RecipeDatabase'in `Engine/DataTable.h`'ı zaten explicit include ettiği disipline paralel) |
| 2026-07-24 | `TestEqual` çağrılarında `FVector` bileşenleri (`double`, UE5 LWC) her zaman `double` literalle karşılaştırılır, `float` (`800.0f`) ile DEĞİL | Derleme hatası (C2666, aşırı yükleme belirsizliği): `TestEqual(FString, double, double, double)` ile `TestEqual(FString, float, float, float)` arasında `(double, float)` karışık argüman listesi ikisine de tam eşleşmiyor. `EAutomationTestFlags_ApplicationContextMask` dersiyle aynı sınıf: UE5.8 API'sinin gerçek imzasını varsaymak yerine derleyiciden öğren |
| 2026-07-24 | DataTable'da `TSoftObjectPtr<UStaticMesh>` sütunu MCP `DataTableTools.set_rows` üzerinden YAZILAMIYOR (3 refPath formatı da sessizce "None" kalıyor, hata dönmüyor) — bina mesh'i şimdilik atanmadı | Bilinen bir MCP-köprüsü tuhaflığı (skill'e `references/mcp-editor-automation.md`'ye not düşüldü); kozmetik alan, DoD'yi (grid/malzeme/yıkım mantığı) etkilemiyor. Gerekirse editör UI'ından elle atanabilir |
| 2026-07-24 | Sistem #9: `FProductionRecipe`, MIMARI.md'deki `AProductionMachine::AvailableRecipes` (İNLİNE `TArray<FProductionRecipe>`) taslağı YERİNE DataTable+`UProductionRecipeDatabase` (Item/Recipe/BuildingDatabase ile aynı desen) | "Veri tabanlı içerik... asla koda gömülmez" (CLAUDE.md); `AProductionMachine` yalnızca hangi RecipeID'leri desteklediğini (`TArray<FName>`) tutar, gerçek veri her zaman veritabanından çözülür — tek-doğruluk-kaynağı, Sistem #5/#6/#7'nin aynı deseniyle tutarlı |
| 2026-07-24 | MIMARI'nin iki-adımlı `SetActiveRecipe`+`StartProduction` API'si TEK `SetActiveRecipe(RecipeID)` adımına indirgendi — üretim, kaynaklar elverdiği sürece Tick üzerinden kendiliğinden başlar/durur | Henüz bir tarif-seçim UI'ı (Sistem #20) yok; iki-adımlı akışın tek gerekçesi "seç ama henüz başlatma" UX'i, şu an karşılığı olmayan bir ayrım. Gerçek ihtiyaç ortaya çıkınca eklenebilir (yıkıcı olmayan bir API genişlemesi) |
| 2026-07-24 | `UProductionManager` (`UWorldSubsystem`+`FTickableGameObject`) "kare bölümleme"yi (CLAUDE.md: "500 makine / 60 frame; sınırsız per-frame O(n) döngü ekleme") ilk kez GERÇEK bir mekanizmaya döktü — `AProductionMachine`'in kendi `PrimaryActorTick`'i YOK, `SurvivalProduction::ComputeBatchSize` ile hesaplanan bir dilim round-robin güncellenir | Yüzlerce makine sahnede olduğunda her birini her karede güncellemek CPU'yu boğar; round-robin + her makinenin kendi `LastProductionUpdateTime`'ı (gerçek geçen süre muhasebesi) CPU maliyetini dağıtırken üretim matematiğini kare-oranından bağımsız tutar |
| 2026-07-24 | `AProductionMachine`, `ABuildingBase`'den türüyor (MIMARI hiyerarşisi, `AProductionMachine : ABuildingBase`) ama BU PASODA `UConstructionComponent`/`build_place` akışına BAĞLANMADI | DoD üretim SİMÜLASYONUNU istiyor, yerleştirme UX'ini değil; Sistem #7'nin `FBuildingDefinition::BuildingClass` genişleme noktası (o zaman bilinçli olarak boş bırakılmıştı) bu entegrasyon için hazır — gerçek ihtiyaç (oyuncunun makine seçip yerleştirmesi) ortaya çıkınca, Sistem #7'nin `ConstructionComponent::ConfirmPlacement`'ını (şu an hep temel `ABuildingBase` spawn ediyor) değiştirmeden dokunmadan kullanılabilir |
| 2026-07-24 | `UInventoryComponent::HasRoomFor`/`HasRoomForBatch` (yeni, additive) — mutasyonsuz, YALNIZCA ağırlık bütçesine bakan kapasite tahmini; `AddItem`'in tam slot/yığın mantığını birebir simüle ETMEZ | Sistem #4'ün test edilmiş `InventoryComponent`'ine dokunmadan (yalnızca yeni sorgu metodu eklenerek) Üretim'in çıktı-bloke durumunu gerçekten `AddItem` çağırmadan ÖNCEDEN değerlendirmesini sağlar. Nadir kenar durumda ("ağırlık uygun ama slotlar dolu") yanlış-pozitif verebilir — çağıranlar zaten `AddItem`'in dönüşünü kontrol edip eksik kabulü loglar (StartCrafting/ConfirmPlacement/RequestTransport'taki desen) |
| 2026-07-24 | Sistem #10: `UResourceSimulation` PROAKTİF olarak Sistem #7/#8/#9'da tekrar tekrar bulunan "aktör yok edildi ama kaydı kalıcı kaldı" hata sınıfına karşı BAŞTAN savunmalı yazıldı — `PruneStaleRegistrations()` her `Tick`'in başında `TWeakObjectPtr` anahtarlı üretici/tüketici kayıtlarını temizler | Aynı hata sınıfı üç sistemde ard arda inceleme tarafından bulunup düzeltildi (AStorageNode, FoundationGrid dolaylı, ProductionManager); dördüncü kez bulunmasını beklemek yerine desen baştan uygulandı — küçük-mod inceleme bu savunmayı ayrıca sorguladı, ek bulgu çıkmadı (yatırım işe yaradı) |
| 2026-07-24 | `FEnergyBudget::Frequency` ANLIK türetilir (üretim/tüketim oranından, hafızasız) ama `FThermalBudget::CurrentTemperature` BİRİKİMLİDİR (ısı üretim/dağıtım dengesi zamanla sıcaklığı sürükler) | Bilinçli bir asimetri, tutarsızlık değil: gerçek elektrik şebekelerinde frekans yüke neredeyse anında tepki verir (önemli "atalet"i yok), termal sistemlerde gerçek kütle/atalet var — model bu fiziksel farkı yansıtıyor |
| 2026-07-24 | Isı DAĞITIMI için ayrı bir "soğutucu" aktör kaydı YOK — sabit `UResourceSimulationSettings::BaselineHeatDissipation` kullanılıyor; enerji ise TAM üretici/tüketici kaydına sahip | Henüz radyatör/soğutucu içeriği yok (Sistem #9 Üretim Makineleri zaten `EnergyPerSecond` ile enerji tüketici içeriği üretiyor, simetrik bir "ısı kaynağı" içeriği henüz yok) — gerçek ihtiyaç ortaya çıkınca (bir soğutucu bina) `RegisterHeatSink` eklenmesi yıkıcı olmayan bir genişleme |
| 2026-07-24 | Yakıt rezervleri (`FuelReserves`) enerji üretici/tüketici muhasebesinden BAĞIMSIZ — ikisi arasında "yakıt tüketip enerji üretmek" bağlantısı YOK | Gerçek bir jeneratör/üretici içeriği (yakıt yakıp enerji üreten bir bina) gerektirir, bu pasoda yok; iki paralel ama bağlantısız kaynak takibi DoD'nin "enerji bütçesi" + "kıtlık alarmı" maddelerini ayrı ayrı karşılıyor, gerçek ihtiyaç Sistem #9'un gelecekteki bir jeneratör makinesinde ortaya çıkabilir |
| 2026-07-24 | `AddFuel`, `AddItem`/`RequestTransport` ile aynı desende GERÇEKTEN kabul edilen miktarı (`float`) döner — `void` DEĞİL | İnceleme bulgusu: `void` iken `MaxAmount` belirtilmeden yapılan ikinci+ çağrılar sessizce kayboluyordu (rezerv ilk yatırılan miktara kilitleniyordu), çağıran tarafın kaybı fark etmesi imkansızdı — dönüş değeri artık bu codebase'in her yerinde kurallı olan "gerçekte ne oldu, sen karar ver" sözleşmesini takip ediyor |
| 2026-07-24 | Sistem #11: MIMARI'nin `AWeatherManager`(`AGameStateBase`)+`UWeatherSimulation`(subsystem) ikilisi TEK bir `UGameInstanceSubsystem`'e birleştirildi | Sistem #7 (`AFoundationGrid`), #9 (`UProductionManager`), #10 (`UResourceSimulation`) ile aynı "Singleton=Subsystem, elle yönetilen Actor/GameState manager yazma" kararının dördüncü ve artık standart uygulaması |
| 2026-07-24 | `OnWeatherStateChanged`, Sistem #10'un edge-triggered `OnScarcityStateChanged`'ının AKSİNE HER Tick'te (sürekli) yayınlanır | Kıtlık alarmı bir durum DEĞİŞİKLİĞİ olayıdır (ayrık), hava değerleri (sıcaklık, görüş mesafesi, rüzgar) sürekli-anlamlı bilgidir — `UCraftingComponent::OnCraftingProgress`'in her tick yayınlanmasıyla aynı gerekçe. İki farklı broadcast felsefesi, aynı codebase'de BİLİNÇLİ olarak yan yana duruyor |
| 2026-07-24 | `FWeatherProfile`, Item/Recipe/Building/ProductionRecipe'nin "DataTable satır ADI = kimlik" deseninden SAPTI — gerçek arama anahtarı satırın içindeki açık `Condition` (enum) alanı | Çağıranlar zaten string değil `EWeatherCondition` enum'uyla çalışıyor (yalnızca 9 sabit değer); satır adını enum'a round-trip etmek (ör. `GetNameStringByValue`) kırılgan bir ek adım olurdu — satır adı yalnızca insan-okunur etiket olarak kalır |
| 2026-07-24 | `LerpWeatherState`, `WindDirection` (birim vektör) için düz `FMath::Lerp` YERİNE `FVector::SlerpNormals` kullanır | İnceleme bulgusu: doğrusal enterpolasyon iki birim vektör arasında uzunluğu KORUMAZ — neredeyse-ters-yönlü bir çiftte Alpha≈0.5'te vektör sıfıra yaklaşıp yön tanımsızlaşırdı (her geçişte rastgele 360° seçilen yön düşünülünce nadir bir durum değil, genel durum). Motorun kendi birim-vektör-enterpolasyonu için sunduğu doğru araç kullanıldı |
| 2026-07-24 | `EvaluateWeather`'in "aynı durum → geçişe gerek yok" kısayolu artık `bHasInitializedState` bayrağıyla korunuyor | İnceleme bulgusu: struct-varsayılanı `TransitionProgress=1.0f` ("zaten tamamlanmış" anlamına geliyor) veritabanından hiç doldurulmamış gerçek durumdan AYIRT EDİLEMİYORDU — ilk değerlendirmede rastgele seçim varsayılan Clear'ı tekrar seçerse `CurrentState` kalıcı olarak sahte varsayılanlarda kilitlenirdi. Bayrak, "ilk değerlendirme HER ZAMAN gerçek bir geçiş başlatır" garantisini sağlıyor |
| 2026-07-24 | Periyodik değerlendirme (`EvaluateWeather`) yalnızca ÖNCEKİ geçiş tamamlandıktan (`TransitionProgress>=1.0`) SONRA çalışır | İnceleme bulgusu (küçük, varsayılan ayarlarda `TransitionDuration`<`EvaluationInterval` olduğu için erişilemez): geçiş SÜRERKEN yeni bir değerlendirme, `CurrentState.Condition`'ı (geçiş sürerken hâlâ ESKİ durum) yeniden seçip devam eden geçişi tersine çevirebilirdi. Geciken değerlendirme kaybolmaz — geçiş bitince bir sonraki Tick'te hemen çalışır |
| 2026-07-24 | Windows **Smart App Control** (Değerlendirme modunda) derlenmiş, imzasız `UnrealEditor-SurvivalGame.dll`'i "Uygulama Denetimi ilkesi" ile ENGELLEYEBİLİR — bu bir KOD hatası DEĞİL, işletim sistemi güvenlik ilkesi. Belirti: derleme `Result: Succeeded` ama editör/test-runner "'X' oyun modülü yüklenemedi" (`GetLastError=4551`) ile açılamıyor | Sistem #10'da ilk kez karşılaşıldı — 9 önceki sistem aynı makinede sorunsuz calıştı, bu makine-durumu bir noktada değişmiş (Windows güncellemesi/yeniden başlatma). Kod/derleme tarafında hiçbir şey değiştirilmedi; operatör Windows Güvenlik ayarını kontrol edip kapatınca AYNI derleme sorunsuz çalıştı. Kalıcı olarak `references/windows-toolchain.md`'ye not düşüldü (tanı: `LoadLibraryEx` + `Marshal.GetLastWin32Error` ile doğrudan hata metni okunarak teşhis edildi, "Uygulama Denetimi ilkesi bu dosyayı engelledi") |
| 2026-07-24 | MCP köprüsü oturumu Sistem #9'da ÜÇÜNCÜ kez bayatladı; bu sefer daha önce eklenen otomatik-iyileştirme (Sistem #8 ADR) TETİKLENMEDİ — kök neden teşhis edilemedi (stale-session hatası bir şekilde tespit edilemedi/farklı bir yolla başarısız oldu), elle önbellek silme ile atlatıldı. Ayrıca oturum ortasında `mcp__unreal-mcp__*` doğrudan araç entegrasyonu bağlantısı koptu (harici, oturumdan bağımsız bir olay), iş ham `ue-tool.ps1`/`mcp-call.ps1` script'leriyle sürdürüldü, araçlar sonradan kendiliğinden geri geldi | Otomatik-iyileştirmenin KENDİSİ güvenilir değil — "elle `.mcp-session-id` dosyasını sil" hâlâ tek güvenilir düzeltme. Kök neden ayrı bir oturumda incelenmeli (script'in stale-session regex'i mi eksik eşleşiyor, yoksa `Invoke-WebRequest`'in PS 5.1'deki hata-yakalama davranışı mı farklı) — şimdilik dokümante edilen iş-arounddan sapma |
| 2026-07-24 | Sistem #8: `AConveyorBelt`/`ATransportDrone` (fiziksel öğe hareketi/spline animasyonu) YAZILMADI — teslimat `ULogisticsSettings::TransportSpeed` sabitiyle mesafe/hız zamanlayıcısı kullanır | DoD'de fiziksel taşıma yok; `ACraftingStation`/`UInputHandler`/`AcceptConstruction` kapsam-dışı kararlarıyla aynı sınıf. PIE'de "2000 UU, 4.0sn" log satırı gerçek mesafe/hız hesabının çalıştığını kanıtladı — zamanlayıcı fiziksel değil ama gerçek |
| 2026-07-24 | `ULogisticsNetwork`'ün hedef önbelleği ("rota önbelleği") ÖĞE-bazlı DEĞİL, düz bir "kabul edebilecek düğümler" listesi — `CanAcceptItem` şu an yalnızca `NodeType`'a bakıyor | Hiçbir düğüm belirli bir ItemID'yi reddetmiyor (henüz); öğe-anahtarlı bir önbellek şu an hepsi AYNI kümeye eşleneceği için yanıltıcı olurdu. Öğe-bazlı filtreleme eklenince (ör. Sistem #9'da bir makine yalnızca belirli girdi kabul ederse) `CanAcceptItem` ItemID parametresini kullanmaya başlar, önbellek yapısı değişmez |
| 2026-07-24 | `ULogisticsNetwork::RequestTransport`, malzemeleri İSTEK anında kaynaktan HEMEN çıkarır (`UCraftingComponent::StartCrafting`'deki "hemen tüket" deseniyle aynı), hedefe yalnızca `TransportTime` dolunca ekler | "Transit'teki" bir öğe iki envanterden de HENÜZ sayılmaz — çifte-sayım riski yok. Sistem #6'nın zaten test edilmiş, incelemeden geçmiş desenini tekrar kullanmak, aynı problemi (zamanlı, atomik tüketimli bir kuyruk) sıfırdan çözmekten daha güvenli |
| 2026-07-24 | Teslimat sırasında hedef dolu/ağırsa kalan miktar KAYIP olur (kaynağa iade EDİLMEZ) | Gerçek "geri bas/bekle" (belt backup) davranışı veya alternatif hedef arama bu pasonun kapsamında değil — DoD'de yok, tasarım/denge kararı (ne zaman kayıp, ne zaman bekleme) ileride ele alınabilir |
| 2026-07-24 | `AStorageNode`, `AContainerActor`'dan (Sistem #4) BİLEREK AYRI tutuldu — ikisi de bir `UInventoryComponent` sarar ama farklı sorumlulukları var (oyuncu-etkileşimi vs. otomatik ağ tüketimi) | Çalışan, test edilmiş bir sınıfı (Sistem #4) spekülatif bir ihtiyaç için (bir konteynerin de ağa kayıtlı olması) şimdiden değiştirmek yerine, gerçek ihtiyaç Sistem #9'da (üretim makineleri girdi/çıktı arabellekleri) ortaya çıkınca birleştirme değerlendirilebilir |
| 2026-07-24 | **İnceleme bulgusu, iki sistemde tekrarlandı (Sistem #7 ve #8):** `AActor` alt sınıflarında SADECE `USceneComponent`-olmayan bileşenler (ör. `UInventoryComponent`, `UActorComponent` türevleri) varsa, kök bileşen atamayı UNUTMAK `GetActorLocation()`'ın sessizce `(0,0,0)` dönmesine yol açar — derleme hatası vermez, PIE'de de "çalışıyor gibi görünür" (yalnızca mesafe-bağımlı mantık bozulur). Yeni her `AActor` alt sınıfında EN AZ bir `USceneComponent` (görsel bir mesh ya da salt-konum bir `USceneComponent`) kök olarak atanmalı | İlk kez Sistem #7'de gözden kaçmadı (BuildingBase/ConstructionGhost ikisi de mesh kökü ile başladı), ama Sistem #8'de `AStorageNode` ilk yazımda atlandı ve inceleme onu yakaladı — motorun kendi kaynağından doğrulandı (`TemplateGetActorLocation`, `RootComponent==nullptr` ise `FVector::ZeroVector`). Skill'e (`references/ue5-api-traps.md`) kalıcı bir kural olarak eklenmeli |
| 2026-07-24 | MCP köprüsünün `mcp-call.ps1` script'i artık bayat oturum kimliğini KENDİ KENDİNE iyileştiriyor (hata mesajını tespit et → önbelleği temizle → yeniden el sıkış → çağrıyı bir kez daha dene) | Sistem #7 ve #8'de İKİ KEZ aynı hatayla karşılaşıldı: arka planda çalışan bir izleyici script'i, editör (config değişikliği için) yeniden başlatıldıktan SONRA eski bir oturum kimliğiyle çağrı yapmaya devam edip "Unknown session id ... client should reinitialize" hatası alıyordu. Elle önbellek temizleme yerine script'in kendisi düzeltildi — üçüncü kez aynı manuel adımı tekrarlamak yerine kök nedeni ortadan kaldır |
| 2026-07-24 | Sistem #18: Motorun GERÇEK native World Partition özelliği (harita-dönüştürme, HLOD, editör-öncelikli akış) YERİNE MIMARI'nin taslağına sadık hafif bir özel `UWorldPartitionHelper` yazıldı | Native World Partition, bu projenin mevcut tek-seviyeli, düz haritasını (17 önceki sistemin test içeriğini barındıran) kökten dönüştürmeyi gerektirirdi — ölçek/risk dengesizliği bu projenin "motoru yeniden kullan" eğiliminin AKSİNE, küçük/özel bir C++ sistemi yazmayı haklı çıkardı |
| 2026-07-24 | **Dosya+sınıf adı çakışması, derleme hatasından ampirik olarak keşfedildi:** UE 5.8'in `Engine/Public/WorldPartition/WorldPartitionSettings.h`'ı VE `UWorldPartitionSettings` sınıfı ZATEN var. Dosya `SurvivalWorldPartitionSettings.h`, sınıf `USurvivalWorldPartitionSettings` olarak yeniden adlandırıldı | UHT hem header DOSYA adının hem UCLASS SINIF adının motor+proje genelinde BENZERSİZ olmasını istiyor — ikisi ayrı ayrı hatalar olarak sırayla ortaya çıktı (önce dosya adı, düzeltilince sınıf adı). Gelecekte "Survival" öneki OLMADAN bir isim seçerken önce motor kaynağında (`grep -rn` ile) çakışma olup olmadığı kontrol edilmeli |
| 2026-07-24 | "Boşaltma" gerçek `Destroy()`/yeniden-`SpawnActor` DEĞİL — yalnızca `SetActorHiddenInGame`+`SetActorEnableCollision` + `UProductionManager`/`UNPCManager`'da ilerletmeyi atlama | DoD'nin "hücre durumunu koru" maddesini OTOMATİK karşılar (hiçbir şey yok edilmediği için geri-yükleyecek bir durum yok) — gerçek bellek-azaltan spawn/destroy akışı her sistemin kendi kaydet/geri-yükle uç noktasını (Sistem #17'nin şu an KAPSAM DIŞI bıraktığı) gerektirir. Bu pasoda render/collision/simülasyon MALİYETİ azalır, bellek AYRIMI aynı kalır — DoD'nin "bellek kullanımını optimize et" maddesi TAM karşılanmıyor, dürüstçe belirtildi |
| 2026-07-24 | `UProductionManager`/`UNPCManager`, boşaltılmış hücrelerdeki makine/NPC'leri `AdvanceProduction`/`AdvanceBrain` ile İLERLETMEZ — YENİ bir "duraklat" semantiği İCAT EDİLMEDİ | Her nesne zaten kendi `LastUpdateTime`'ını tuttuğundan (Sistem #9'un "gerçek geçen süre asla kaybolmaz" garantisi), hücre yeniden yüklenince bir SONRAKİ ilerletme çağrısı GERÇEK geçen süreyi otomatik telafi eder — mevcut mimariyle mükemmel uyumlu, sıfır yeni kod-yolu karmaşıklığı |
| 2026-07-24 | Dünya taraması `TActorIterator<ABuildingBase>`/`<ANPCCharacter>`/`<AStorageNode>` ile YAPILIR — hiçbir aktör sınıfına kayıt-kodu EKLENMEDİ | `TActorIterator` alt sınıfları da kapsar (AProductionMachine/AWindTurbine, ABuildingBase üzerinden) — BEŞ sistemin (7/9/13/15/8) HİÇBİRİNE dokunmadan kapsama sağlandı, sıfır regresyon riski |
| 2026-07-24 | **İnceleme bulgusu:** gizle/collision-kapat artık HER taramada (`RebuildCellRegistry`) HER aktöre KOŞULSUZ uygulanır — yalnızca hücre-seviyesi GEÇİŞTE (`SetCellLoaded`) DEĞİL | Eskiden: zaten-boşaltılmış bir hücreye YENİ bir aktör eklenirse (inşa edildi/doğdu) VEYA bir aktör (özellikle kendi `AIController`'ı `AdvanceBrain`'den BAĞIMSIZ hareket eden bir NPC) BAŞKA bir hücreye TAŞINIRSA, o aktör için hiçbir hücre-seviyesi GEÇİŞ tetiklenmediğinden görünürlüğü asla senkronize OLMAZDI — sessizce yanlış (görünür-ama-boşaltılmış VEYA gizli-ama-yüklü) kalırdı |
| 2026-07-24 | Sistem #17: DÜRÜST kapsam — yalnızca Zaman (`UTimeKeeper::TotalGameSeconds`) + Oyuncu (konum/can/vücut sıcaklığı/envanter) kaydediliyor. İnşaat/Üretim/Lojistik/Kaynak/Hava/NPC/Görev durumu BU PASODA kaydedilmiyor | MIMARI'nin "tam oyun durumu" DoD'si GERÇEK anlamda HER sistemin kendi kaydet/yükle uç noktasını eklemesini gerektirir — bu, mekanizmanın (sıkıştırma/sürüm/bozulma/yuva) doğru kurulduğu bu pasodan SONRA, gerçek ihtiyaç ortaya çıktıkça (her sistem kendi ADR'siyle) genişletilebilir bir seam. `UTimeKeeper`'ın `GetTotalGameSeconds`/`SetTotalGameSeconds`'ı Sistem #1'den BERİ tam bu amaç için hazır bekliyordu |
| 2026-07-24 | Async serileştirme (DoD'nin önerdiği, Ölçeklenebilirlik Riski'nin çözümü) BU PASODA yazılmadı — kayıt/yükleme senkron | Bu pasodaki yük (Zaman+Oyuncu) küçük, frame-drop riski yok; `UGameplayStatics::AsyncSaveGameToSlot`/`AsyncLoadGameFromSlot` motor API'leri zaten var ve hazır — gerçek ihtiyaç (daha fazla sistem eklenip yük büyüyünce, MIMARI'nin "500 bina+makine=100+MB" riski gerçekleşince) ortaya çıkınca eklenebilir |
| 2026-07-24 | "Bulut senkronizasyonu (temel)" DoD maddesi HİÇ yazılmadı | Projede HİÇBİR cloud platform entegrasyonu (Steam/EOS) yok — o Sistem #28'i (Steamworks) bekliyor. Konuşacak bir backend olmadan "temel" bile olsa bir cloud-sync API'si icat etmek sahte/test edilemez kod olurdu — CLAUDE.md'nin "asla var olmayan dosya/fonksiyon uydurma" kuralına aykırı düşerdi |
| 2026-07-24 | **En ciddi inceleme bulgusu (motor kaynağından doğrulandı):** `FArchiveLoadCompressedProxy`, bozuk/rastgele baytlarda `GetError()` ile hata DÖNMEZ — `FArchive::SerializeCompressedNew`'in başlık-etiketi eşleşmezse `Fatal`-seviyeli `UE_LOGF` ile DOĞRUDAN ÇÖKER (`LowLevelFatalError`); ayrıca kesilmiş veride ayrı bir `check()` de çökebilir. `SurvivalSave::DecompressSaveObject` artık motorun kodunu çağırmadan ÖNCE kendi imza+CRC32+uzunluk zarfını doğruluyor | Bu motor API'si "güvenilir/kendi ürettiğimiz" veri için tasarlanmış, GÜVENİLMEYEN keyfi baytlara (kayıt dosyası bozulması/kesilmesi/yanlış dosya) karşı DAYANIKLI DEĞİL — `Decompressor.GetError()` konstrüktörden hemen sonra kontrol edilse bile HER ZAMAN false döner (ölü kod, ayrı bir küçük bulgu). CRC32 eşleşmesi, baytların KENDİ ürettiğimiz baytlarla AYNI olduğunu garanti ettiğinden bu noktadan sonra motor çağrısı güvenli — kendi bozuk-veri testimiz (`Garbage.Init(0xFF,64)`) düzeltmeden ÖNCE bu haliyle otomasyon sürecini GERÇEKTEN çökertirdi |
| 2026-07-24 | `ApplySaveDataToLiveSystems`, oyuncu piyonunu her şeyden ÖNCE bulur — bulunamazsa `UTimeKeeper` dahil HİÇBİR canlı sistem değiştirilmeden erken çıkılır | İnceleme bulgusu: eskiden `UTimeKeeper` oyuncu-kontrolünden ÖNCE güncelleniyordu — oyuncu bulunamazsa fonksiyon `false` dönerken saat zaten değişmiş olurdu, `false`'un "hiçbir şey değişmedi" sözleşmesini (header yorumu) ihlal edip yarı-yüklü bir duruma yol açardı |
| 2026-07-24 | `InventoryComponent::RestoreSlots`, BOŞ kalan slotlar için de `OnInventoryChanged` yayınlar (`RemoveItem`'daki desenle AYNI) | İnceleme bulgusu: yalnızca dolu slotlar yayın alıyordu — önceki oturumda dolu, kaydedilen durumda boş bir slot için UI/dinleyiciler hiç haberdar edilmez, eski (artık yanlış) içeriği göstermeye devam ederdi |
| 2026-07-24 | Sistem #16: MIMARI'nin "öncelik kuyruğu (heap)" imasının YERİNE düz `TArray<FTaskDefinition>` + doğrusal tarama (`SurvivalTaskScheduler::FindBestEligibleTaskIndex`) | Beceri-eşleştirme VE backoff-uygunluğu birer FİLTRE — bir öncelik-heap'inde "en üstteki eleman uygun değilse bir altına bak" işlemi heap düzenini bozmadan yapılamaz (deheap/reheap gerektirir, karmaşıklık kazancı yok). Beklenen ölçekte (bir yerleşimdeki onlarca bekleyen görev) O(n) doğrusal tarama zaten önemsiz maliyette — Wind'in stateless-gust, Snow'un global-depth kararlarıyla aynı sınıf "MIMARI taslağını basitleştir" kararı |
| 2026-07-24 | `UNPCBrain::WorkElapsedTime`/`WalkingElapsedTime`, Hurt/Sleeping gibi kesintilerde SIFIRLANMAZ — yalnızca GERÇEKTEN yeni bir görev (`AssignTask`) sıfırlar | DoD'nin "görev durumunu devam ettir" maddesi böyle okundu: bir NPC işi yarıda yaralanıp iyileşirse KALDIĞI yerden devam etmeli, baştan başlamamalı. Zamanlayıcılar yalnızca ilgili durumdayken (Working/Walking) arttığından, kesinti sırasında doğal olarak "donarlar", ekstra kod gerekmedi |
| 2026-07-24 | `UNPCBrain` artık İKİ ayrı subsystem'e kayıtlı — `UNPCManager` (kare-bölümlemeli tikleme) ve `UTaskScheduler` (görev tamamlama/başarısızlık delegate aboneliği) — TEK bir subsystem'e ikisi de yüklenmedi | İki farklı sorumluluk (yaşam-döngüsü tetiklemesi vs. iş atama) — Tek Sorumluluk ilkesi; `AssignTask`/`ClearTask` gibi Brain-seviyesi API zaten var, Scheduler yalnızca BUNLARI çağırıyor, kendi paralel bir görev-durumu YOK |
| 2026-07-24 | **İnceleme bulgusu:** `UNPCBrain::ClearTask()` artık kendi içinde `EvaluateState()`'i HEMEN yeniden çağırır | Görev başarısız/tamamlanıp temizlendiğinde `CurrentState`/`AIController` bir SONRAKI `AdvanceBrain`'e (kare-bölümlemede bir tam döngü) kadar senkron KALMAZDI — piyon Walking'de başarısız olunca artık-geçersiz hedefe görünür şekilde yürümeye devam ederdi. `ClearTask()`'ın kendisini düzeltmek (yalnızca `CompleteCurrentTask`/`FailCurrentTask`'ı değil) dışarıdan doğrudan çağrılan bir `ClearTask()`'ı da aynı korumaya kavuşturdu |
| 2026-07-24 | `UTaskScheduler::UnregisterBrain`, aktif bir görevi `HandleTaskFailed` ile AYNI backoff'lu requeue mantığıyla geri kuyruğa alır | İnceleme bulgusu: NPC yok edildiğinde (`EndPlay`) aktif göreve ne olacağı BELİRSİZDİ — sessizce düşerse iş kalıcı olarak kaybolur, hiçbir başka NPC'ye asla teklif edilmezdi. Symmetri: "görev sahibi artık yok" da bir tür başarısızlıktır, aynı yoldan geçmeli |
| 2026-07-24 | Sistem #15: NPC'nin canı MIMARI'nin ayrı `float Health` taslağı YERİNE Sistem #12'nin `UHealthComponent`'i NPC'ye de eklenerek YENİDEN KULLANILDI | Bileşen zaten "yeniden kullanılabilir" olarak tasarlanmıştı (Sistem #12 ADR'sinde açıkça belirtildi) — NPC-özel ikinci bir can alanı, aynı `TakeDamage`/`Heal`/`OnDeath` mantığının kopyası olurdu |
| 2026-07-24 | MIMARI'nin polymorphic `UNPCTask : UObject` (+`Execute`/`IsComplete` virtual) hiyerarşisi YERİNE minimal `FNPCTaskData` struct'ı (TaskID/TargetLocation/Priority) — gerçek kuyruk/öncelik/beceri-eşleşmesi Sistem #16'ya (`UTaskScheduler`, henüz YAZILMADI) bırakıldı | MIMARI'nin kendisi bu ikisini AYRI sistemler olarak numaralandırıyor (#15 görev YÜRÜTME, #16 görev PLANLAMA/atama) — Sistem #15'in DoD'si "NPC bir göreve gidip yapabilsin"i istiyor, kuyruk/beceri-eşleştirme mantığını değil; MIMARI'nin kendi ayrımına sadık kalındı |
| 2026-07-24 | Yürüme (Walking durumu) motorun kendi NAVMESH + `AAIController::MoveToLocation`'ı ile yapılır — özel bir yol bulma/graf sistemi YAZILMADI | "Motorda zaten varsa yeniden yazma" kuralı (Sistem #2'nin `UInputHandler` kararıyla aynı sınıf) — pathfinding notoriously karmaşık bir alan, UE'nin kendi Recast/Detour tabanlı sistemi zaten savaş-test edilmiş |
| 2026-07-24 | `UNPCManager` (`UWorldSubsystem`+`FTickableGameObject`) Sistem #9'un kare-bölümleme desenini BİREBİR tekrarlıyor — `SurvivalProduction::ComputeBatchSize` NPC'lere özel ikinci bir batch-hesaplama fonksiyonu YAZILMADAN doğrudan yeniden kullanıldı | Fonksiyon zaten tamamen genel (yalnızca `TotalCount`/`FramesPerCycle` aritmetiği, Production'a özgü hiçbir şey yok); CLAUDE.md'nin "yeni her sürekli-çalışan sistem frame bölümlemeli" kuralı NPC'ler için de PROAKTİF olarak uygulandı (Sistem #10'un `PruneStaleRegistrations`'ı gibi, bulgu beklenmeden) |
| 2026-07-24 | **İnceleme bulgusu, aynı kök nedenin iki görünümü:** `UNPCBrain::AssignTask`, NPC zaten `Walking` durumundayken çağrılırsa hedefi güncelledikten SONRA `AAIController::MoveToLocation`'ı ELLE yeniden tetikler; yeni `OnStateExited(OldState)`, `Walking`'den (varmadan) başka bir duruma her çıkışta `StopMovement()` çağırır | `EvaluateState`'in "durum DEĞİŞTİ mi" kontrolü Walking→Walking'i (görev yeniden atansa bile) bir değişiklik SAYMAZ — bu yüzden tek başına `OnStateEntered` (yalnızca durum-GİRİŞinde çalışır) ne yeniden-hedeflemeyi ne de görev-iptalinde durdurmayı yakalayabilir; ikisi de EvaluateState'in durum-geçiş bekçisinin DIŞINDA, kendi tetikleyicileriyle (AssignTask çağrısı / OnStateExited) ele alınmalı |
| 2026-07-24 | `UNPCBrain::BeginPlay`/`EndPlay` `protected` (public DEĞİL) — `AProductionMachine`/`UHealthComponent` ile aynı görünürlük | İnceleme bulgusu: public'te kalırsa dışarıdan (ör. gelecekteki bir görev sistemi) yanlışlıkla doğrudan çağrılıp `UNPCManager`'dan sessizce kayıt silinebilirdi |
| 2026-07-24 | Kare-bölümlemeli seyrek güncellemede bir durum geçişiyle ÇAKIŞAN yorgunluk/moral payı, `AProductionMachine::Tick_Production`'ın döngü-bölme çözümünün AKSİNE, ESKİ değil YENİ duruma atfedilir (bilinçli kabul edilen basitleştirme, DÜZELTİLMEDİ) | Production'daki döngü-bölme sabit bir `ProductionTime` süresine göre dilimlenebiliyordu; NPC'nin durum değişimleri (can/saat/konum kaynaklı) sabit bir döngü uzunluğuna sahip değil — "geçişin TAM olarak ne zaman olduğunu" geriye-türetmek orantısız karmaşıklık gerektirirdi, etki `FramesPerCycle=60` ile sınırlı ve kendiliğinden düzelen (bir sonraki advance'de biriken hata yok) |
| 2026-07-24 | Sistem #14: MIMARI'nin `FSnowAccumulationGrid` (`TMap<FIntPoint,float> SnowDepth`, per-hücre ızgara) taslağı YERİNE TEK global `float CurrentSnowDepth` (Sistem #10'un `FThermalBudget::CurrentTemperature`'ıyla AYNI desen) | Bu projede hava durumu VE ortam sıcaklığı zaten TAMAMEN global (konumdan bağımsız) — per-hücre bir ızgara, HER hücre HER ZAMAN aynı değere yakınsayacağından hiçbir simülasyon değeri katmadan yalnızca aynı float'ın kopyalarını tutardı. Konumsal olan TEK unsur (çığ riski/yamaç eğimi) ayrı bir ızgara GEREKTİRMEZ — oyuncunun bulunduğu noktada anlık bir yer-normali line-trace'iyle sorgulanır. Gerçek mekansal hava çeşitliliği (biome/yükseklik) eklenirse ızgara yeniden değerlendirilebilir |
| 2026-07-24 | Kar birikimi/erimesi Hava Durumu'nun `Condition`/`Precipitation` VE Sıcaklık'ın `GetAmbientTemperature()`'ını (Sistem #11/#12) DOĞRUDAN yeniden kullanır — paralel bir "kaç derece"/"ne kadar yağış" kavramı YOK | Tek-doğruluk-kaynağı ilkesi (Sistem #12'nin Weather-Temperature ilişkisiyle aynı) — Kar, kendi hava/sıcaklık modelini icat etmek yerine zaten var olan iki sistemin çıktısını tüketiyor; bu üç sistem arasında GERÇEK bir entegrasyon zinciri kuruyor (Faz 1'in üç ayrı sistemin birbirine hiç bağlı olmadığı durumundan NİTELİKSEL OLARAK FARKLI) |
| 2026-07-24 | Oyuncunun kar-kaynaklı hareket cezası, `UCharacterMovementComponent::MaxWalkSpeed`'i HER Tick doğrudan çarpanla ölçekler; ilk gözlemlenen (kar'dan ETKİLENMEMİŞ) değer `TWeakObjectPtr<ACharacter>` ile HANGİ piyon için önbelleklendiği de takip edilerek bir kez yakalanır | İnceleme bulgusu: yalnızca bir bool bayrakla ("ilk gördüğümde yakala, bir daha asla") önbellekleseydik, piyon değişince (henüz erişilemez bir gelecek özellik: respawn/OpenLevel) YENİ piyonun kendi hızı hiç yakalanmaz, ESKİ piyonun değeriyle sessizce ezilirdi — bu projenin "yok edilen/değişen aktöre bağlı önbellek/kayıt bayatlar" hata sınıfının (normalde bir `TWeakObjectPtr` registry'sinde görülen) bir başka görünümü, henüz TETİKLENMEDEN proaktif olarak kapatıldı |
| 2026-07-24 | İnşaat-engeli tek-nokta olarak `UConstructionComponent::EvaluatePlacementValidity`'e (Sistem #7) eklendi — hem ghost-önizleme hem `ConfirmPlacement` AYNI kontrol yolundan geçtiği için tek bir ek if bloğu yeterli | Grid-doluluk kontrolüyle AYNI yerde, aynı desen (erken `return false`) — Sistem #7'nin zaten kurduğu "tek doğrulama noktası" mimarisi yeni bir sistemin ek kısıtını hiçbir kod tekrarı olmadan kabul edebiliyor |
| 2026-07-24 | Çığ riski değerlendirmesi HER Tick DEĞİL, `AvalancheCheckInterval` (varsayılan 5sn) periyodunda yapılır ve yalnızca durum DEĞİŞTİĞİNDE (edge-triggered) yayınlanır | Yer-normali line-trace'i (`LineTraceSingleByChannel`) her Tick çalıştırmak gereksiz maliyetli — Sistem #11'in `EvaluationInterval` periyodik-değerlendirme deseniyle VE Sistem #10'un `OnScarcityStateChanged` edge-triggered broadcast felsefesiyle aynı, ikisinin birleşimi |
| 2026-07-24 | Sistem #13: `UWindSimulation` (`UGameInstanceSubsystem`) BİLEREK `FTickableGameObject` DEĞİL — küresel yön/hız her sorguda `UWeatherSimulation::GetCurrentState()`'ten okunur (Sistem #11'in zaten sahip olduğu `WindSpeed`/`WindDirection`, paralel bir "küresel rüzgar" kavramı YOK), yerel hamle (gust) konum+oyun-zamanına göre TAMAMEN durumsuz/deterministik hesaplanır (RNG yok, `TArray<FVector> LocalGustsPerGrid` gibi bir kayıt/temizleme yaşam döngüsü YOK) | Sistem #10/#11/#12'nin HER biri kendi durumunu tutup Tick'te ilerletiyordu; Rüzgar'da ilerletilecek gerçek bir durum yok (gust her an konum+zamandan yeniden türetilebilir) — gereksiz Tick makinesi eklemek "en küçük değişiklik" kuralını ihlal ederdi |
| 2026-07-24 | **KRİTİK inceleme bulgusu (canlıya geçmeden ÖNCE, review aşamasında yakalandı):** `AProductionMachine::AdvanceProduction`, `AWindTurbine`'ın override edebilmesi için `virtual` işaretlendi — YALNIZCA `GetProgress` işaretlenmişti, `AdvanceProduction` unutulmuştu | İki bağımsız mercek (ue-api, logic-lifecycle) BAĞIMSIZ olarak aynı hatayı buldu: düzeltilmeseydi ya derleme hatası (override specifier virtual olmayan bir metoda uygulanamaz) ya da (specifier'lar kaldırılsaydı) `UProductionManager::Tick`'in HER ZAMAN `AProductionMachine*` taban-sınıf işaretçisi üzerinden yaptığı çağrı sessizce taban sınıfın recipe-mantığına giderdi — türbin hiçbir hata/log vermeden asla enerji üretmezdi. Derleme + PIE ile düzeltmenin gerçekten override'ı çalıştırdığı kanıtlandı (`State=Running`, `CurrentOutput=27.75`) |
| 2026-07-24 | `AWindTurbine : AProductionMachine`, taban sınıfın recipe/girdi/çıktı makinesini (Sistem #9) KULLANMAZ — `AdvanceProduction`/`GetProgress`'i TAMAMEN farklı (rüzgar->enerji) bir mantıkla override eder, ama `UProductionManager`'ın kayıt+kare-bölümlemeli tetikleme altyapısını OLDUĞU GİBİ miras alır; ürettiği enerjiyi `UResourceSimulation::RegisterEnergyProducer`'a (Sistem #10) kaydeder | Yeni bir paralel tick sistemi VEYA yeni bir enerji-muhasebesi YAZILMADI — iki mevcut sistemin (kare-bölümleme + enerji bütçesi) yeniden kullanımı, CLAUDE.md'nin "sistemler arası iletişim = mevcut deseni kullan, yeni katman ekleme" ruhuyla birebir |
| 2026-07-24 | `SurvivalWind::ComputeTurbineOutput`'ta CutIn altı 0, Rated üstü/eşiti sabit `RatedOutput`, arada KÜBİK ramp (`Ratio³`) — gerçek türbinlerin çıkışı rüzgarın kinetik enerji akısıyla ~v³ orantılı olduğu için | Doğrusal bir ramp gerçekçi değildi; kübik form hem fiziksel olarak daha doğru hem de düşük-orta rüzgarlarda türbinin "zar zor çalışıyor" hissini, rated'a yaklaşırken hızlı yükselişi doğal olarak veriyor |
| 2026-07-24 | Rüzgarın yapılara etkisi (`GetWindLoadOnStructure`) yalnızca VERİ olarak hesaplanır — `ABuildingBase`'e bir "yapısal bütünlük/kararlılık" alanı EKLENMEDİ, 28 sistemde ayrı bir "Yapısal Bütünlük" sistemi de YOK | Spekülatif bir stabilite/hasar mekaniği icat etmek yerine (DoD'de "dengesizleştirme" var ama tüketecek bir sistem yok), Sistem #9'un tüketilmeyen ısı-üretici verisiyle AYNI desen: değer doğru hesaplanıyor, gerçek ihtiyaç (bir gelecekteki yapısal-bütünlük sistemi) ortaya çıkınca tüketilebilir |
| 2026-07-24 | Sistem #12: MIMARI'nin 28 sisteminde HİÇBİR YERDE oyuncu can/hasar bileşeni yoktu (Sıcaklık DoD'si "oyuncu hipotermi/sıcak-çarpması hasarı" istediği için gerçek bir mimari boşluk) — operatöre soruldu, **"Minimal `UHealthComponent` ekle"** seçildi: genel-amaçlı, `TakeDamage`/`Heal` GERÇEKTEN uygulanan miktarı döner (`AddItem`/`AddFuel` ile aynı sözleşme), `OnHealthChanged`/`OnDeath` broadcast eder | Gelecekteki tüm hasar kaynakları (NPC saldırısı, açlık, düşme hasarı vb. — Sistem #15+) bu TEK bileşeni tüketebilir; Sıcaklık'a özel, tek-kullanımlık bir "hipotermi canı" alanı YAZILMADI |
| 2026-07-24 | **KRİTİK inceleme sonrası PIE bulgusu:** `SurvivalTemperature::ComputeBodyTemperatureDelta`'ya ham ortam/hava sıcaklığı DOĞRUDAN hedef olarak verilmez — yeni `ComputeTargetBodyTemperature(EnvironmentTemp, ComfortMinTemp=10, ComfortMaxTemp=30)` araya girer: bant İÇİ ortam sıcaklığı hedefi DEĞİŞTİRMEZ (37°C sabit kalır), yalnızca bandın DIŞI (fazlalık/eksiklik) hedefe yansır | Ham modelle `DT_WeatherProfiles`'daki TÜM sıradan profiller (en yaygın `Clear`=22°C dahil) 35°C güvenli-alt-sınırın altında olduğundan oyuncu HER normal havada hipotermiden ölüyordu — PIE'de doğrudan gözlemlendi (can ~1 dakikada 0). Ne pure-fonksiyon birim testleri (her fonksiyon izole doğruydu) ne de küçük-mod inceleme (aynı izolasyonla çalıştı) bunu yakalayabildi — yalnızca uçtan-uca CANLI PIE testi ortaya çıkardı. Artık sıradan hava güvenli, yalnızca `Snowing`/`Blizzard`/`Heatwave` gerçek risk — Faz 2'nin "İlk kar fırtınası" çıkış kriteriyle kasıtlı olarak hizalı |
| 2026-07-24 | `ComputeBodyTemperatureDelta`, doğrusal (Euler, `Clamp(rate*dt,0,1)`) yaklaşım YERİNE TAM üstel sönüm (`1-exp(-rate*dt)`) kullanır | Küçük-mod inceleme bulgusu: doğrusal yaklaşım büyük `DeltaTime`'larda (kare düşüşü/donma) kare-hızından TAM bağımsız değildi — aynı gerçek süreyi az sayıda büyük adıma mı yoksa çok sayıda küçük adıma mı böldüğüne göre farklı sonuç verirdi. Üstel form kompozisyon altında matematiksel olarak DEĞİŞMEZ, ayrıca zaten [0,1) aralığında olduğundan ayrı bir `Clamp` de gerekmiyor |
| 2026-07-24 | `UTemperatureSimulation`, MIMARI'nin `GetInteriorTemperature(Building, DeltaTime)` imzası YERİNE anlık `GetEnvironmentTemperatureAt(Position)` kullanır — ayrı, kalıcı bir termal-kütle/gecikme modeli YOK | Bir bölgenin iç sıcaklığı her sorguda `ComputeInteriorTemperature` ile ANLIK hesaplanır (ortam+bölge temeli+ısı kaynağı, yalıtıma göre karışım); gerçek bir "ısınması/soğuması zaman alır" gecikme modeli DoD'de yok ve `AProductionMachine`'in enerji/ısı üretici içeriğiyle simetrik bir "bina ısıtıcısı" içeriği henüz yok — gerçek ihtiyaç ortaya çıkınca genişletilebilir |
| 2026-07-24 | Oyuncunun termal stresi (`ApplyPlayerThermalStress`) `UTemperatureSimulation::Tick`'in İÇİNDEN, `GetFirstPlayerController()->GetPawn()` ile HER Tick doğrudan bulunup işlenir — ayrı bir "oyuncu kaydı" (register/unregister) YOK | Projenin baştan beri geçerli "tek-oyunculu varsayım" kuralına paralel (bkz. `TimeKeeper` multiplayer notu); Sistem #10'un konsol-komutu yardımcılarındaki `FindPlayerPawn` desenini Tick'in kendisine taşıdı. Bölge kaydı (`ThermalZones`) ise Sistem #10'un self-pruning `TWeakObjectPtr` desenini AYNEN tekrarlıyor — çoklu bölge (bina) olabileceğinden gerçek bir registry gerekiyor |
| 2026-07-24 | Sistem #19: Projede HİÇ ses varlığı (`USoundWave`/`USoundCue`) yok (`Content/` tamamen sessiz) — tasarım baştan motorun GERÇEK API'lerine (`UGameplayStatics::PlaySoundAtLocation`/`PlaySound2D`/`CreateSound2D`+`UAudioComponent::FadeIn`/`FadeOut`) ince bir sarmalayıcı olarak kuruldu, kategori karışımı (`ComputeEffectiveVolume`) ve mesafe budaması (`ShouldCullSound`) SAF fonksiyonlarda | `USoundClass`/`USoundMix`/`USoundAttenuation` içerik varlıkları da yok — bunları MCP ile sahte/boş içerikle oluşturmak CLAUDE.md'nin "var olmayan varlık uydurma" kuralına aykırı düşerdi; kategori mikserinin KENDİSİ (TMap<ESoundCategory,float>) koda taşındı, gerçek ihtiyaç (ses içeriği) geldiğinde bu varlıklara geçiş yıkıcı olmayan bir genişleme |
| 2026-07-24 | `FSoundInstance::Sound`, MIMARI'nin önerdiği `USoundWave*` YERİNE `USoundBase*` | `PlaySoundAtLocation`/`PlaySound2D` zaten `USoundBase*` bekliyor (motor kaynağından doğrulandı) ve hem `USoundWave` hem `USoundCue` bundan türüyor — `USoundWave*` kullanmak her çağrıda gereksiz bir daraltma/cast dayatırdı |
| 2026-07-24 | **KRİTİK inceleme bulgusu (motor kaynağından doğrulandı):** `UGameplayStatics::SpawnSound2D`/`CreateSound2D` varsayılan `bAutoDestroy=true` ile çağrılıyordu — `AudioComponent.cpp::PlaybackCompleted` motorun döngüsüz bir parça bitince bileşeni KENDİLİĞİNDEN `DestroyComponent()` ettiğini (hiçbir `OnAudioFinished` bildirimi olmadan) doğruladı. `UMusicManager` artık `bAutoDestroy=false` geçiyor (yaşam döngüsü TAMAMEN elle, `Stop`+`DestroyComponent`) ve TÜM kontrol noktaları bare non-null YERİNE `IsValid()` kullanıyor | `MarkAsGarbage()` (motorun `DestroyComponent()` içinde çağırdığı) dış UPROPERTY/ham işaretçileri SENKRON sıfırlamaz — bu yüzden `CurrentMusicComponent` auto-destroy sonrası bir sonraki `PlayMusic`/`CrossfadeMusic` çağrısına kadar dangling kalabilirdi, bare `if (CurrentMusicComponent)` bunu YAKALAYAMAZDI. Bu, projenin tekrarlayan "yok edilen nesneye bağlı bayat kayıt/önbellek" hata sınıfının (Sistem #8/#14/#18'de görülen) ses sistemindeki görünümü |
| 2026-07-24 | **İnceleme bulgusu:** `UAudioManager::SetCategoryVolume` artık yeni `FOnCategoryVolumeChanged` (dynamic multicast, `BlueprintAssignable`) yayınlıyor; `UMusicManager` `Initialize`'da abone olup ÇALMAKTA olan parçanın sesini `SetVolumeMultiplier` ile ANINDA günceller | Eskiden `SetCategoryVolume` yalnızca haritayı güncelliyordu — zaten çalan bir müzik parçası bir sonraki `PlayMusic`/`CrossfadeMusic`'e kadar ESKİ sesle çalmaya devam ederdi (Master/Music kaydırıcısı hareket ettirilince hiçbir duyulabilir etki olmazdı). CLAUDE.md'nin "sistemler arası iletişim = delegate broadcast, doğrudan bağımlılık kurma" kuralının Ses sisteminde ilk somut uygulaması (`WorldPartitionHelper::OnCellLoadStateChanged`/`NPCBrain::OnTaskCompleted` ile aynı `DECLARE_DYNAMIC_MULTICAST_DELEGATE` deseni) |
| 2026-07-24 | `UMusicManager::PlayMusic`/`CrossfadeMusic`, `Spawn`/`CreateSound2D` NULL dönerse `CurrentMusic`'i de `nullptr`'a çeker (yalnızca `CurrentMusicComponent` değil) | İnceleme bulgusu: bileşen oluşturma başarısız olsa bile `CurrentMusic` yeni parçaya ayarlanıyordu — `GetCurrentMusic()` (BlueprintPure) hiç çalmayan bir parçayı "şu an çalıyor" gibi gösterirdi. İki alan artık HER ZAMAN birlikte tutarlı (ikisi de dolu ya da ikisi de boş) |
| 2026-07-24 | `UMusicManager::CrossfadeMusic`'te eski bileşenin temizliği (`Stop`+`DestroyComponent`) `FTimerHandle`+`TWeakObjectPtr` ile ELLE yapılır — `FadeOut` motor kaynağından doğrulandığı üzere bileşeni YOK ETMEZ, yalnızca susturur | Motorun kendi crossfade/mixer sistemi (`USoundMix`/`USoundClass`) yok (içerik yok); `FadeIn`/`FadeOut` ELLE hesaplanan bir "geçiş-ilerlemesi" YERİNE tercih edildi (motorun ses iş parçacığındaki gerçek fader'ını taklit etmek gereksiz/riskli olurdu) ama bu, bileşen ömrünü SAF fader mantığının dışında ayrıca yönetmeyi gerektiriyor |
| 2026-07-24 | "Ortam döngüleri (rüzgar/yağmur/makine)" ve "max 128 eşzamanlı ses" DoD maddeleri BU PASODA yazılmadı | Birincisi gerçek döngü sesi İÇERİĞİ gerektiriyor (yok) — `ABuildingBase`'in boş mesh alanıyla aynı dürüst-atlama gerekçesi. İkincisi motorun kendi `USoundConcurrency` sistemi zaten üstlenebilir VE içerik olmadan (hiç ses çalınamadığından) test edilemez bir sayaç eklemek anlamsız/doğrulanamaz kod olurdu |
| 2026-07-24 | Sistem #20: `UScreenBase` MIMARI'nin taslağından SAPMADAN yazıldı ama BİLEREK `Abstract` DEĞİL — proje hiç Widget Blueprint içeriğine sahip olmadığından, içeriksiz taban sınıfın kendisi `PushScreen(UScreenBase::StaticClass())` ile PIE'de gerçek bir widget olarak test edilebiliyor | `ABuildingBase`'in içeriksiz (mesh'siz) test edilebilirliğiyle AYNI gerekçe — gerçek görsel düzen (WBP_PauseMenu vb.) bu sınıfı Blueprint'te miras alıp içerik fazında eklenir |
| 2026-07-24 | **KRİTİK inceleme bulgusu (motor kaynağından doğrulandı):** `ScreenStack`/`ActiveWidgets` bileşen yaşam-döngüsünü yalnızca `IsValid()` ile takip ediyordu — bu, bir widget'ın `PopScreen`/`HideWidget` DIŞINDA (ör. kendi kapat düğmesinden doğrudan `RemoveFromParent`) kapanmasını ASLA yakalayamaz (widget hâlâ geçerli bir UObject, yalnızca Slate'ten kopuk). Her `UUserWidget`'ın sahip olduğu `OnNativeDestruct` olayına (`RemoveFromParent`'in SENKRON tetikledigi, `SObjectWidget` destructor'ından doğrulandı) abone olunarak düzeltildi — `UGameViewportSubsystem::OnWidgetRemoved`'a (motor-geneli, EngineSubsystem) BAĞIMLILIK GEREKMEDİ | Düzeltilmeseydi: bir gelecek WBP_PauseMenu'nun kendi X düğmesi `RemoveFromParent` çağırırsa, oyuncu görünmeyen bir menüde `FInputModeUIOnly`+görünür imleçte KİLİTLİ kalırdı (hareket/bakış çalışmaz) çünkü `ApplyInputModeForCurrentStack` yalnızca `PushScreen`/`PopScreen`'den tetiklenir. Per-widget `OnNativeDestruct`, motor-geneli bir subsystem'e abone olup elle unbind (Deinitialize/EndPlay) yönetmekten daha basit VE kendiliğinden güvenli (widget yok olunca olay da onunla birlikte ölür) |
| 2026-07-24 | `UHUDController::HideWidget`, kaydı diziden ÖNCE çıkarır, `RemoveFromParent`'i SONRA çağırır (`ScreenManager::PopScreen`'in zaten sahip olduğu sırayla AYNI) | `RemoveFromParent` `OnNativeDestruct`'i SENKRON tetikler — ters sırada yapılsaydı, `HandleWidgetDestructed` (aynı widget için) AYNI diziyi `HideWidget`'in kendi döngüsü HÂLÂ üzerinde çalışırken mutasyona uğratıp yeniden-giriş (reentrancy) hatasına yol açardı |
| 2026-07-24 | `UInventoryWidget`, `NativeDestruct()`'i override edip `OnInventoryChanged` aboneliğini burada da bırakır — yalnızca `SetBoundInventory`'nin tekrar çağrılmasına GÜVENMEZ | İnceleme bulgusu: widget `HideWidget`/`PopScreen` ile kaldırılınca (ama `SetBoundInventory(nullptr)` hiç çağrılmadan) envanterin (genelde oyuncuyla birlikte UZUN ÖMÜRLÜ) `OnInventoryChanged` abonelik listesinde kalıcı bir "ölü" giriş birikirdi |
| 2026-07-24 | `UInventoryWidget::BoundInventory`, MIMARI'nin taslağının aksine güçlü `TObjectPtr` DEĞİL — `ULogisticsNetwork`'ün `Source`/`Destination`'ıyla AYNI `TWeakObjectPtr` deseni; `GetBoundInventory()` `.Get()` ile GÜVENLE nullptr döner | `AContainerActor` gibi widget'tan BAĞIMSIZ yaşam döngülü bir aktöre ait olabilir — konteyner widget açıkken yok edilirse güçlü referans sarkan bir işaretçi tutardı, zayıf işaretçi ise `IsValid()` gerektirmeden doğal olarak güvenli davranır |
| 2026-07-24 | `UScreenManager::ApplyInputModeForCurrentStack`, en üst ekrana `FInputModeUIOnly::SetWidgetToFocus` ile odak verir — AMA yalnızca `SWidget::SupportsKeyboardFocus()` true dönerse | **PIE canlı testinde YAKALANDI:** odak kontrolü olmadan içerik-boş `UScreenBase` (odaklanacak gerçek alt widget'ı yok) için motor "Attempting to focus Non-Focusable widget" HATASI logluyordu. `SupportsKeyboardFocus()` kontrolüyle düzeltilip PIE'de hatasız olduğu yeniden doğrulandı — gerçek içerik (odaklanabilir butonlar) eklenince otomatik olarak odak alacak |
| 2026-07-24 | `UHUDController::GetOwningPlayerController`'ın `Pawn` henüz Possess edilmemişse (ör. çok erken çağrılırsa) sessizce nullptr dönmesi DÜZELTİLMEDİ, yalnızca dokümante edildi | İnceleme bulgusu minör olarak işaretlendi: zaten bir Warning logluyor (tam sessiz değil), şu an `ShowWidget`'ı `BeginPlay`'den önce çağıran hiçbir kod yok (yalnızca PIE konsol komutları, Possess çoktan tamamlanmış olur) — gerçek bir tetikleyici olmadan bir sıraya-alma/gecikmeli-çağrı mekanizması icat etmek "gerekmedikçe soyutlama ekleme" kuralına aykırı düşerdi |
| 2026-07-24 | MIMARI'nin `UInventoryGridPanel` (gerçek ızgara/ikon UMG düzeni) alanı hiç YAZILMADI — motorda böyle bir sınıf yok (MIMARI'nin kendi örnek/uydurma taslağı) | Gerçek bir ızgara düzeni Widget Blueprint içerik çalışması gerektirir (CLAUDE.md: "C++ çekirdek sistemler + Blueprint içerik/görsellik") — `UInventoryWidget` yalnızca veri-bağlama katmanı (`OnBoundSlotChanged` BlueprintImplementableEvent) sağlar, gerçek görsel WBP_Inventory içerik fazında bu sınıfı miras alır |
| 2026-07-25 | Faz 1 entegrasyon borcu (operatör Faz 2 sonrası bu çalışmayı seçti): `FBuildingDefinition::BuildingClass` (`TSubclassOf<ABuildingBase>`, boş=geriye-uyumlu `ABuildingBase`) + `ProductionRecipeIDs` eklendi; `ConfirmPlacement` artık `Def.BuildingClass`'a göre spawn ediyor | `ABuildingBase::BeginConstruction`'ın zaten `virtual` olması (Sistem #7'de bilerek bırakılmıştı) sayesinde `AProductionMachine`'in kendi override'ı (Super çağırıp `ProductionRecipeIDs`'i `AvailableRecipeIDs`'e ekleyen) hiçbir `ConstructionComponent` değişikliği GEREKTİRMEDEN otomatik çalışıyor — Sistem #7/#9 arasındaki bağımlılık yönü (Production→Construction, ters değil) korunuyor |
| 2026-07-25 | `AStorageNode`'a `BindExternalStorage`/`SetNodeType` eklendi — `AProductionMachine::BeginPlay` bunlarla `InputBuffer`/`OutputBuffer`'ı `MachineInput`/`MachineOutput` tipli iki proxy düğümle lojistik ağına kaydediyor. `ULogisticsNetwork`'ün KENDİSİ (Register/FindBestDestination/RequestTransport/CompleteTransport) HİÇ DEĞİŞTİRİLMEDİ | Sistem #8'in `EStorageNodeType::MachineInput`/`MachineOutput` değerleri baştan "Sistem #9 tüketecek" yorumuyla tanımlanmıştı — burada İLK KEZ gerçekten kullanıldı. Yeni bir arayüz/soyutlama (ör. `IInventoryProvider`) icat etmek yerine mevcut, test edilmiş `AStorageNode` sınıfının kendisini (dışarıdan envanter bağlayabilecek şekilde küçük bir ek yetenekle) yeniden kullanmak, `AProductionMachine`'in giriş/çıkışının iki ayrı yönlü (biri yalnızca alıcı, biri yalnızca verici) olması gerçeğiyle de doğal olarak örtüşüyor |
| 2026-07-25 | **KRİTİK inceleme bulgusu (motor kaynağından doğrulandı):** proxy düğümler `World->SpawnActor` YERİNE `SpawnActorDeferred`+`FinishSpawning` ile oluşturulur — `NodeType`/`BindExternalStorage` `BeginPlay` (dolayısıyla `ULogisticsNetwork::RegisterNode`→`RebuildAcceptorCache`) çalışmadan ÖNCE ayarlanır | Sıradan `SpawnActor`, dünya zaten `BeginPlay` yapmışken yeni aktörün `BeginPlay`'ini SENKRON tetikler — `SetNodeType` çağrısı (spawn'dan SONRA) çalıştığında `AcceptorCache` zaten (yanlış, varsayılan `Container` tipiyle) kurulmuş olurdu; `SetNodeType`'ın kendisi de bir rebuild TETİKLEMEZ. `MachineOutput` için bu, ağın çıktı arabelleğini yanlışlıkla bir teslimat hedefi sayması anlamına gelirdi — şu an yalnızca `CanAcceptItem`'ın ayrıca CANLI kontrol etmesi sayesinde MASKELENEN, tasarlanmamış bir tesadüf. `SpawnActorDeferred` bu projede İLK KEZ kullanıldı |
| 2026-07-25 | `AWindTurbine::ShouldRegisterLogisticsNodes()` `false` döner — `InputBuffer`/`OutputBuffer`'ı (Sistem #13'ten beri) hiç KULLANMADIĞINDAN, kalıcı boş proxy düğümler ağı "sessizce hiçbir şey işlemeyen ama yine de geçerli bir teslimat hedefi" ile kirletmesin diye | `AdvanceProduction`/`GetProgress`'teki `virtual` override deseninin (Sistem #13) AYNISI — taban sınıf davranışı varsayılan, alt sınıf kendi gerçek ihtiyacına göre KAPATIR. Yeni bir bayrak/flag alanı yerine zaten var olan virtual-override felsefesiyle tutarlı |
| 2026-07-25 | `AConveyorBelt` (fiziksel/görsel taşıma) bu pasoda da YAZILMADI — Faz 1 entegrasyon borcunun (1) ve (2) numaralı kısımları kapatıldı, (3) kasıtlı olarak AÇIK bırakıldı | Sistem #8'in kendi ADR'sindeki gerekçe DEĞİŞMEDİ: gerçek içerik (mesh/spline animasyonu) gerektirir, teslimat zaten mesafe/hız zamanlayıcısıyla FONKSİYONEL çalışıyor — bu yalnızca görsel bir yükseltme, davranış değişikliği değil |
| 2026-07-25 | PIE doğrulaması İKİ oturumda TAMAMLANDI: ilk oturumda `build_place OdunFirini` "malzeme yetersiz" ile başarısız oldu (oyuncu Odun toplamadan denedi); operatör editörü kapatıp sonra yeniden açtı, konteyneri ÖNCE topladı, TEKRAR denedi | İkinci denemede `build_place OdunFirini` başarılı oldu: "Makine kaydedildi (toplam 3)" + "Dugum kaydedildi (toplam 5)→(toplam 6)" (yeni makinenin İKİ proxy düğümü de kaydoldu) + "Insaat: basarili" logları VE MCP sahne taramasının yeni `ProductionMachine_1` aktörünü bulması, (1) numaralı entegrasyonu (build_place→AProductionMachine spawn) uçtan uca kanıtladı — (2) numaralı entegrasyon (lojistik kaydı) zaten ilk oturumda önceden var olan bir makine üzerinde kanıtlanmıştı |
| 2026-07-25 | Profiling ölçüm yöntemi sertleştirildi: `stat fps` PIE penceresi OS-odaksızken GÜVENİLMEZ | İlk ölçüm 100 bina+50 makine ile 8-20 FPS gösterdi (kod-tarafı kök neden aranıp `WorldPartitionHelper`'a şüphelenildi), `stat unit` ile Game+Draw+GPU toplamının (~14-21ms) Frame süresinden (118ms) tamamen kopuk olduğu görülünce editörün "Use Less CPU when in Background" throttle'ı teşhis edildi; pencere odaklanınca aynı sahne 59.93 FPS/16.69ms ölçtü — kod hiç değiştirilmedi, gerçek darboğaz yoktu |
| 2026-07-25 | Operatör kararı: oyuncu ölünce son kayıt noktasına döner (respawn/ölüm-ekranı YERİNE) | `USaveGameManager::RevertToLastSave()` + `APlayerCharacter::HandleDeath()` (`HealthComponent::OnDeath`'e tek abone — bileşen genel-amaçlı kalmaya devam ediyor, ölüm-sonrası politikası `PlayerCharacter`'da) |
| 2026-07-25 | Kayıt noktasına dönüşte `AActor::SetActorLocation` YERİNE `TeleportTo` kullanılıyor | İnceleme bulgusu (motor kaynağından doğrulandı): `SetActorLocation` `TeleportSucceeded`/`UCharacterMovementComponent::OnTeleported` zincirini tetiklemiyor — oyuncu ışınlandıktan sonra ölüm anındaki eski hız/düşme moduyla hareket etmeye devam ederdi; `TeleportTo` bunu VE geometriye-gömülme kontrolünü (`FindTeleportSpot`) tek çağrıda çözüyor |
| 2026-07-25 | Kayıt yoksa (ilk ölüm) oyuncu tam canla AYNI yerde devam eder, kalıcı 0-HP'de bırakılmaz | İnceleme bulgusu (kritik): `TakeDamage`/`Heal` `IsDead()` iken kalıcı no-op olduğundan, dönülecek kontrol noktası yokken oyuncuyu 0-HP'de bırakmak kalıcı, kurtarılamaz bir soft-lock olurdu |
| 2026-07-25 | `HandleDeath` HER dönüş türünde (başarılı kayıttan veya kayıtsız fallback'ten fark etmeksizin) `VücutSıcaklığı`'nı 37°C'ye sıfırlıyor | PIE'de CANLI bulunan bulgu: yalnızca canı geri getirmek yetmiyordu — ölüm sebebi (hipotermi) hâlâ sürüyorsa bir sonraki karede `TemperatureSimulation` tekrar hasar verip `OnDeath`'i yeniden tetikliyor, sonsuz ölüm-diriliş döngüsü oluşuyordu (canlı testte onlarca tekrarla doğrulandı, statik inceleme bunu kaçırmıştı). Kod tabanındaki TEK hasar kaynağı hipotermi/aşırı-sıcak olduğundan güvenli bir varsayım — yeni bir hasar kaynağı eklenirse yeniden değerlendirilmeli |
| 2026-07-25 | Debug-çizim (`DrawDebugSphere`/`DrawDebugLine`) her tick yeniden çizilirken `LifeTime` olarak `DeltaTime` verilir, `0.0f` DEĞİL | İnceleme bulgusu (motor kaynağından doğrulandı): `0.0f`, "tek kare" anlamına gelmiyor — `DrawDebugHelpers.cpp::GetDebugLineLifeTime`, `LifeTime<=0` olduğunda çizimi `ULineBatchComponent::DefaultLifeTime`'a (sabit 1.0sn) düşürüyor, her çağrı ÖNCEKİni silmeden yeni segment ekliyor — 60 FPS'te ~60 çakışan kopya birikip hareketli nesneler "iz" gibi görünüyordu (`logistics_visualize` incelemesinde bulundu) |
| 2026-07-25 | `DrawDebugString`'e her etiket için kendi düğüm/aktörü `TestBaseActor` olarak verilir, `nullptr` DEĞİL | İnceleme bulgusu (motor kaynağından doğrulandı): `nullptr` verilince TÜM etiketler aynı aktöre (`WorldSettings`) bağlanıyor, motorun aktör-başına 128-etiket sınırı (`r.DebugSafeZone.MaxDebugTextStringsPerActor`) aşılınca fazlası hiçbir uyarı vermeden çizilmiyordu |
| 2026-07-25 | Anonim ad alanındaki (`namespace { ... }`) dosya-yerel yardımcı fonksiyonlara sistem-özel isim verilir (ör. `FindWindPlayerPawn`), genel isim (`FindPlayerPawn`) DEĞİL | `ResourceSimulation.cpp` ve `WindSimulation.cpp`'nin ikisi de anonim ad alanında aynı isimde farklı-dönüş-tipli bir `FindPlayerPawn` tanımlamıştı — normalde ayrı çeviri birimleri oldukları için çakışmazlardı ama Unreal'ın Adaptive Unity Build'i (git-durumuna göre hangi dosyaların birleştirileceğini seçiyor) bu turda ikisini AYNI derleme birimine düşürünce derleme koptu (önceki hiçbir build'de bu ikisi aynı gruba düşmemişti) — projede aynı desen (dev-komut dosyası başına yerel `FindPlayerPawn` yardımcı fonksiyonu) tekrarlanıyor, gelecekte üçüncü bir sistem aynı ismi kullanırsa yine patlayabilir |
| 2026-07-25 | `DevTools/ScopedTimer.h` (`SURVIVAL_SCOPED_TIMER`) yalnızca Development/DebugGame/Editor için — Shipping'de çalışmaz, `bUseLoggingInShipping` AÇILMADI | İnceleme bulgusu (motor kaynağından doğrulandı): proje `Target.cs`'lerinde `bUseLoggingInShipping` hiç ayarlanmamış (motor varsayılanı: kapalı) → Shipping build'de `NO_LOGGING=1` → `UE_LOG` (Warning dahil, yalnız Fatal hariç) derleme zamanında tamamen elenir. Bunu şimdi açmak (tüm oyun için Shipping'de logging'i etkinleştirmek) kapsam dışı, ayrı ve büyük bir proje kararı — projedeki TÜM diğer dev-konsol komutları zaten aynı şekilde yalnızca Development/Editor'da anlamlı, bu yeni bir kısıtlama değil |
| 2026-07-25 | `WorldPartitionHelper::RebuildCellRegistry` ilk gerçek ölçümü: 0.03-0.06 ms/çağrı (mevcut sahne ölçeği), darboğaz DEĞİL | Sistem #25'te (`SURVIVAL_SCOPED_TIMER_WARN`, 5ms eşik) PIE'de ölçüldü — daha önce (profiling FPS incelemesinde) "kare-bölümlemesiz periyodik tarama, ölçülmedi" olarak dokümante edilmiş açık nokta böylece kapandı; aktör sayısı çok büyürse (100+ bina/makine/NPC) tekrar ölçülmeli, kare-bölümleme o zaman gerekebilir |
| 2026-07-25 | Sistem #23 (Hata İşleme & Log): MIMARI'nin `FGameLogger`/`FErrorHandler`/`FStructuredLogger`/`FCrashReporter` taslağı YAZILMADI, onun yerine whole-codebase çökme-güvenliği denetimi yapıldı | `FGameLogger` UE_LOG+LogSurvival* kategorileriyle çakışırdı (paralel sistem); `FErrorHandler::HandleNetworkDisconnect` olmayan Sistem #21'e hizmet ederdi; `HandleSaveCorruption` zaten Sistem #17'de var; `FStructuredLogger` tüketicisiz JSON altyapısı olurdu; `FCrashReporter::SendToDeveloper` backend gerektirir (yok) — UE'nin kendi `CrashReportClient`'i zaten aktif. Denetim (5 grup paralel, 80 araç çağrısı) SIFIR gerçek bulgu verdi — mevcut per-sistem adversarial-review sürecinin işe yaradığını doğruladı, kod değişikliği gerekmedi |
| 2026-07-25 | `content_validate` (Sistem #24) `#if WITH_EDITOR` ile sarmalı — modül runtime'da linklense de kontrol ANLAMSIZ olur | İnceleme bulgusu (motor kaynağından doğrulandı): cooklenmiş bir pakette Blueprint/WidgetBlueprint varlıklarının AssetRegistry sınıf-adı `BlueprintGeneratedClass`/`WidgetBlueprintGeneratedClass`'a değişiyor — bu, projenin en çok kullandığı iki kuralı (BP_/WBP_) cooklenmiş bir çalıştırmada sessizce geçersiz kılıp yine de "temiz" özet basardı |
| 2026-07-25 | `content_validate`'te tam-eşleşme (TMap) kuralları HER ZAMAN `DataAsset` soyağacı kontrolünden ÖNCE çalışır | PIE'de CANLI bulunan bulgu: `InputAction`/`InputMappingContext` gerçekten `UDataAsset`'ten türüyor — soyağacı kontrolü önce çalışınca bu ikisini (kendi özel `IA_`/`IMC_` kuralları varken) yanlışlıkla `DA_` bekler hale getirdi (sahte-ihlal) |
| 2026-07-25 | Denge geçişi #1'de `constructionTime` DEĞİŞTİRİLMEDİ (inşaat anlık kalıyor) | `BuildingBase.cpp`'nin kendi yorumunda alan "TÜKETİLMİYOR" diye önceden belgelenmiş (Sistem #7 kararı) — DataTable sayısını artırmak sessizce hiçbir şey değiştirmezdi; gerçek yapmak yeni kod (ConstructionComponent'e bekleme mekaniği) gerektirir, operatöre soruldu, yanıt gelmeyince en az müdahaleci seçenek uygulandı |
| 2026-07-25 | `DA_BalanceConfig` (MIMARI'nin Sistem #27 taslağı) YAZILMADI, denge değerleri per-sistem DataTable/Settings'te kaldı | Zaten çalışan, test edilmiş dağıtık deseni (DT_Recipes, DT_ProductionRecipes, *Settings.h) merkezi bir "paralel sistem" ile değiştirmek gerekçesizdi |
| 2026-07-26 | **Keşfedilen boşluk**: kodda (yeni eklenenler dahil) HİÇBİR ham madde için dünyada toplama/hasat mekanizması yok — mevcut `Odun`/`Tas` bile şu an yalnızca tarif/envanter/DataTable seviyesinde var, `AInteractableBase`'den türeyen bir ağaç/kaya/maden-yatağı actor'ü hiç yazılmamış, `give_item` gibi bir test-cheat komutu da yok | İçerik hacmi genişletmesi sırasında (yeni 13 RawMaterial'i uçtan uca PIE'de test etmeye çalışırken) fark edildi. MIMARI.md'de de planlanmamış (hasat/toplama/maden/avlanma kelimeleri hiç geçmiyor). Bu yüzden yeni tarif/üretim zincirleri yalnızca veri-seviyesinde doğrulandı (DataTable round-trip + referans bütünlüğü); gerçek bir "topla→zanaat et→üret" oynanış testi şu an hiçbir ham madde için mümkün değil. Ayrı, planlanmamış bir sistem (dünya kaynak-noktası / hasat) gerektirir — bu pasonun kapsamı dışında bırakıldı, gelecekte değerlendirilmeli |
| 2026-07-26 | **Takip**: yukarıdaki boşluk için 4 ajanlı bağımsız tasarım paneli (3 bağımsız mimari öneri + motor-kaynağı/kod-tabanı doğrulamalı yargılama-sentez) çalıştırıldı; sonuç MIMARI.md'ye "Sistem #29 — Hasat Düğümleri (Harvest Nodes)" olarak eklendi (`AHarvestNode`/`UHarvestNodeDatabase`/`UHarvestNodeManager`/`DT_HarvestNodes`, kök isim "Harvest" — "Resource" ile çakışmasın diye). Panel önceliği **ÇOK YÜKSEK** buldu: #21 (multiplayer)/#26 (CI/CD)/#28 (Steam)'den ÖNCE gelmeli, bu bir Faz 1 dikey-dilim borcu. Operatör kararı: şimdilik yalnızca MIMARI.md'ye dokümante et, kodlama AYRI bir pasoda yapılacak | Panelin tam gerekçesi + 3 önerinin karşılaştırması + 15 adımlık uygulama sırası bu konuşmanın kaydında; özet MIMARI.md Sistem #29 bölümünde. Sıradaki adım (kodlamaya başlanırsa): önce "Harvest" kökünün motor kaynağıyla çakışmadığını doğrula, sonra `DT_HarvestNodes`/`HarvestNodeDefinition.h`'tan başla |
| 2026-07-26 | Sistem #29 kodlandı ve 2-mercek (ue-api + logic-lifecycle) adversarial incelemeden geçirildi — ue-api merceği SIFIR bulgu verdi (API kullanımı temiz), logic-lifecycle merceği 4 gerçek bulgu buldu, dördü de düzeltildi: kısmi-AddItem-kabulünde sınırsız-çiftlik riski (`HasRoomFor` ön-kontrolü eklendi), geçersiz NodeID'de kalıcı bozuk-görünür aktör (BeginPlay'e gizle+collision-kapat eklendi), `YieldCountMin=0` kenar durumu (`NormalizeYieldRange` artık Min'i 1'e kelepçeliyor), `content_validate`'in vaat edilen ama uygulanmayan `DT_HarvestNodes` referans kontrolü (eklendi) | Bu, projenin "yeni sistem = pure function + review + fix" desenine tam uyumlu dördüncü örneği; ue-api merceğinin sıfır bulgu vermesi, motor-kaynağı-önce-doğrula disiplininin (referans dosyaları taklit ederek yazma) işe yaradığını gösteriyor |
| 2026-07-26 | Sistem #26 (CI/CD) `.github/workflows/ci.yml` sağlamlaştırıldı — `-TestExit` parametresinin `UnrealEditor-Cmd.exe` doğrudan çağrıldığında hiç işlenmediği (yalnızca RunUAT.exe konvansiyonu) motor kaynağından doğrulandı, kaldırıldı; `TestResults\index.json`'u ayrıca doğrulayan bir adım eklendi (yalnızca exit-code'a güvenmek çökme/sıfır-test durumlarını kaçırabilir) | Self-hosted runner kurulumu (bu makineyi kalıcı bir CI çalıştırıcısına çevirmek — güvenlik/kalıcılık açısından önemli bir karar) operatöre soruldu, bilinçli olarak ERTELENDİ; CI hâlâ yalnızca elle (`workflow_dispatch`) tetiklenebilir |
| 2026-07-26 | **Ayrı, önemli bulgu**: Sistem #29'u PIE'de canlı test ederken `UTimeKeeper::Tick`'in `DeltaTime`'ı hiç kelepçelemediği keşfedildi — editör MCP çağrılarına yanıt verirken oyun thread'i bloke olunca bir sonraki karede dev bir `DeltaTime` (dakikalarca) geliyor, x10 zaman ölçeğinde bu saati tek karede saatlerce ileri sıçratıyordu. Canlı gözlemlendi: PIE başlangıcından ~2 dakika sonra saat "gün 0 08:00"dan "gün 1 gece yarısı"na sıçradı, `NightIntensityLux=0.05` ile ekran tamamen karardı ve önce bu bir render/Lumen hatası sanıldı | `SurvivalTime::ClampFrameDeltaTime` (yeni saf fonksiyon, `TimeMath.h`, azami 1.0sn) eklenip `TimeKeeper::Tick`'te kullanıldı, birim testiyle (`DeltaTimeKelepcesi`) doğrulandı. Sistem #1'in (en temel, en çok bağımlı sistem) bu denli temel bir korumadan yoksun olması, editör/MCP-tabanlı test döngülerinin normal oynanışta rastlanmayacak donma senaryoları üretebildiğini gösteriyor — benzer bir kelepçe diğer Tick'li sistemlerde (ProductionManager, NPCManager, vb.) de YOK, ama onlar mutlak-zaman-damgası deseni kullandığından (bkz. WorldPartitionHelper notları) tek-kare sıçramaya karşı daha dayanıklılar; TimeKeeper'ın kendisi ise dogrudan biriktirdiği için savunmasızdı |
